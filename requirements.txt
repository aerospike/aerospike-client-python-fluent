# Runtime dependencies for aerospike-fluent-client-python
#
# PAC is declared in pyproject.toml (git URL). For a local PAC tree, see README
# "Local PAC checkout" or requirements-local.txt.

# ANTLR runtime for string-based DSL parsing
antlr4-python3-runtime>=4.13.0