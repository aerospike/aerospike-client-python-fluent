{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Aerospike Python Fluent Client","text":"<p>A fluent, chainable API for the Aerospike database, built on top of the Aerospike Python Async Client.</p>"},{"location":"#quick-example","title":"Quick Example","text":"AsyncSync <pre><code>import asyncio\nfrom aerospike_fluent import FluentClient, DataSet, Behavior\n\nasync def main():\n    async with FluentClient(\"localhost:3000\") as client:\n        session = client.create_session(Behavior.DEFAULT)\n        users = DataSet.of(\"test\", \"users\")\n\n        # Write\n        await (\n            session.upsert(users.id(1))\n            .bin(\"name\").set_to(\"Alice\")\n            .bin(\"age\").set_to(30)\n            .execute()\n        )\n\n        # Read\n        stream = await session.query(users.id(1)).execute()\n        result = await stream.first_or_raise()\n        print(result.record.bins)  # {'name': 'Alice', 'age': 30}\n\n        # Query with DSL filter\n        stream = await (\n            session.query(users)\n            .where(\"$.age &gt; 25\")\n            .execute()\n        )\n        async for result in stream:\n            print(result.record.bins)\n        stream.close()\n\nasyncio.run(main())\n</code></pre> <pre><code>from aerospike_fluent import SyncFluentClient, DataSet, Behavior\n\nwith SyncFluentClient(\"localhost:3000\") as client:\n    session = client.create_session(Behavior.DEFAULT)\n    users = DataSet.of(\"test\", \"users\")\n\n    # Write\n    session.upsert(users.id(1)).bin(\"name\").set_to(\"Alice\").bin(\"age\").set_to(30).execute()\n\n    # Read\n    stream = session.query(users.id(1)).execute()\n    result = stream.first_or_raise()\n    print(result.record.bins)\n\n    # Query with DSL filter\n    stream = session.query(users).where(\"$.age &gt; 25\").execute()\n    for result in stream:\n        print(result.record.bins)\n    stream.close()\n</code></pre>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install aerospike-fluent-client-python\n</code></pre> <p>Or install from source:</p> <pre><code>git clone https://github.com/aerospike/aerospike-client-python-fluent.git\ncd aerospike-client-python-fluent\npip install -e \".[dev]\"\n</code></pre>"},{"location":"#key-concepts","title":"Key Concepts","text":"<p>FluentClient / SyncFluentClient :   Entry point. Connects to an Aerospike cluster and manages the connection lifecycle.</p> <p>Session :   Scoped to a <code>Behavior</code> (policy defaults for timeouts, consistency, etc.). All reads and writes go through a session.</p> <p>DataSet :   A namespace + set pair. Use <code>DataSet.of(\"ns\", \"set\")</code> to create one, then <code>.id(key)</code> to produce keys.</p> <p>Builders :   Reads return a <code>QueryBuilder</code>, writes return a <code>WriteSegmentBuilder</code>. Chain methods, then call <code>.execute()</code>.</p> <p>Expression DSL :   Filter records with string expressions: <code>\"$.age &gt; 18 and $.status == 'active'\"</code>. See the DSL guide.</p>"},{"location":"#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+</li> <li>Aerospike Server 6.0+ (7.0+ for some features)</li> <li>aerospike-client-python-async</li> </ul>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Connecting to a Cluster</li> <li>Reading Data</li> <li>Writing Data</li> <li>API Reference</li> </ul>"},{"location":"api/","title":"API Reference","text":"<p>The Aerospike Python Fluent Client is organized into three layers:</p>"},{"location":"api/#async-api","title":"Async API","text":"<p>The primary API. All operations are <code>async</code>/<code>await</code>.</p> Class Description <code>FluentClient</code> Entry point \u2014 connect, create sessions, manage lifecycle <code>Session</code> Scoped reads and writes with a fixed <code>Behavior</code> <code>QueryBuilder</code> Build and execute read queries (point, set, batch) <code>WriteSegmentBuilder</code> Build and execute writes (upsert, insert, update, replace, delete) <code>BatchOperationBuilder</code> Low-level batch operation builder <code>IndexBuilder</code> Create and drop secondary indexes <code>BackgroundTaskSession</code> Server-side background jobs (update, delete, touch, UDF) <code>UdfFunctionBuilder</code> Foreground UDF execution <code>InfoCommands</code> Aerospike info protocol commands <code>TransactionalSession</code> Multi-record transactions"},{"location":"api/#sync-api","title":"Sync API","text":"<p>Synchronous wrappers for the async API. Same functionality, no <code>async</code>/<code>await</code>.</p> Class Description <code>SyncFluentClient</code> Sync entry point <code>SyncSession</code> Sync session <code>SyncQueryBuilder</code> Sync query builder"},{"location":"api/#core","title":"Core","text":"<p>Shared types used by both async and sync APIs.</p> Class Description <code>DataSet</code> Namespace + set pair, key factory <code>RecordResult</code> Single result from a query or batch <code>RecordStream</code> Async iterator over query results <code>Behavior</code> Policy presets (timeouts, consistency) <code>ClusterDefinition</code> Cluster connection configuration <code>ErrorStrategy</code> Error handling strategies <code>Exceptions</code> Exception hierarchy <code>QueryHint</code> Query optimization hints <code>IndexesMonitor</code> Background secondary index discovery"},{"location":"api/#dsl","title":"DSL","text":"<p>Expression parsing and filter generation.</p> Class / Function Description <code>parse_dsl</code> Parse DSL strings into filter expressions <code>FilterGenerator</code> Secondary index filter generation <code>Exp</code> Programmatic expression builder"},{"location":"api/background/","title":"Background Task Builders","text":"<p>Fluent builders for server-side background operations on datasets.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundTaskSession","title":"BackgroundTaskSession","text":"<pre><code>BackgroundTaskSession(session)\n</code></pre> <p>Choose a dataset-wide background job (update, delete, touch, or UDF).</p> <p>From :meth:<code>~aerospike_fluent.aio.session.Session.background_task</code>. Each method returns a builder to add filters, bin operations or UDF arguments, then <code>await ...execute()</code> for a server :class:<code>~aerospike_async.ExecuteTask</code>.</p> Example <p>Background update with a filter::</p> <pre><code>task = await (\n    session.background_task()\n    .update(users)\n    .where(\"$.active == true\")\n    .bin(\"score\").add(1)\n    .execute()\n)\n</code></pre> See Also <p>:meth:<code>~aerospike_fluent.aio.session.Session.execute_udf</code>: Foreground UDF on keys.</p> <p>Bind to session; prefer :meth:<code>Session.background_task</code>.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundTaskSession.update","title":"update","text":"<pre><code>update(dataset)\n</code></pre> <p>Start a <code>query_operate</code> update over records in dataset.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>DataSet</code> <p>Namespace/set scope for the scan.</p> required <p>Returns:</p> Type Description <code>BackgroundOperationBuilder</code> <p>class:<code>BackgroundOperationBuilder</code> \u2014 add <code>where</code>, <code>bin</code>, then</p> <code>BackgroundOperationBuilder</code> <p>meth:<code>BackgroundOperationBuilder.execute</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>On execute if no bin operations were added.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundTaskSession.delete","title":"delete","text":"<pre><code>delete(dataset)\n</code></pre> <p>Start a background delete of all records matching optional filters.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>DataSet</code> <p>Namespace/set to scan.</p> required <p>Returns:</p> Type Description <code>BackgroundOperationBuilder</code> <p>class:<code>BackgroundOperationBuilder</code> (no bin ops required for delete).</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundTaskSession.touch","title":"touch","text":"<pre><code>touch(dataset)\n</code></pre> <p>Start a background touch (TTL refresh) for matching records.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>DataSet</code> <p>Namespace/set to scan.</p> required <p>Returns:</p> Type Description <code>BackgroundOperationBuilder</code> <p>class:<code>BackgroundOperationBuilder</code> \u2014 optional <code>expire_record_after_seconds</code>.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundTaskSession.execute_udf","title":"execute_udf","text":"<pre><code>execute_udf(dataset)\n</code></pre> <p>Start a background UDF executed via <code>query_execute_udf</code>.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>DataSet</code> <p>Namespace/set scope.</p> required <p>Returns:</p> Name Type Description <code>BackgroundUdfFunctionBuilder</code> <p>class:<code>BackgroundUdfFunctionBuilder</code> \u2014 call :meth:<code>BackgroundUdfFunctionBuilder.function</code></p> <code>then</code> <code>BackgroundUdfFunctionBuilder</code> <p>meth:<code>BackgroundUdfBuilder.passing</code> and :meth:<code>BackgroundUdfBuilder.execute</code>.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundWriteBinBuilder","title":"BackgroundWriteBinBuilder","text":"<pre><code>BackgroundWriteBinBuilder(parent, bin_name)\n</code></pre> <p>Per-bin write helper for background updates (<code>put</code> / <code>add</code> only).</p> <p>Obtained from :meth:<code>BackgroundOperationBuilder.bin</code>. Call :meth:<code>set_to</code> or :meth:<code>add</code>, which return the parent builder for further chaining.</p> <p>Example::</p> <pre><code>builder.bin(\"score\").add(10)\n</code></pre> <p>Capture the bin name; prefer :meth:<code>BackgroundOperationBuilder.bin</code>.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundWriteBinBuilder.set_to","title":"set_to","text":"<pre><code>set_to(value)\n</code></pre> <p>Set the bin to value (<code>Operation.put</code>).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>The value to write.</p> required <p>Returns:</p> Type Description <code>BackgroundOperationBuilder</code> <p>The parent :class:<code>BackgroundOperationBuilder</code>.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundWriteBinBuilder.add","title":"add","text":"<pre><code>add(value)\n</code></pre> <p>Add a numeric value to the bin (<code>Operation.add</code>).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Numeric amount to add (may be negative).</p> required <p>Returns:</p> Type Description <code>BackgroundOperationBuilder</code> <p>The parent :class:<code>BackgroundOperationBuilder</code>.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundOperationBuilder","title":"BackgroundOperationBuilder","text":"<pre><code>BackgroundOperationBuilder(session, dataset, op_type)\n</code></pre> <p>Configure filters, TTL, and operations for <code>query_operate</code>.</p> <p>Not all query-policy knobs are wired through to PAC for background jobs; <code>records_per_second</code> is stored for API parity but may not affect the underlying call.</p> See Also <p>:meth:<code>BackgroundTaskSession.update</code>: Typical construction path.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundOperationBuilder.where","title":"where","text":"<pre><code>where(expression: str) -&gt; BackgroundOperationBuilder\n</code></pre><pre><code>where(\n    expression: FilterExpression,\n) -&gt; BackgroundOperationBuilder\n</code></pre> <pre><code>where(expression)\n</code></pre> <p>Restrict the scan with a DSL or <code>FilterExpression</code> predicate.</p> <p>Returns:</p> Type Description <code>BackgroundOperationBuilder</code> <p>This builder for chaining.</p> <p>Example::     builder.where(\"$.status == 'inactive'\")</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundOperationBuilder.bin","title":"bin","text":"<pre><code>bin(name)\n</code></pre> <p>Start a scalar write on name (update jobs only).</p> <p>Example::     builder.bin(\"score\").add(10)</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundOperationBuilder.expire_record_after_seconds","title":"expire_record_after_seconds","text":"<pre><code>expire_record_after_seconds(seconds)\n</code></pre> <p>Set record TTL in seconds for touches/updates when supported by policy.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundOperationBuilder.records_per_second","title":"records_per_second","text":"<pre><code>records_per_second(rps)\n</code></pre> <p>Store a throttle hint (may be unused depending on PAC background API).</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundOperationBuilder.fail_on_filtered_out","title":"fail_on_filtered_out","text":"<pre><code>fail_on_filtered_out()\n</code></pre> <p>Unsupported for background tasks (raises <code>TypeError</code>).</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundOperationBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>Unsupported for background tasks (raises <code>TypeError</code>).</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundOperationBuilder.execute","title":"execute  <code>async</code>","text":"<pre><code>execute()\n</code></pre> <p>Start the server job and return an :class:<code>~aerospike_async.ExecuteTask</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>For update without bin operations.</p> <code>RuntimeError</code> <p>If the fluent client is not connected.</p> <code>AerospikeError</code> <p>On PAC errors (converted).</p> <p>Example::     task = await (         session.background_task()             .update(users)             .bin(\"visits\").add(1)             .execute()     )     await task.wait_till_complete()</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundUdfFunctionBuilder","title":"BackgroundUdfFunctionBuilder","text":"<pre><code>BackgroundUdfFunctionBuilder(session, dataset)\n</code></pre> <p>Pick module and function for a dataset background UDF.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundUdfFunctionBuilder.function","title":"function","text":"<pre><code>function(package_name, function_name)\n</code></pre> <p>Select the registered package and Lua entrypoint.</p> <p>Parameters:</p> Name Type Description Default <code>package_name</code> <code>str</code> <p>Server module name (no <code>.lua</code> suffix).</p> required <code>function_name</code> <code>str</code> <p>Lua function to invoke.</p> required <p>Returns:</p> Type Description <code>BackgroundUdfBuilder</code> <p>class:<code>BackgroundUdfBuilder</code> for arguments and execution.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If either string is empty.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundUdfBuilder","title":"BackgroundUdfBuilder","text":"<pre><code>BackgroundUdfBuilder(\n    session, dataset, package_name, function_name\n)\n</code></pre> <p>Arguments, optional filter, and execution for <code>query_execute_udf</code>.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundUdfBuilder.passing","title":"passing","text":"<pre><code>passing(*args)\n</code></pre> <p>Set Lua arguments after the implicit record parameter.</p> <p>Returns:</p> Type Description <code>BackgroundUdfBuilder</code> <p>This builder for chaining.</p> <p>Example::     builder.passing(\"arg1\", 42)</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundUdfBuilder.where","title":"where","text":"<pre><code>where(expression: str) -&gt; BackgroundUdfBuilder\n</code></pre><pre><code>where(expression: FilterExpression) -&gt; BackgroundUdfBuilder\n</code></pre> <pre><code>where(expression)\n</code></pre> <p>Optional predicate limiting which records invoke the UDF.</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundUdfBuilder.records_per_second","title":"records_per_second","text":"<pre><code>records_per_second(rps)\n</code></pre> <p>Throttle hint stored for API parity (may not affect PAC).</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundUdfBuilder.fail_on_filtered_out","title":"fail_on_filtered_out","text":"<pre><code>fail_on_filtered_out()\n</code></pre> <p>Unsupported (raises <code>TypeError</code>).</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundUdfBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>Unsupported (raises <code>TypeError</code>).</p>"},{"location":"api/background/#aerospike_fluent.aio.background.BackgroundUdfBuilder.execute","title":"execute  <code>async</code>","text":"<pre><code>execute()\n</code></pre> <p>Start the background UDF job.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the client is not connected.</p> <code>AerospikeError</code> <p>On PAC errors (converted).</p> <p>Example::     task = await (         session.background_task()             .execute_udf(users)             .function(\"mypkg\", \"expire_old\")             .passing(30)             .execute()     )     await task.wait_till_complete()</p>"},{"location":"api/batch/","title":"Batch Operation Builders","text":"<p>BatchOperationBuilder - Builder for chaining operations across multiple keys.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchOpType","title":"BatchOpType","text":"<p>               Bases: <code>Enum</code></p> <p>Type of batch operation.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder","title":"BatchBinBuilder","text":"<pre><code>BatchBinBuilder(key_op, bin_name)\n</code></pre> <p>Builder for chaining bin operations within a batch key operation.</p> Example <p>batch.insert(key).bin(\"name\").set_to(\"Alice\").bin(\"age\").set_to(25)</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder.set_to","title":"set_to","text":"<pre><code>set_to(value)\n</code></pre> <p>Set a bin value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>The value to set.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>The parent BatchKeyOperationBuilder for chaining.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder.add","title":"add","text":"<pre><code>add(value)\n</code></pre> <p>Add value to the bin (numeric increment).</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder.increment_by","title":"increment_by","text":"<pre><code>increment_by(value)\n</code></pre> <p>Alias for :meth:<code>add</code>.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder.append","title":"append","text":"<pre><code>append(value)\n</code></pre> <p>Append a string to a bin value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>The string to append.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>The parent BatchKeyOperationBuilder for chaining.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder.prepend","title":"prepend","text":"<pre><code>prepend(value)\n</code></pre> <p>Prepend a string to a bin value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>The string to prepend.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>The parent BatchKeyOperationBuilder for chaining.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder.select_from","title":"select_from","text":"<pre><code>select_from(expression, *, ignore_eval_failure=False)\n</code></pre> <p>Read the result of an expression into this bin.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or pre-built FilterExpression.</p> required <code>ignore_eval_failure</code> <code>bool</code> <p>If True, suppress evaluation errors.</p> <code>False</code> <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>The parent BatchKeyOperationBuilder for chaining.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder.insert_from","title":"insert_from","text":"<pre><code>insert_from(\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result to this bin only if it does not already exist.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or pre-built FilterExpression.</p> required <code>ignore_op_failure</code> <code>bool</code> <p>If True, suppress BIN_EXISTS_ERROR.</p> <code>False</code> <code>ignore_eval_failure</code> <code>bool</code> <p>If True, suppress evaluation errors.</p> <code>False</code> <code>delete_if_null</code> <code>bool</code> <p>If True, delete bin when expression evaluates to nil.</p> <code>False</code> <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>The parent BatchKeyOperationBuilder for chaining.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder.update_from","title":"update_from","text":"<pre><code>update_from(\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result to this bin only if it already exists.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or pre-built FilterExpression.</p> required <code>ignore_op_failure</code> <code>bool</code> <p>If True, suppress BIN_NOT_FOUND.</p> <code>False</code> <code>ignore_eval_failure</code> <code>bool</code> <p>If True, suppress evaluation errors.</p> <code>False</code> <code>delete_if_null</code> <code>bool</code> <p>If True, delete bin when expression evaluates to nil.</p> <code>False</code> <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>The parent BatchKeyOperationBuilder for chaining.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchBinBuilder.upsert_from","title":"upsert_from","text":"<pre><code>upsert_from(\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result to this bin (create or update).</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or pre-built FilterExpression.</p> required <code>ignore_op_failure</code> <code>bool</code> <p>If True, suppress policy errors.</p> <code>False</code> <code>ignore_eval_failure</code> <code>bool</code> <p>If True, suppress evaluation errors.</p> <code>False</code> <code>delete_if_null</code> <code>bool</code> <p>If True, delete bin when expression evaluates to nil.</p> <code>False</code> <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>The parent BatchKeyOperationBuilder for chaining.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder","title":"BatchKeyOperationBuilder","text":"<pre><code>BatchKeyOperationBuilder(batch, key, op_type)\n</code></pre> <p>Builder for a single key's operation within a batch.</p> <p>This class allows chaining bin operations and then continuing to add more keys to the batch.</p> Example <p>batch.insert(key1).bin(\"name\").set_to(\"Alice\") \\      .update(key2).bin(\"counter\").add(1)</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder.bin","title":"bin","text":"<pre><code>bin(bin_name)\n</code></pre> <p>Start a bin operation chain.</p> <p>Parameters:</p> Name Type Description Default <code>bin_name</code> <code>str</code> <p>The name of the bin.</p> required <p>Returns:</p> Type Description <code>BatchBinBuilder</code> <p>A BatchBinBuilder for chaining bin operations.</p> Example <p>batch.insert(key).bin(\"name\").set_to(\"Alice\").bin(\"age\").set_to(25)</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder.put","title":"put","text":"<pre><code>put(bins)\n</code></pre> <p>Set multiple bins at once.</p> <p>Parameters:</p> Name Type Description Default <code>bins</code> <code>Dict[str, Any]</code> <p>Dictionary of bin name to value mappings.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>self for method chaining.</p> Example <p>batch.insert(key).put({\"name\": \"Alice\", \"age\": 25})</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder.insert","title":"insert","text":"<pre><code>insert(key)\n</code></pre> <p>Add an insert operation for another key.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder.update","title":"update","text":"<pre><code>update(key)\n</code></pre> <p>Add an update operation for another key.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder.upsert","title":"upsert","text":"<pre><code>upsert(key)\n</code></pre> <p>Add an upsert operation for another key.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder.replace","title":"replace","text":"<pre><code>replace(key)\n</code></pre> <p>Add a replace operation for another key.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder.replace_if_exists","title":"replace_if_exists","text":"<pre><code>replace_if_exists(key)\n</code></pre> <p>Add a replace-if-exists operation for another key.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder.delete","title":"delete","text":"<pre><code>delete(key)\n</code></pre> <p>Add a delete operation for another key.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchKeyOperationBuilder.execute","title":"execute  <code>async</code>","text":"<pre><code>execute()\n</code></pre> <p>Execute all batch operations.</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchOperationBuilder","title":"BatchOperationBuilder","text":"<pre><code>BatchOperationBuilder(client, behavior=None)\n</code></pre> <p>Builder for chaining operations across multiple keys.</p> <p>This class enables fluent chaining of operations on different keys, which are then executed as a single batch operation.</p> Example <pre><code>results = await session.batch() \\\n    .insert(key1).bin(\"name\").set_to(\"Alice\").bin(\"age\").set_to(25) \\\n    .update(key2).bin(\"counter\").add(1) \\\n    .delete(key3) \\\n    .execute()\n</code></pre> <p>The operations are collected and executed together using the async client's batch_operate method for optimal performance.</p> <p>Initialize a BatchOperationBuilder.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>Client</code> <p>The underlying async client.</p> required <code>behavior</code> <code>Optional[Behavior]</code> <p>Optional Behavior for deriving policies.</p> <code>None</code>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchOperationBuilder.insert","title":"insert","text":"<pre><code>insert(key)\n</code></pre> <p>Add an insert (create only) operation for a key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Key</code> <p>The key for the record.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>A BatchKeyOperationBuilder for chaining bin operations.</p> Example <p>batch.insert(key).bin(\"name\").set_to(\"Alice\")</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchOperationBuilder.update","title":"update","text":"<pre><code>update(key)\n</code></pre> <p>Add an update (update only) operation for a key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Key</code> <p>The key for the record.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>A BatchKeyOperationBuilder for chaining bin operations.</p> Example <p>batch.update(key).bin(\"counter\").add(1)</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchOperationBuilder.upsert","title":"upsert","text":"<pre><code>upsert(key)\n</code></pre> <p>Add an upsert (create or update) operation for a key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Key</code> <p>The key for the record.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>A BatchKeyOperationBuilder for chaining bin operations.</p> Example <p>batch.upsert(key).bin(\"name\").set_to(\"Bob\")</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchOperationBuilder.replace","title":"replace","text":"<pre><code>replace(key)\n</code></pre> <p>Add a replace (create or replace) operation for a key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Key</code> <p>The key for the record.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>A BatchKeyOperationBuilder for chaining bin operations.</p> Example <p>batch.replace(key).put({\"name\": \"Charlie\", \"age\": 35})</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchOperationBuilder.replace_if_exists","title":"replace_if_exists","text":"<pre><code>replace_if_exists(key)\n</code></pre> <p>Add a replace-if-exists operation for a key.</p> <p>This operation will fail if the record does not exist.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Key</code> <p>The key for the record.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>A BatchKeyOperationBuilder for chaining bin operations.</p> Example <p>batch.replace_if_exists(key).put({\"name\": \"Updated\", \"status\": \"active\"})</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchOperationBuilder.delete","title":"delete","text":"<pre><code>delete(key)\n</code></pre> <p>Add a delete operation for a key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Key</code> <p>The key for the record.</p> required <p>Returns:</p> Type Description <code>BatchKeyOperationBuilder</code> <p>A BatchKeyOperationBuilder for continuing the chain.</p> Example <p>batch.delete(key1).delete(key2).execute()</p>"},{"location":"api/batch/#aerospike_fluent.aio.operations.batch.BatchOperationBuilder.execute","title":"execute  <code>async</code>","text":"<pre><code>execute()\n</code></pre> <p>Execute all batch operations.</p> <p>Example::     stream = await (         session.batch()         .insert(key1)         .bin(\"name\").set_to(\"Ada\")         .upsert(key2)         .bin(\"n\").set_to(1)         .execute()     )     rows = await stream.collect()</p> <p>Returns:</p> Name Type Description <code>A</code> <code>RecordStream</code> <p>class:<code>RecordStream</code> of per-key :class:<code>RecordResult</code> items.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no operations have been added.</p>"},{"location":"api/behavior/","title":"Behavior","text":""},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior","title":"Behavior","text":"<pre><code>Behavior(name, patches, parent=None)\n</code></pre> <p>Immutable, scope-aware configuration for Aerospike operation policies.</p> <p>A Behavior holds a set of scope-keyed <code>Settings</code> patches that are resolved at operation time based on the operation's kind, shape, and namespace mode.  More-specific scopes override less-specific ones, and a child Behavior inherits from its parent.</p> <p>Resolved settings are eagerly cached at construction time so that <code>get_settings()</code> is a simple dict lookup.</p> <p>Example::</p> <pre><code># Use a pre-defined behavior\nsession = cluster.create_session(Behavior.DEFAULT)\n\n# Derive a custom behavior with scope-specific overrides\nfast = Behavior.DEFAULT.derive_with_changes(\n    \"fast_reads\",\n    reads=Settings(total_timeout=timedelta(milliseconds=200)),\n    reads_batch=Settings(max_concurrent_nodes=8),\n)\nsession = cluster.create_session(fast)\n</code></pre> <p>Attributes:</p> Name Type Description <code>DEFAULT</code> <p>Balanced defaults (30 s total timeout, 2 retries, send key).</p> <code>READ_FAST</code> <p>Low-latency reads (200 ms total, 50 ms socket, 3 retries).</p> <code>STRICTLY_CONSISTENT</code> <p>Placeholder for SC mode when PAC exposes it.</p> <code>FAST_RACK_AWARE</code> <p>Like READ_FAST with rack-preferred replica.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.name","title":"name  <code>property</code>","text":"<pre><code>name\n</code></pre> <p>Identifier for this behavior (for example <code>'DEFAULT'</code>).</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.parent","title":"parent  <code>property</code>","text":"<pre><code>parent\n</code></pre> <p>Behavior from which this one inherits, or <code>None</code> for root.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.children","title":"children  <code>property</code>","text":"<pre><code>children\n</code></pre> <p>Behaviors derived from this one via :meth:<code>derive_with_changes</code>.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.total_timeout","title":"total_timeout  <code>property</code>","text":"<pre><code>total_timeout\n</code></pre> <p>Total timeout for point reads (from <code>READ:POINT:AP</code> scope).</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.socket_timeout","title":"socket_timeout  <code>property</code>","text":"<pre><code>socket_timeout\n</code></pre> <p>Socket timeout for point reads (from <code>READ:POINT:AP</code> scope).</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.max_retries","title":"max_retries  <code>property</code>","text":"<pre><code>max_retries\n</code></pre> <p>Max retries for point reads (from <code>READ:POINT:AP</code> scope).</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.retry_delay","title":"retry_delay  <code>property</code>","text":"<pre><code>retry_delay\n</code></pre> <p>Retry delay for point reads (from <code>READ:POINT:AP</code> scope).</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.send_key","title":"send_key  <code>property</code>","text":"<pre><code>send_key\n</code></pre> <p>Whether to send the user key with point reads (from <code>READ:POINT:AP</code> scope).</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.get_settings","title":"get_settings","text":"<pre><code>get_settings(kind, shape, mode=AP)\n</code></pre> <p>Return the fully-resolved Settings for the given operation context.</p> <p>This is a cached O(1) dict lookup; the matrix is pre-computed at construction time.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.clear_cache","title":"clear_cache","text":"<pre><code>clear_cache()\n</code></pre> <p>Recompute the resolved settings matrix and cascade to children.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.derive_with_changes","title":"derive_with_changes","text":"<pre><code>derive_with_changes(\n    name,\n    *,\n    all=None,\n    reads=None,\n    reads_point=None,\n    reads_batch=None,\n    reads_query=None,\n    reads_ap=None,\n    reads_sc=None,\n    writes=None,\n    writes_retryable=None,\n    writes_non_retryable=None,\n    writes_point=None,\n    writes_batch=None,\n    writes_query=None,\n    writes_ap=None,\n    writes_sc=None,\n    total_timeout=None,\n    socket_timeout=None,\n    max_retries=None,\n    retry_delay=None,\n    send_key=None,\n)\n</code></pre> <p>Create a child Behavior with the specified overrides.</p> <p>Accepts either scope-keyed <code>Settings</code> objects or flat keyword arguments (which are applied to the <code>ALL</code> scope).  Both styles can be combined; flat kwargs are merged into the <code>all</code> Settings.</p> <p>Example::</p> <pre><code>fast_reads = default_behavior.derive_with_changes(\n    \"fast_reads\",\n    reads=Settings(total_timeout=timedelta(milliseconds=200)),\n)\n</code></pre>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.find_behavior","title":"find_behavior","text":"<pre><code>find_behavior(name)\n</code></pre> <p>Search this behavior and its descendants for one with name.</p> <p>Returns:</p> Type Description <code>Optional[Behavior]</code> <p>The matching :class:<code>Behavior</code>, or <code>None</code> if not found.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior.Behavior.explain","title":"explain","text":"<pre><code>explain()\n</code></pre> <p>Return a human-readable summary of overrides and resolved settings.</p> <p>Example::</p> <pre><code>print(behavior.explain())\n</code></pre> <p>Returns:</p> Type Description <code>str</code> <p>Multi-line string with overrides and the full resolved settings matrix.</p>"},{"location":"api/behavior/#behavior-settings","title":"Behavior Settings","text":"<p>Settings and scope types for the Behavior model.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior_settings.OpKind","title":"OpKind","text":"<p>               Bases: <code>Enum</code></p> <p>The kind of database operation.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior_settings.OpShape","title":"OpShape","text":"<p>               Bases: <code>Enum</code></p> <p>The shape (cardinality) of the operation.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior_settings.Mode","title":"Mode","text":"<p>               Bases: <code>Enum</code></p> <p>Namespace consistency mode.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior_settings.Scope","title":"Scope","text":"<p>               Bases: <code>Enum</code></p> <p>Named scopes for configuring operation settings within a Behavior.</p> <p>Scope values correspond to keyword arguments accepted by <code>Behavior.derive_with_changes()</code>.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior_settings.Settings","title":"Settings  <code>dataclass</code>","text":"<pre><code>Settings(\n    total_timeout=None,\n    socket_timeout=None,\n    max_retries=None,\n    retry_delay=None,\n    send_key=None,\n    durable_delete=None,\n    commit_level=None,\n    replica=None,\n    consistency_level=None,\n    max_concurrent_nodes=None,\n    record_queue_size=None,\n    allow_inline=None,\n    allow_inline_ssd=None,\n    read_touch_ttl_percent=None,\n)\n</code></pre> <p>Immutable set of operation settings.</p> <p>All fields are Optional; <code>None</code> means \"not configured / inherit from a less-specific scope or the parent Behavior\".</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior_settings.Settings.merge","title":"merge  <code>classmethod</code>","text":"<pre><code>merge(base, override)\n</code></pre> <p>Merge two Settings; override's non-None fields win.</p>"},{"location":"api/behavior/#aerospike_fluent.policy.behavior_settings.resolution_order","title":"resolution_order","text":"<pre><code>resolution_order(kind, shape, mode=AP)\n</code></pre> <p>Return applicable scopes from least-specific to most-specific.</p> <p>Used by <code>Behavior.get_settings()</code> to layer patches so that more-specific scopes override less-specific ones.</p>"},{"location":"api/cdt-read/","title":"CDT Read Builders","text":"<p>Read-only CDT action builders for query bin operations.</p> <p>Two builder classes provide terminal read methods on a CDT path:</p> <ul> <li><code>CdtReadBuilder</code>  -- non-invertable terminals (get_values, count, \u2026)   plus singular CDT navigation for deeper nesting.</li> <li><code>CdtReadInvertableBuilder</code> -- adds inverted \"all others\" terminals   (no further navigation).</li> </ul> <p>Both are generic on the parent builder type <code>T</code> so terminal methods return the parent for continued chaining.  The actual CDT operation is produced by an <code>op_factory</code> callable injected by the navigation method that created the builder.</p> <p>Nested navigation accumulates <code>CTX</code> entries.  Each navigation step pushes the current selector into the context and creates new factories for the next selector with <code>.set_context()</code> applied.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder","title":"CdtReadBuilder","text":"<pre><code>CdtReadBuilder(\n    parent,\n    op_factory,\n    return_type_cls,\n    *,\n    is_map,\n    bin_name=\"\",\n    ctx=(),\n    to_ctx=None,\n)\n</code></pre> <p>               Bases: <code>Generic[T]</code></p> <p>Terminal read actions and singular navigation for a CDT path.</p> <p>The parent (type <code>T</code>) must expose <code>add_operation(op)</code> so the builder can register the produced operation before returning it.</p> <p>Navigation fields (<code>bin_name</code>, <code>ctx</code>, <code>to_ctx</code>) are optional; when not provided, further navigation is not available (terminal-only).</p> Example <p>Read values from a map key within a query::</p> <pre><code>stream = await (\n    session.query(key)\n        .bin(\"settings\").on_map_key(\"theme\").get_values()\n        .execute()\n)\n</code></pre>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.on_map_key","title":"on_map_key","text":"<pre><code>on_map_key(key)\n</code></pre> <p>Navigate into a map element by key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Any</code> <p>Map key to target.</p> required <p>Returns:</p> Type Description <code>CdtReadBuilder[T]</code> <p>class:<code>CdtReadBuilder</code> for reading the targeted element.</p> <p>Example::     .bin(\"m\").on_map_key(\"x\").get_values()</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.on_map_index","title":"on_map_index","text":"<pre><code>on_map_index(index)\n</code></pre> <p>Navigate into a map element by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Map index to target.</p> required <p>Returns:</p> Type Description <code>CdtReadBuilder[T]</code> <p>class:<code>CdtReadBuilder</code> for reading the targeted element.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.on_map_rank","title":"on_map_rank","text":"<pre><code>on_map_rank(rank)\n</code></pre> <p>Navigate into a map element by rank (0 = lowest value).</p> <p>Parameters:</p> Name Type Description Default <code>rank</code> <code>int</code> <p>Rank position (0 = lowest value).</p> required <p>Returns:</p> Type Description <code>CdtReadBuilder[T]</code> <p>class:<code>CdtReadBuilder</code> for reading the targeted element.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.on_list_index","title":"on_list_index","text":"<pre><code>on_list_index(index)\n</code></pre> <p>Navigate into a list element by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>List index (0-based, negative counts from end).</p> required <p>Returns:</p> Type Description <code>CdtReadBuilder[T]</code> <p>class:<code>CdtReadBuilder</code> for reading the targeted element.</p> <p>Example::     .bin(\"items\").on_list_index(0).get_values()</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.on_list_rank","title":"on_list_rank","text":"<pre><code>on_list_rank(rank)\n</code></pre> <p>Navigate into a list element by rank (0 = lowest value).</p> <p>Parameters:</p> Name Type Description Default <code>rank</code> <code>int</code> <p>Rank position (0 = lowest value).</p> required <p>Returns:</p> Type Description <code>CdtReadBuilder[T]</code> <p>class:<code>CdtReadBuilder</code> for reading the targeted element.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.get_values","title":"get_values","text":"<pre><code>get_values()\n</code></pre> <p>Return value(s) at the current CDT selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p> <p>Example::     .bin(\"m\").on_map_key(\"x\").get_values()</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.get_keys","title":"get_keys","text":"<pre><code>get_keys()\n</code></pre> <p>Return map keys at the current CDT selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.get_keys_and_values","title":"get_keys_and_values","text":"<pre><code>get_keys_and_values()\n</code></pre> <p>Return map key-value pairs at the current CDT selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.count","title":"count","text":"<pre><code>count()\n</code></pre> <p>Return the count of elements at the current CDT selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.get_indexes","title":"get_indexes","text":"<pre><code>get_indexes()\n</code></pre> <p>Return indexes of the selected CDT elements.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.get_reverse_indexes","title":"get_reverse_indexes","text":"<pre><code>get_reverse_indexes()\n</code></pre> <p>Return reverse indexes of the selected CDT elements.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.get_ranks","title":"get_ranks","text":"<pre><code>get_ranks()\n</code></pre> <p>Return ranks of the selected CDT elements.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.get_reverse_ranks","title":"get_reverse_ranks","text":"<pre><code>get_reverse_ranks()\n</code></pre> <p>Return reverse ranks of the selected CDT elements.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.exists","title":"exists","text":"<pre><code>exists()\n</code></pre> <p>Check whether the selected CDT element(s) exist.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.map_size","title":"map_size","text":"<pre><code>map_size()\n</code></pre> <p>Return the element count of the map at the current CDT path.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder.list_size","title":"list_size","text":"<pre><code>list_size()\n</code></pre> <p>Return the element count of the list at the current CDT path.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadInvertableBuilder","title":"CdtReadInvertableBuilder","text":"<pre><code>CdtReadInvertableBuilder(\n    parent,\n    op_factory,\n    return_type_cls,\n    *,\n    is_map,\n    bin_name=\"\",\n    ctx=(),\n    to_ctx=None,\n)\n</code></pre> <p>               Bases: <code>CdtReadBuilder[T]</code></p> <p>Terminal read actions with inverted (all-others) variants.</p> <p>Used for range and list selectors where INVERTED makes semantic sense (e.g. \"all keys except those in this range\").  Does not support further navigation.</p> Example <p>Get all map values except those in a key range::</p> <pre><code>.bin(\"m\").on_map_key_range(\"a\", \"d\").get_all_other_values()\n</code></pre>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadInvertableBuilder.get_all_other_values","title":"get_all_other_values","text":"<pre><code>get_all_other_values()\n</code></pre> <p>Return all values except those matching the selection (INVERTED).</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadInvertableBuilder.get_all_other_keys","title":"get_all_other_keys","text":"<pre><code>get_all_other_keys()\n</code></pre> <p>Return all map keys except those matching the selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadInvertableBuilder.get_all_other_keys_and_values","title":"get_all_other_keys_and_values","text":"<pre><code>get_all_other_keys_and_values()\n</code></pre> <p>Return all map key-value pairs except those matching the selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadInvertableBuilder.count_all_others","title":"count_all_others","text":"<pre><code>count_all_others()\n</code></pre> <p>Return the count of elements except those matching the selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadInvertableBuilder.get_all_other_indexes","title":"get_all_other_indexes","text":"<pre><code>get_all_other_indexes()\n</code></pre> <p>Return indexes of all elements except those matching the selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadInvertableBuilder.get_all_other_reverse_indexes","title":"get_all_other_reverse_indexes","text":"<pre><code>get_all_other_reverse_indexes()\n</code></pre> <p>Return reverse indexes of all elements except those matching the selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadInvertableBuilder.get_all_other_ranks","title":"get_all_other_ranks","text":"<pre><code>get_all_other_ranks()\n</code></pre> <p>Return ranks of all elements except those matching the selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-read/#aerospike_fluent.aio.operations.cdt_read.CdtReadInvertableBuilder.get_all_other_reverse_ranks","title":"get_all_other_reverse_ranks","text":"<pre><code>get_all_other_reverse_ranks()\n</code></pre> <p>Return reverse ranks of all elements except those matching the selection.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/","title":"CDT Write Builders","text":"<p>CDT action builders for write contexts.</p> <p>Extends the read-only builders from <code>cdt_read</code> with:</p> <ul> <li><code>remove()</code> / <code>remove_all_others()</code> terminals</li> <li><code>set_to(value)</code> / <code>add(value)</code> write terminals (map key navigation)</li> <li>Collection-level map/list terminals (<code>map_clear</code>, <code>map_upsert_items</code>, \u2026)</li> <li>Nested navigation that preserves write capability through the chain</li> </ul>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder","title":"CdtWriteBuilder","text":"<pre><code>CdtWriteBuilder(\n    parent,\n    op_factory,\n    remove_factory,\n    return_type_cls,\n    *,\n    is_map,\n    bin_name=\"\",\n    ctx=(),\n    to_ctx=None,\n    set_to_factory=None,\n    add_factory=None,\n)\n</code></pre> <p>               Bases: <code>_RemoveMixin</code>, <code>CdtReadBuilder[T]</code></p> <p>CDT action builder with read, remove, and write terminals.</p> <p>Inherits all read terminals and navigation from <code>CdtReadBuilder</code>. Adds <code>remove()</code>, and on map-key paths <code>set_to()</code> / <code>add()</code>. Navigation returns <code>CdtWriteBuilder</code> to preserve write capability.</p> Example <p>Set a nested map value and remove another key::</p> <pre><code>await (\n    session.upsert(key)\n        .bin(\"settings\").on_map_key(\"theme\").set_to(\"dark\")\n        .bin(\"settings\").on_map_key(\"old_key\").remove()\n        .execute()\n)\n</code></pre>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.on_map_key","title":"on_map_key","text":"<pre><code>on_map_key(key)\n</code></pre> <p>Navigate into a map element by key (supports set_to / add).</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Any</code> <p>Map key to target.</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[T]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.set_to","title":"set_to","text":"<pre><code>set_to(value)\n</code></pre> <p>Set the value at the current CDT path.</p> <p>Requires prior <code>on_map_key()</code> navigation.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>New value to store.</p> required <p>Example::     .bin(\"m\").on_map_key(\"color\").set_to(\"blue\")</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If not preceded by <code>on_map_key()</code> navigation.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.add","title":"add","text":"<pre><code>add(value)\n</code></pre> <p>Increment the value at the current CDT path.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Numeric value to add.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If not preceded by <code>on_map_key()</code> navigation.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.map_clear","title":"map_clear","text":"<pre><code>map_clear()\n</code></pre> <p>Remove all entries from the map at the current CDT path.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.map_upsert_items","title":"map_upsert_items","text":"<pre><code>map_upsert_items(items)\n</code></pre> <p>Put multiple map entries (create or update each key).</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Mapping or sequence of <code>(key, value)</code> pairs.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.map_insert_items","title":"map_insert_items","text":"<pre><code>map_insert_items(items)\n</code></pre> <p>Put map entries only for keys that do not yet exist.</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Mapping or sequence of <code>(key, value)</code> pairs.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.map_update_items","title":"map_update_items","text":"<pre><code>map_update_items(items)\n</code></pre> <p>Update existing map entries only (no new keys).</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Mapping or sequence of <code>(key, value)</code> pairs.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.map_create","title":"map_create","text":"<pre><code>map_create(order)\n</code></pre> <p>Create an empty map with the given key order.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>MapOrder</code> <p>Key sort order for the map.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.map_set_policy","title":"map_set_policy","text":"<pre><code>map_set_policy(order)\n</code></pre> <p>Set map sort order policy without changing entries.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>MapOrder</code> <p>Key sort order to apply.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.list_clear","title":"list_clear","text":"<pre><code>list_clear()\n</code></pre> <p>Remove all elements from the list at the current CDT path.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.list_sort","title":"list_sort","text":"<pre><code>list_sort(flags=DEFAULT)\n</code></pre> <p>Sort the list at the current path.</p> <p>Parameters:</p> Name Type Description Default <code>flags</code> <code>ListSortFlags</code> <p>Sort behaviour flags (default <code>DEFAULT</code>).</p> <code>DEFAULT</code> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.list_append_items","title":"list_append_items","text":"<pre><code>list_append_items(items)\n</code></pre> <p>Append values to an unordered list.</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Sequence[Any]</code> <p>Values to append.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p> <p>Example::     .bin(\"tags\").on_map_key(\"items\").list_append_items([10, 20])</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.list_add_items","title":"list_add_items","text":"<pre><code>list_add_items(items)\n</code></pre> <p>Insert values into an ordered list (sorted positions).</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Sequence[Any]</code> <p>Values to insert.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.list_create","title":"list_create","text":"<pre><code>list_create(order, *, pad=False, persist_index=False)\n</code></pre> <p>Create an empty list with the given order.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>ListOrderType</code> <p>Element ordering for the list.</p> required <code>pad</code> <code>bool</code> <p>If <code>True</code>, allow sparse indexes.</p> <code>False</code> <code>persist_index</code> <code>bool</code> <p>If <code>True</code>, maintain a persistent index.</p> <code>False</code> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteBuilder.list_set_order","title":"list_set_order","text":"<pre><code>list_set_order(order)\n</code></pre> <p>Set list sort order without changing elements.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>ListOrderType</code> <p>Sort order to apply.</p> required <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteInvertableBuilder","title":"CdtWriteInvertableBuilder","text":"<pre><code>CdtWriteInvertableBuilder(\n    parent,\n    op_factory,\n    remove_factory,\n    return_type_cls,\n    *,\n    is_map,\n)\n</code></pre> <p>               Bases: <code>_RemoveMixin</code>, <code>CdtReadInvertableBuilder[T]</code></p> <p>CDT action builder with read, inverted-read, and remove terminals.</p> <p>Inherits all read and inverted-read terminals from <code>CdtReadInvertableBuilder</code> and adds <code>remove()</code> / <code>remove_all_others()</code>.  Does not support further navigation.</p> Example <p>Remove everything except the selected range::</p> <pre><code>.bin(\"m\").on_map_value_range(0, 100).remove_all_others()\n</code></pre>"},{"location":"api/cdt-write/#aerospike_fluent.aio.operations.cdt_write.CdtWriteInvertableBuilder.remove_all_others","title":"remove_all_others","text":"<pre><code>remove_all_others()\n</code></pre> <p>Remove all CDT elements except the selected ones.</p> <p>Returns:</p> Type Description <code>T</code> <p>The parent builder for chaining.</p>"},{"location":"api/client/","title":"FluentClient","text":""},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient","title":"FluentClient","text":"<pre><code>FluentClient(\n    seeds, policy=None, index_refresh_interval=5.0\n)\n</code></pre> <p>Async entry point for the Fluent API over the Aerospike Python Async Client.</p> <p>Use <code>async with FluentClient(...) as client</code> (or <code>await connect()</code>) to open a connection, then :meth:<code>create_session</code> for reads and writes with a chosen :class:<code>~aerospike_fluent.policy.behavior.Behavior</code>.</p> Example <p>async with FluentClient(\"127.0.0.1:3000\") as client:     session = client.create_session()     stream = await client.query(         namespace=\"test\",         set_name=\"users\",     ).execute()     async for row in stream:         if row.record is not None:             print(row.record.bins)</p> See Also <p>:meth:<code>create_session</code>: Primary API for application code.</p> <p>Store cluster seeds and policy; connection starts in :meth:<code>connect</code> or <code>async with</code>.</p> <p>Parameters:</p> Name Type Description Default <code>seeds</code> <code>str</code> <p>Seed address string understood by the async client (for example <code>\"127.0.0.1:3000\"</code> or a comma-separated host list if supported).</p> required <code>policy</code> <code>Optional[ClientPolicy]</code> <p>Optional :class:<code>~aerospike_async.ClientPolicy</code>; defaults to a new client policy when omitted.</p> <code>None</code> <code>index_refresh_interval</code> <code>float</code> <p>Seconds between secondary index cache refreshes (default 5.0). The monitor periodically fetches index metadata from the cluster so that DSL-based <code>where()</code> calls can transparently generate secondary index filters.</p> <code>5.0</code> Note <p>No network I/O occurs here. The client connects when you <code>await connect()</code> or enter <code>async with</code>.</p>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.is_connected","title":"is_connected  <code>property</code>","text":"<pre><code>is_connected\n</code></pre> <p>Check if the client is connected.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when :meth:<code>connect</code> has succeeded and :meth:<code>close</code> has not been called.</p>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.underlying_client","title":"underlying_client  <code>property</code>","text":"<pre><code>underlying_client\n</code></pre> <p>The underlying aerospike_async (PAC) Client for direct API access.</p> <p>Use this when you need PAC calls that are not wrapped by the fluent API, e.g. info(), nodes(), get_node(). The returned client is the same instance used internally by the fluent API.</p> Example <pre><code>async with FluentClient(\"localhost:3000\") as client:\n    pac = client.underlying_client\n    response = await pac.info(\"sindex-list\")\n    nodes = await pac.nodes()\n    node = await pac.get_node(nodes[0].name)\n    response = await node.info(\"build\")\n</code></pre> <p>Returns:</p> Type Description <code>Client</code> <p>The aerospike_async Client instance.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the client is not connected.</p>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.connect","title":"connect  <code>async</code>","text":"<pre><code>connect()\n</code></pre> <p>Open a connection to the cluster using the configured seeds and policy.</p> <p>Idempotent: if already connected, returns immediately.</p> <p>Raises:</p> Type Description <code>ConnectionError</code> <p>If the async client cannot reach the cluster (from PAC).</p> See Also <p>:meth:<code>close</code>: Release the connection.</p> <p>Example::</p> <pre><code>client = FluentClient(ClusterDefinition(\"localhost\", 3000))\nawait client.connect()\n</code></pre>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.close","title":"close  <code>async</code>","text":"<pre><code>close()\n</code></pre> <p>Close the underlying async client and clear connection state.</p> <p>Safe to call when already closed.</p> See Also <p>:meth:<code>connect</code>.</p>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__()\n</code></pre> <p>Async context manager entry.</p>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(exc_type, exc_val, exc_tb)\n</code></pre> <p>Async context manager exit.</p>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.query","title":"query","text":"<pre><code>query(*, dataset: DataSet) -&gt; QueryBuilder\n</code></pre><pre><code>query(*, key: Key) -&gt; QueryBuilder\n</code></pre><pre><code>query(*, keys: List[Key]) -&gt; QueryBuilder\n</code></pre><pre><code>query(namespace: str, set_name: str) -&gt; QueryBuilder\n</code></pre> <pre><code>query(\n    arg1=None,\n    set_name=None,\n    namespace=None,\n    *,\n    dataset=None,\n    key=None,\n    keys=None,\n    behavior=None,\n)\n</code></pre> <p>Create a query builder.</p> <p>Supports multiple calling styles:</p> <ol> <li> <p>Using a DataSet (positional or keyword):    <pre><code>users = DataSet.of(\"test\", \"users\")\nasync for record in client.query(users).execute():\n# or\nasync for record in client.query(dataset=users).execute():\n    print(record.bins)\n</code></pre></p> </li> <li> <p>Using a single Key (positional or keyword):    <pre><code>users = DataSet.of(\"test\", \"users\")\nkey = users.id(\"user123\")\nrecordset = await client.query(key).execute()\n# or\nrecordset = await client.query(key=key).execute()\n</code></pre></p> </li> <li> <p>Using multiple Keys (positional or keyword):    <pre><code>users = DataSet.of(\"test\", \"users\")\nkeys = users.ids(\"user1\", \"user2\", \"user3\")\nrecordset = await client.query(keys).execute()\n# or\nrecordset = await client.query(keys=keys).execute()\n</code></pre></p> </li> <li> <p>Explicit namespace/set (original style):    <pre><code>async for record in client.query(\n    namespace=\"test\",\n    set_name=\"users\"\n).execute():\n    print(record.bins)\n</code></pre></p> </li> </ol> <p>Parameters:</p> Name Type Description Default <code>arg1</code> <code>Optional[Union[DataSet, Key, List[Key], str]]</code> <p>Optional first positional: :class:<code>~aerospike_fluent.dataset.DataSet</code>, :class:<code>~aerospike_async.Key</code>, list of keys, or namespace string for the <code>(\"namespace\", \"set\")</code> pair form.</p> <code>None</code> <code>set_name</code> <code>Optional[str]</code> <p>When <code>arg1</code> is a namespace string, the set name as the second positional (<code>client.query(\"test\", \"users\")</code>).</p> <code>None</code> <code>namespace</code> <code>Optional[str]</code> <p>Optional third positional; not used for the usual two-string namespace/set pair (that form uses <code>arg1</code> and <code>set_name</code>).</p> <code>None</code> <code>dataset</code> <code>Optional[DataSet]</code> <p>Keyword-only :class:<code>~aerospike_fluent.dataset.DataSet</code>.</p> <code>None</code> <code>key</code> <code>Optional[Key]</code> <p>Keyword-only single key for a point read.</p> <code>None</code> <code>keys</code> <code>Optional[List[Key]]</code> <p>Keyword-only list of keys for a batch read.</p> <code>None</code> <code>behavior</code> <code>Optional[Behavior]</code> <p>Optional :class:<code>~aerospike_fluent.policy.behavior.Behavior</code> for timeouts, retries, and replica settings on this builder. If <code>None</code>, the client uses generic defaults (unlike :meth:<code>~aerospike_fluent.aio.session.Session.query</code>, which applies the session's behavior automatically).</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>QueryBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.QueryBuilder</code> for</p> <code>QueryBuilder</code> <p>chaining filters, bin selection, and execution.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If a positional argument is not a dataset, key, or key list.</p> <code>ValueError</code> <p>If required namespace/set information is missing or key lists are empty.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.session.Session.query</code>: Same builder with     session-scoped behavior.</p>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.index","title":"index","text":"<pre><code>index(*, dataset: DataSet) -&gt; IndexBuilder\n</code></pre><pre><code>index(namespace: str, set_name: str) -&gt; IndexBuilder\n</code></pre> <pre><code>index(namespace=None, set_name=None, *, dataset=None)\n</code></pre> <p>Create an index builder.</p> <p>Supports multiple calling styles:</p> <ol> <li> <p>Using a DataSet:    <pre><code>users = DataSet.of(\"test\", \"users\")\nawait client.index(dataset=users).on_bin(\"age\").named(\"age_idx\").numeric().create()\n</code></pre></p> </li> <li> <p>Explicit namespace/set (original style):    <pre><code>await client.index(\n    namespace=\"test\",\n    set_name=\"users\"\n).on_bin(\"age\").named(\"age_idx\").numeric().create()\n</code></pre></p> </li> </ol> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>Optional[str]</code> <p>The namespace name (if not using DataSet).</p> <code>None</code> <code>set_name</code> <code>Optional[str]</code> <p>The set name (if not using DataSet).</p> <code>None</code> <code>dataset</code> <code>Optional[DataSet]</code> <p>Optional DataSet to use for namespace/set.</p> <code>None</code> <p>Returns:</p> Type Description <code>IndexBuilder</code> <p>An IndexBuilder for chaining index operations.</p>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.transaction_session","title":"transaction_session","text":"<pre><code>transaction_session()\n</code></pre> <p>Create a transactional session.</p> <p>All operations within this session will be part of the same transaction. The transaction is automatically committed when the session exits successfully, or rolled back on error.</p> <p>Note: Full transaction support depends on Aerospike server capabilities. This is a placeholder for future transaction support.</p> <p>Returns:</p> Type Description <code>TransactionalSession</code> <p>A TransactionalSession for transactional operations.</p> Example <pre><code>async with client.transaction_session() as session:\n    kv = session.key_value(\"test\", \"users\")\n    await kv.put(\"user1\", {\"name\": \"John\"})\n    await kv.put(\"user2\", {\"name\": \"Jane\"})\n    # Transaction auto-committed on exit\n</code></pre>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.create_session","title":"create_session","text":"<pre><code>create_session(behavior=None)\n</code></pre> <p>Create a session with the specified behavior.</p> <p>A session represents a logical connection to the cluster with specific behavior settings that control how operations are performed (timeouts, retry policies, consistency levels, etc.).</p> <p>Parameters:</p> Name Type Description Default <code>behavior</code> <code>Optional[Behavior]</code> <p>The behavior configuration for the session.      If None, uses Behavior.DEFAULT.</p> <code>None</code> <p>Returns:</p> Type Description <code>Session</code> <p>A new :class:<code>~aerospike_fluent.aio.session.Session</code> bound to this</p> <code>Session</code> <p>client.</p> Example <p>session = client.create_session() users = DataSet.of(\"test\", \"users\") await session.upsert(users.id(1)).put({\"k\": 1}).execute()</p> Example <p>from datetime import timedelta fast = Behavior.DEFAULT.derive_with_changes(     name=\"fast\",     total_timeout=timedelta(seconds=5), ) session = client.create_session(fast)</p> See Also <p>:class:<code>~aerospike_fluent.policy.behavior.Behavior</code>: Available presets.</p>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.register_udf","title":"register_udf  <code>async</code>","text":"<pre><code>register_udf(\n    body, server_path, language=LUA, *, policy=None\n)\n</code></pre> <p>Register a UDF package from in-memory bytes on the cluster.</p> <p>Parameters:</p> Name Type Description Default <code>body</code> <code>bytes</code> <p>Raw module source (for example UTF-8 encoded Lua).</p> required <code>server_path</code> <code>str</code> <p>Path name stored on the server (often ends with <code>.lua</code>).</p> required <code>language</code> <code>UDFLang</code> <p>:class:<code>~aerospike_async.UDFLang</code>; default is Lua.</p> <code>LUA</code> <code>policy</code> <code>Optional[AdminPolicy]</code> <p>Optional :class:<code>~aerospike_async.AdminPolicy</code> (PAC leading argument); use keyword <code>policy=</code>.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>RegisterTask</code> <p>class:<code>~aerospike_async.RegisterTask</code>; await</p> <code>RegisterTask</code> <p><code>wait_till_complete(...)</code> until propagation finishes.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If not connected.</p> <code>AerospikeError</code> <p>On cluster or admin errors (via PAC).</p> See Also <p>:meth:<code>register_udf_from_file</code>: Load source from disk.</p> <p>Example::</p> <pre><code>task = await client.register_udf(\"my_module\", udf_source_code)\nawait task.wait_till_complete()\n</code></pre>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.register_udf_from_file","title":"register_udf_from_file  <code>async</code>","text":"<pre><code>register_udf_from_file(\n    client_path, server_path, language=LUA, *, policy=None\n)\n</code></pre> <p>Register a UDF by reading module bytes from a local path.</p> <p>Parameters:</p> Name Type Description Default <code>client_path</code> <code>str</code> <p>Filesystem path to the module file on the client machine.</p> required <code>server_path</code> <code>str</code> <p>Path name stored on the server.</p> required <code>language</code> <code>UDFLang</code> <p>:class:<code>~aerospike_async.UDFLang</code>; default is Lua.</p> <code>LUA</code> <code>policy</code> <code>Optional[AdminPolicy]</code> <p>Optional admin policy; use keyword <code>policy=</code>.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>RegisterTask</code> <p>class:<code>~aerospike_async.RegisterTask</code> for completion polling.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If not connected.</p> <code>OSError</code> <p>If <code>client_path</code> cannot be read.</p> <code>AerospikeError</code> <p>On cluster or admin errors (via PAC).</p> See Also <p>:meth:<code>register_udf</code>: Register from bytes.</p> <p>Example::</p> <pre><code>task = await client.register_udf_from_file(\"scripts/my_module.lua\", \"my_module.lua\")\nawait task.wait_till_complete()\n</code></pre>"},{"location":"api/client/#aerospike_fluent.aio.client.FluentClient.remove_udf","title":"remove_udf  <code>async</code>","text":"<pre><code>remove_udf(server_path, *, policy=None)\n</code></pre> <p>Remove a registered UDF package from the cluster.</p> <p>Parameters:</p> Name Type Description Default <code>server_path</code> <code>str</code> <p>Same server path used when registering the module.</p> required <code>policy</code> <code>Optional[AdminPolicy]</code> <p>Optional admin policy; use keyword <code>policy=</code>.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>UdfRemoveTask</code> <p>class:<code>~aerospike_async.UdfRemoveTask</code>; await completion like register.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If not connected.</p> <code>AerospikeError</code> <p>On cluster or admin errors (via PAC).</p> <p>Example::</p> <pre><code>task = await client.remove_udf(\"my_module\")\nawait task.wait_till_complete()\n</code></pre>"},{"location":"api/cluster-definition/","title":"ClusterDefinition &amp; Host","text":"<p>ClusterDefinition - Builder for configuring Aerospike cluster connections.</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.Host","title":"Host","text":"<pre><code>Host(name, port, tls_name=None)\n</code></pre> <p>Seed address for cluster discovery.</p> <p>Example::</p> <pre><code>host = Host(\"192.168.1.10\", 3000)\n# or use the convenience parser\nhosts = Host.parse_hosts(\"host1:3000,host2:3000\", 3000)\n</code></pre> <p>Initialize a Host.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Hostname or IP address</p> required <code>port</code> <code>int</code> <p>Port number</p> required <code>tls_name</code> <code>Optional[str]</code> <p>Optional TLS name for certificate validation</p> <code>None</code>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.Host.of","title":"of  <code>staticmethod</code>","text":"<pre><code>of(name, port)\n</code></pre> <p>Create a Host instance.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Hostname or IP address.</p> required <code>port</code> <code>int</code> <p>Port number.</p> required <p>Returns:</p> Type Description <code>Host</code> <p>A Host with the given name and port.</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.Host.parse_hosts","title":"parse_hosts  <code>staticmethod</code>","text":"<pre><code>parse_hosts(host_string, default_port)\n</code></pre> <p>Parse a host string into a list of Host objects.</p> <p>Format: <code>host1:port1,host2:port2</code> or <code>host1,host2</code> (uses <code>default_port</code>).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a port segment is present but not a valid integer.</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition","title":"ClusterDefinition","text":"<pre><code>ClusterDefinition(hostname=None, port=None, hosts=None)\n</code></pre> <p>Configure seeds, auth, TLS, rack awareness, and validation before :meth:<code>connect</code>.</p> <p>Call :meth:<code>connect</code> to obtain a live :class:<code>~aerospike_fluent.aio.cluster.Cluster</code>. The sync counterpart lives under <code>aerospike_fluent.sync.cluster_definition</code>.</p> Example <pre><code>cluster = await (\n    ClusterDefinition(\"localhost\", 3100)\n    .with_native_credentials(\"user\", \"secret\")\n    .using_services_alternate()\n    .preferring_racks(1, 2)\n    .validate_cluster_name_is(\"my-cluster\")\n    .connect()\n)\n</code></pre> See Also <p>:class:<code>~aerospike_fluent.aio.cluster.Cluster</code></p> <p>Create a cluster definition.</p> <p>Parameters:</p> Name Type Description Default <code>hostname</code> <code>Optional[str]</code> <p>Hostname or IP address (if single host)</p> <code>None</code> <code>port</code> <code>Optional[int]</code> <p>Port number (if single host)</p> <code>None</code> <code>hosts</code> <code>Optional[Union[List[Host], tuple[Host, ...]]]</code> <p>List of Host objects (if multiple hosts)</p> <code>None</code> <p>Examples:</p> <p>ClusterDefinition(\"localhost\", 3000) ClusterDefinition(hosts=[Host.of(\"host1\", 3000), Host.of(\"host2\", 3000)])</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.auth_mode","title":"auth_mode  <code>property</code>","text":"<pre><code>auth_mode\n</code></pre> <p>The current authentication mode.</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.with_native_credentials","title":"with_native_credentials","text":"<pre><code>with_native_credentials(user_name, password)\n</code></pre> <p>Sets authentication credentials using Aerospike's internal authentication.</p> <p>Hashed password is stored on the server. Pass empty strings for both parameters to disable authentication.</p> <p>Parameters:</p> Name Type Description Default <code>user_name</code> <code>str</code> <p>The username for authentication</p> required <code>password</code> <code>str</code> <p>The password for authentication</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p> <p>Example::</p> <pre><code>cd = ClusterDefinition(\"localhost\", 3000).with_native_credentials(\"admin\", \"pass123\")\n</code></pre>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.with_external_credentials","title":"with_external_credentials","text":"<pre><code>with_external_credentials(user_name, password)\n</code></pre> <p>Sets authentication credentials using external authentication (e.g. LDAP).</p> <p>External authentication is configured on the server. If TLS is configured, the clear password is sent on node login via TLS. Raises an error at connect time if TLS is not configured.</p> <p>Parameters:</p> Name Type Description Default <code>user_name</code> <code>str</code> <p>The username for authentication</p> required <code>password</code> <code>str</code> <p>The password for authentication</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.with_certificate_credentials","title":"with_certificate_credentials","text":"<pre><code>with_certificate_credentials()\n</code></pre> <p>Configures certificate-based (PKI) authentication.</p> <p>Uses client certificates instead of username/password credentials. Automatically enables TLS if not already configured. Requires server version 5.7.0+.</p> <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If any host is missing a TLS name</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.validate_cluster_name_is","title":"validate_cluster_name_is","text":"<pre><code>validate_cluster_name_is(cluster_name)\n</code></pre> <p>Validates that the cluster name matches the expected value.</p> <p>This enables cluster name validation to ensure the client connects to the expected cluster. If the actual cluster name doesn't match, the connection will fail.</p> <p>Parameters:</p> Name Type Description Default <code>cluster_name</code> <code>str</code> <p>The expected cluster name to validate against</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p> <p>Example::</p> <pre><code>cd = ClusterDefinition(\"localhost\", 3000).validate_cluster_name_is(\"my-cluster\")\n</code></pre>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.preferring_racks","title":"preferring_racks","text":"<pre><code>preferring_racks(*racks)\n</code></pre> <p>Sets preferred racks for rack-aware operations.</p> <p>This enables rack awareness and specifies which racks should be preferred for read operations. Rack awareness helps improve performance by reading from local racks when possible.</p> <p>Parameters:</p> Name Type Description Default <code>*racks</code> <code>int</code> <p>The rack IDs to prefer, in order of preference</p> <code>()</code> <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This builder for chaining.</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.using_services_alternate","title":"using_services_alternate","text":"<pre><code>using_services_alternate()\n</code></pre> <p>Enables the use of alternate services for cluster discovery.</p> <p>When enabled, the client will use alternate service endpoints for cluster discovery, which can be useful in certain network configurations or when using service mesh solutions.</p> <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This builder for chaining.</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.fail_if_not_connected","title":"fail_if_not_connected","text":"<pre><code>fail_if_not_connected(fail)\n</code></pre> <p>Controls whether <code>connect()</code> raises if the cluster is unreachable.</p> <p>If <code>True</code> (the default), <code>connect()</code> raises a <code>ConnectionError</code> when all seed connections fail or a seed connects but none of its peers are reachable.</p> <p>If <code>False</code>, a partial cluster is created and the client will automatically connect to the remaining nodes when they become available.</p> <p>Parameters:</p> Name Type Description Default <code>fail</code> <code>bool</code> <p>Whether to raise on connection failure.</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This builder for chaining.</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.with_ip_map","title":"with_ip_map","text":"<pre><code>with_ip_map(ip_map)\n</code></pre> <p>Sets an IP address translation table for cluster node discovery.</p> <p>Used when clients from different networks need different IP addresses to reach the same server nodes (e.g. inside vs. outside a VPN or NAT). The key is the IP address returned from server info requests; the value is the actual IP address the client should connect to.</p> <p>Consider using <code>using_services_alternate()</code> instead, which lets the server handle address translation without client-side configuration.</p> <p>Parameters:</p> Name Type Description Default <code>ip_map</code> <code>dict[str, str]</code> <p>Mapping of server-reported IPs to actual connection IPs</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This builder for chaining.</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.with_system_settings","title":"with_system_settings","text":"<pre><code>with_system_settings(settings)\n</code></pre> <p>Set cluster-wide system settings (connection pool, tend interval, etc.).</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>SystemSettings</code> <p>The SystemSettings to apply.</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This builder for chaining.</p> <p>Example::</p> <pre><code>cluster = await ClusterDefinition(\"localhost\", 3000) \\\n    .with_system_settings(SystemSettings(\n        max_connections_per_node=200,\n        tend_interval=timedelta(seconds=2),\n    )) \\\n    .connect()\n</code></pre>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.with_tls_config_of","title":"with_tls_config_of","text":"<pre><code>with_tls_config_of()\n</code></pre> <p>Begins TLS configuration using a fluent builder pattern.</p> <p>This method returns a TlsBuilder that allows you to configure various TLS settings such as TLS name, CA file, protocols, ciphers, and other TLS-specific options. Call done() on the TlsBuilder to return to this ClusterDefinition for further configuration.</p> <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>A TlsBuilder for configuring TLS settings.</p>"},{"location":"api/cluster-definition/#aerospike_fluent.aio.cluster_definition.ClusterDefinition.connect","title":"connect  <code>async</code>","text":"<pre><code>connect()\n</code></pre> <p>Establishes a connection to the Aerospike cluster.</p> <p>This method creates and returns a Cluster instance using the configured parameters. The returned Cluster should be closed when no longer needed to properly release resources.</p> Example with async context manager <pre><code>async with await ClusterDefinition(\"localhost\", 3100).connect() as cluster:\n    session = cluster.create_session(Behavior.DEFAULT)\n    # Use the session...\n</code></pre> <p>Returns:</p> Type Description <code>Cluster</code> <p>A connected Cluster instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If PKI auth is configured but hosts are missing TLS names</p> <code>ConnectionError</code> <p>If <code>fail_if_not_connected</code> is True (default) and the cluster is unreachable</p>"},{"location":"api/cluster/","title":"Cluster","text":""},{"location":"api/cluster/#aerospike_fluent.aio.cluster.Cluster","title":"Cluster","text":"<pre><code>Cluster(fluent_client)\n</code></pre> <p>Live connection to a cluster, obtained from :meth:<code>ClusterDefinition.connect</code>.</p> <p>Owns a connected :class:<code>~aerospike_fluent.aio.client.FluentClient</code> and exposes :meth:<code>create_session</code> / :meth:<code>create_transactional_session</code>. Prefer <code>async with await ClusterDefinition(...).connect() as cluster</code> so :meth:<code>close</code> runs on exit.</p> Example <pre><code>async with await ClusterDefinition(\"localhost\", 3100).connect() as cluster:\n    session = cluster.create_session(Behavior.DEFAULT)\n</code></pre> See Also <p>:class:<code>~aerospike_fluent.aio.cluster_definition.ClusterDefinition</code></p> <p>Initialize a Cluster instance.</p> <p>Parameters:</p> Name Type Description Default <code>fluent_client</code> <code>FluentClient</code> <p>The underlying FluentClient instance</p> required Note <p>This should not be called directly. Use ClusterDefinition.connect() instead.</p>"},{"location":"api/cluster/#aerospike_fluent.aio.cluster.Cluster.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__()\n</code></pre> <p>Async context manager entry.</p>"},{"location":"api/cluster/#aerospike_fluent.aio.cluster.Cluster.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(exc_type, exc_val, exc_tb)\n</code></pre> <p>Async context manager exit.</p>"},{"location":"api/cluster/#aerospike_fluent.aio.cluster.Cluster.create_session","title":"create_session","text":"<pre><code>create_session(behavior=None)\n</code></pre> <p>Open a :class:<code>~aerospike_fluent.aio.session.Session</code> with optional behavior.</p> <p>A session represents a logical connection to the cluster with specific behavior settings that control how operations are performed (timeouts, retry policies, consistency levels, etc.).</p> <p>Parameters:</p> Name Type Description Default <code>behavior</code> <code>Optional[Behavior]</code> <p>Defaults to :attr:<code>~aerospike_fluent.policy.behavior.Behavior.DEFAULT</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Session</code> <p>Session bound to this cluster's fluent client.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.client.FluentClient.create_session</code></p>"},{"location":"api/cluster/#aerospike_fluent.aio.cluster.Cluster.create_transactional_session","title":"create_transactional_session","text":"<pre><code>create_transactional_session(behavior=None)\n</code></pre> <p>Return a transactional session facade (behavior reserved for API parity).</p> <p>Parameters:</p> Name Type Description Default <code>behavior</code> <code>Optional[Behavior]</code> <p>Accepted for symmetry with :meth:<code>create_session</code>; the underlying client may not apply it yet.</p> <code>None</code> <p>Returns:</p> Type Description <code>TransactionalSession</code> <p>class:<code>~aerospike_fluent.aio.transactional_session.TransactionalSession</code>.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.client.FluentClient.transaction_session</code></p>"},{"location":"api/cluster/#aerospike_fluent.aio.cluster.Cluster.is_connected","title":"is_connected","text":"<pre><code>is_connected()\n</code></pre> <p>Mirror :attr:<code>~aerospike_fluent.aio.client.FluentClient.is_connected</code>.</p>"},{"location":"api/cluster/#aerospike_fluent.aio.cluster.Cluster.close","title":"close  <code>async</code>","text":"<pre><code>close()\n</code></pre> <p>Close the fluent client and release cluster resources.</p> <p>Invoked automatically when used as an async context manager.</p>"},{"location":"api/dataset/","title":"DataSet","text":""},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet","title":"DataSet","text":"<pre><code>DataSet(namespace, set_name)\n</code></pre> <p>Pair a namespace and set name for building :class:<code>~aerospike_async.Key</code> values.</p> <p>Use :meth:<code>of</code> as the factory, then :meth:<code>id</code> for one key or :meth:<code>ids</code> for many. Pass keys to :meth:<code>~aerospike_fluent.aio.session.Session.query</code>, :meth:<code>~aerospike_fluent.aio.session.Session.upsert</code>, and other session APIs.</p> Example <p>users = DataSet.of(\"test\", \"users\") k = users.id(\"user-123\") ks = users.ids(\"a\", \"b\", \"c\")</p> See Also <p>:class:<code>~aerospike_fluent.aio.session.Session</code>: Operations on keys.</p> <p>Create a DataSet instance.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The Aerospike namespace</p> required <code>set_name</code> <code>str</code> <p>The set name within the namespace</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If namespace or set_name is empty</p>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.namespace","title":"namespace  <code>property</code>","text":"<pre><code>namespace\n</code></pre> <p>Get the namespace of this dataset.</p>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.set_name","title":"set_name  <code>property</code>","text":"<pre><code>set_name\n</code></pre> <p>Get the set name of this dataset.</p>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.of","title":"of  <code>staticmethod</code>","text":"<pre><code>of(namespace, set_name)\n</code></pre> <p>Construct a dataset handle for <code>namespace</code> and <code>set_name</code>.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>Non-empty Aerospike namespace name.</p> required <code>set_name</code> <code>str</code> <p>Non-empty set name within that namespace.</p> required <p>Returns:</p> Name Type Description <code>A</code> <code>DataSet</code> <p>class:<code>DataSet</code> sharing the same equality/hash for the pair.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If either string is empty.</p> Example <p>orders = DataSet.of(\"prod\", \"orders\")</p> See Also <p>:meth:<code>id</code>: Build a single user key. :meth:<code>ids</code>: Build several keys at once.</p>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.id","title":"id","text":"<pre><code>id(identifier)\n</code></pre> <p>Build one :class:<code>~aerospike_async.Key</code> for this namespace and set.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>Union[str, int, bytes]</code> <p>User key value (string, integer, or bytes).</p> required <p>Returns:</p> Name Type Description <code>A</code> <code>Key</code> <p>class:<code>~aerospike_async.Key</code> bound to this dataset's namespace/set.</p> Example <p>users = DataSet.of(\"test\", \"users\") key_str = users.id(\"user123\") key_int = users.id(12345) key_bin = users.id(b\"pk\")</p> See Also <p>:meth:<code>ids</code>: Multiple keys in one call.</p>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.id_from_digest","title":"id_from_digest","text":"<pre><code>id_from_digest(digest)\n</code></pre> <p>Create an Aerospike key from a digest.</p> <p>Parameters:</p> Name Type Description Default <code>digest</code> <code>Union[str, bytes]</code> <p>The digest as a hex string or bytes</p> required <p>Returns:</p> Type Description <code>Key</code> <p>A new Key instance created from the digest</p> Example <pre><code>users = DataSet.of(\"test\", \"users\")\n# From hex string\nkey = users.id_from_digest(\"a1b2c3d4...\")\n# From bytes\nkey = users.id_from_digest(b\"\\xa1\\xb2\\xc3...\")\n</code></pre>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.id_for_object","title":"id_for_object","text":"<pre><code>id_for_object(obj)\n</code></pre> <p>Create an Aerospike key from an object identifier. Supports String, Integer, Long, and byte array types.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>object</code> <p>The object to use as the key identifier</p> required <p>Returns:</p> Type Description <code>Key</code> <p>A new Key instance</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the object type is not supported</p> Example <pre><code>users = DataSet.of(\"test\", \"users\")\nkey1 = users.id_for_object(\"user123\")     # String key\nkey2 = users.id_for_object(123)           # Integer key\nkey3 = users.id_for_object(12345)         # Integer key\nkey4 = users.id_for_object(b\"bytes\")      # Bytes key\n</code></pre>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.ids","title":"ids","text":"<pre><code>ids(*identifiers: str) -&gt; List[Key]\n</code></pre><pre><code>ids(*identifiers: int) -&gt; List[Key]\n</code></pre><pre><code>ids(*identifiers: bytes) -&gt; List[Key]\n</code></pre><pre><code>ids(identifiers: List[Union[str, int, bytes]]) -&gt; List[Key]\n</code></pre> <pre><code>ids(*identifiers)\n</code></pre> <p>Build many keys sharing this dataset's namespace and set.</p> <p>Call either with several positional identifiers (<code>ids(\"a\", \"b\", \"c\")</code>) or with a single list (<code>ids([\"a\", \"b\", \"c\"])</code>). Mixed-type lists use :meth:<code>id_for_object</code> per element when the single-list form is used.</p> <p>Parameters:</p> Name Type Description Default <code>*identifiers</code> <code>Union[str, int, bytes]</code> <p>One or more identifiers, or exactly one list of identifiers.</p> <code>()</code> <p>Returns:</p> Type Description <code>List[Key]</code> <p>A list of :class:<code>~aerospike_async.Key</code> instances in input order.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If list elements are not supported key types.</p> Example <p>users = DataSet.of(\"test\", \"users\") keys1 = users.ids(\"u1\", \"u2\", \"u3\") keys2 = users.ids([\"u1\", \"u2\"]) keys3 = users.ids(1, 2, 3)</p> See Also <p>:meth:<code>id</code>: Single-key helper.</p>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.ids_from_digests","title":"ids_from_digests","text":"<pre><code>ids_from_digests(*digests)\n</code></pre> <p>Create multiple Aerospike keys from digests.</p> <p>Parameters:</p> Name Type Description Default <code>*digests</code> <code>Union[str, bytes]</code> <p>Variable number of digests (hex strings or bytes)</p> <code>()</code> <p>Returns:</p> Type Description <code>List[Key]</code> <p>A list of Key instances created from the digests</p> Example <pre><code>users = DataSet.of(\"test\", \"users\")\nkeys = users.ids_from_digests(\"digest1\", \"digest2\", b\"digest3\")\n</code></pre>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>Return a string representation of this DataSet.</p>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.__eq__","title":"__eq__","text":"<pre><code>__eq__(other)\n</code></pre> <p>Check equality with another DataSet.</p>"},{"location":"api/dataset/#aerospike_fluent.dataset.DataSet.__hash__","title":"__hash__","text":"<pre><code>__hash__()\n</code></pre> <p>Return hash of this DataSet.</p>"},{"location":"api/dsl-filter-gen/","title":"Filter Generation","text":"<p>Filter generation from DSL expressions.</p> <p>This module provides functionality to generate optimal secondary index Filters from DSL expressions based on available indexes. It splits expressions to use secondary indexes where possible and filter expressions for the rest.</p> <p>The implementation uses a tree-based approach: 1. Build an expression tree that tracks filter eligibility 2. Mark nodes under OR as \"excluded from filter\" (can't use secondary index) 3. Collect all filterable expressions grouped by cardinality 4. Choose the best by cardinality (or alphabetically if tied) 5. Generate complementary Exp, skipping the part used for Filter</p>"},{"location":"api/dsl-filter-gen/#aerospike_fluent.dsl.filter_gen.Index","title":"Index  <code>dataclass</code>","text":"<pre><code>Index(\n    bin,\n    index_type,\n    namespace=None,\n    name=None,\n    bin_values_ratio=None,\n    collection_index_type=None,\n    ctx=None,\n)\n</code></pre> <p>Represents an Aerospike secondary index.</p> <p>Attributes:</p> Name Type Description <code>namespace</code> <code>Optional[str]</code> <p>Namespace of the indexed bin.</p> <code>bin</code> <code>str</code> <p>Name of the indexed bin.</p> <code>index_type</code> <code>IndexTypeEnum</code> <p>Type of the index (NUMERIC, STRING, GEO2D_SPHERE).</p> <code>name</code> <code>Optional[str]</code> <p>Optional name of the index.</p> <code>bin_values_ratio</code> <code>Optional[float]</code> <p>Cardinality ratio (entries_per_bval). Higher = more selective. Used to choose optimal index when multiple are available.</p> <code>collection_index_type</code> <code>Optional[CollectionIndexType]</code> <p>Collection index type for list/map indexes.</p> <code>ctx</code> <code>Optional[List[CTX]]</code> <p>Array of CTX representing context of the index.</p>"},{"location":"api/dsl-filter-gen/#aerospike_fluent.dsl.filter_gen.IndexContext","title":"IndexContext  <code>dataclass</code>","text":"<pre><code>IndexContext(namespace, indexes=list())\n</code></pre> <p>Holds namespace and indexes for filter generation.</p> <p>Attributes:</p> Name Type Description <code>namespace</code> <code>str</code> <p>Namespace to match with index namespaces.</p> <code>indexes</code> <code>List[Index]</code> <p>Collection of Index objects for filter generation.</p>"},{"location":"api/dsl-filter-gen/#aerospike_fluent.dsl.filter_gen.IndexContext.of","title":"of  <code>classmethod</code>","text":"<pre><code>of(namespace, indexes)\n</code></pre> <p>Create IndexContext with namespace and indexes.</p>"},{"location":"api/dsl-filter-gen/#aerospike_fluent.dsl.filter_gen.IndexTypeEnum","title":"IndexTypeEnum","text":"<p>               Bases: <code>Enum</code></p> <p>Index type for filter generation matching.</p>"},{"location":"api/dsl-filter-gen/#aerospike_fluent.dsl.filter_gen.IndexTypeEnum.to_aerospike","title":"to_aerospike","text":"<pre><code>to_aerospike()\n</code></pre> <p>Convert to aerospike_async IndexType.</p>"},{"location":"api/dsl-filter-gen/#aerospike_fluent.dsl.filter_gen.ParseResult","title":"ParseResult  <code>dataclass</code>","text":"<pre><code>ParseResult(filter, exp)\n</code></pre> <p>Result of parsing DSL with filter generation.</p> <p>Contains both a secondary index Filter (if applicable) and a filter Exp (for remaining expression parts that can't use secondary indexes).</p> <p>Attributes:</p> Name Type Description <code>filter</code> <code>Optional[Filter]</code> <p>Secondary index Filter. None if no applicable index found.</p> <code>exp</code> <code>Optional[FilterExpression]</code> <p>Filter expression for remaining parts. None if fully covered by filter.</p>"},{"location":"api/dsl-filter-gen/#aerospike_fluent.dsl.filter_gen.FilterGenerator","title":"FilterGenerator","text":"<pre><code>FilterGenerator(index_context=None)\n</code></pre> <p>Generates optimal Filter from DSL expression based on available indexes.</p> <p>The generator analyzes the expression tree to find parts that can use secondary indexes, choosing the most selective index based on cardinality. Parts that can't use secondary indexes remain as filter expressions.</p> <p>Initialize with optional index context.</p>"},{"location":"api/dsl-filter-gen/#aerospike_fluent.dsl.filter_gen.FilterGenerator.generate","title":"generate","text":"<pre><code>generate(\n    dsl_string,\n    placeholder_values=None,\n    *,\n    hint_index_name=None,\n    hint_bin_name=None,\n)\n</code></pre> <p>Generate Filter and Exp from DSL string.</p> <p>Parameters:</p> Name Type Description Default <code>dsl_string</code> <code>str</code> <p>The DSL expression string.</p> required <code>placeholder_values</code> <code>Any</code> <p>Optional placeholder values.</p> <code>None</code> <code>hint_index_name</code> <code>Optional[str]</code> <p>If set, the generated Filter uses a <code>_by_index</code> variant keyed to this index name instead of the bin name.</p> <code>None</code> <code>hint_bin_name</code> <code>Optional[str]</code> <p>If set, the generated Filter addresses this bin name instead of the one found in the DSL expression.</p> <code>None</code> <p>Returns:</p> Type Description <code>ParseResult</code> <p>ParseResult with Filter and/or Exp.</p>"},{"location":"api/dsl-parser/","title":"DSL Parser","text":"<p>DSL Parser - Parse text DSL strings into FilterExpression objects.</p> This module provides parsing of text-based DSL expressions like <p>\"$.country == 'US' and $.order_total &gt; 500\"</p> <p>into Aerospike FilterExpression objects.</p> Supports parameterized queries with placeholders <p>parse_dsl(\"$.age &gt; ?0\", 30)</p> Also supports parsing paths into CTX arrays <p>parse_ctx(\"$.listBin.[0].[1]\")  # Returns [CTX.list_index(0), CTX.list_index(1)]</p> Also supports filter generation with index context <p>parse_dsl_with_index(\"$.intBin1 &gt; 100 and $.intBin2 &lt; 50\", index_context)</p>"},{"location":"api/dsl-parser/#aerospike_fluent.dsl.parser--returns-parseresult-with-optimal-filter-remaining-exp","title":"Returns ParseResult with optimal Filter + remaining Exp","text":""},{"location":"api/dsl-parser/#aerospike_fluent.dsl.parser.DSLParser","title":"DSLParser","text":"<p>Parser for Aerospike DSL expressions.</p>"},{"location":"api/dsl-parser/#aerospike_fluent.dsl.parser.DSLParser.parse","title":"parse","text":"<pre><code>parse(dsl_string, placeholder_values=None)\n</code></pre> <p>Parse a DSL string into a FilterExpression.</p> <p>Parameters:</p> Name Type Description Default <code>dsl_string</code> <code>str</code> <p>The DSL expression string (e.g., \"$.country == 'US' and $.order_total &gt; 500\")</p> required <code>placeholder_values</code> <code>Optional[PlaceholderValues]</code> <p>Optional values for placeholders (?0, ?1, etc.)</p> <code>None</code> <p>Returns:</p> Type Description <code>FilterExpression</code> <p>A FilterExpression object that can be used in queries.</p> <p>Raises:</p> Type Description <code>DslParseException</code> <p>If the DSL string cannot be parsed.</p>"},{"location":"api/dsl-parser/#aerospike_fluent.dsl.parser.PlaceholderValues","title":"PlaceholderValues","text":"<pre><code>PlaceholderValues(*values)\n</code></pre> <p>Internal container for placeholder values (?0, ?1, ...) in DSL queries.</p> <p>Not part of the public API \u2014 use parse_dsl() with *args instead.</p>"},{"location":"api/dsl-parser/#aerospike_fluent.dsl.parser.parse_dsl","title":"parse_dsl","text":"<pre><code>parse_dsl(dsl_string, *args)\n</code></pre> <p>Parse a DSL string into a FilterExpression.</p> <p>Convenience function that uses a global parser instance.</p> <p>Parameters:</p> Name Type Description Default <code>dsl_string</code> <code>str</code> <p>The DSL expression string.</p> required <code>*args</code> <code>Any</code> <p>Values for placeholders ?0, ?1, ?2, etc.</p> <code>()</code> <p>Returns:</p> Type Description <code>FilterExpression</code> <p>A FilterExpression object.</p> Example <p>expr = parse_dsl(\"$.age &gt; 30\") expr = parse_dsl(\"$.age &gt; ?0 and $.name == ?1\", 30, \"John\")</p>"},{"location":"api/dsl-parser/#aerospike_fluent.dsl.parser.parse_dsl_with_index","title":"parse_dsl_with_index","text":"<pre><code>parse_dsl_with_index(\n    dsl_string,\n    index_context=None,\n    placeholder_values=None,\n    *,\n    hint_index_name=None,\n    hint_bin_name=None,\n)\n</code></pre> <p>Parse a DSL string and generate optimal Filter + Exp based on available indexes.</p> <p>This function analyzes the DSL expression and available secondary indexes to: 1. Extract parts that can use secondary index Filters (more efficient) 2. Return remaining parts as filter Exp (for post-filtering)</p> <p>The algorithm: 1. Build an expression tree that tracks filter eligibility 2. Mark nodes under OR as \"excluded from filter\" (can't use secondary index) 3. Collect all filterable expressions grouped by cardinality 4. Choose the best by cardinality (or alphabetically if tied) 5. Generate complementary Exp, skipping the part used for Filter</p> <p>Rules for filter generation: - AND expressions: One part can become Filter, rest becomes Exp - OR expressions: Cannot use Filter (need to evaluate both branches) - Nested AND inside OR: The AND parts are still excluded - AND(a, OR(b, c)): 'a' can become Filter, OR(b,c) becomes Exp - Only simple comparisons (==, &gt;, &lt;, &gt;=, &lt;=) on indexed bins can become Filters - String comparisons (&gt;, &lt;, etc.) are not supported by secondary index</p> <p>Parameters:</p> Name Type Description Default <code>dsl_string</code> <code>str</code> <p>The DSL expression string.</p> required <code>index_context</code> <code>Optional[IndexContext]</code> <p>IndexContext with available indexes. If None, only Exp is returned.</p> <code>None</code> <code>placeholder_values</code> <code>Optional[Sequence[Any]]</code> <p>Optional sequence of values for placeholders (?0, ?1, etc.)</p> <code>None</code> <code>hint_index_name</code> <code>Optional[str]</code> <p>Force the generated Filter to target this named secondary index via <code>Filter.*_by_index()</code>.</p> <code>None</code> <code>hint_bin_name</code> <code>Optional[str]</code> <p>Override the bin name used for the generated Filter.</p> <code>None</code> <p>Returns:</p> Type Description <code>ParseResult</code> <p>ParseResult containing:</p> <code>ParseResult</code> <ul> <li>filter: Secondary index Filter (or None if not applicable)</li> </ul> <code>ParseResult</code> <ul> <li>exp: Filter expression for remaining parts (or None if fully covered)</li> </ul> Example <p>indexes = [     Index(bin=\"intBin1\", index_type=IndexTypeEnum.NUMERIC, bin_values_ratio=0),     Index(bin=\"intBin2\", index_type=IndexTypeEnum.NUMERIC, bin_values_ratio=1), ] ctx = IndexContext.of(\"test\", indexes)</p> <p>result = parse_dsl_with_index(\"$.intBin1 &gt; 100 and $.intBin2 &lt; 1000\", ctx)</p> <p>result = parse_dsl_with_index(\"$.intBin1 &gt; ?0\", ctx, (100,))</p>"},{"location":"api/dsl-parser/#aerospike_fluent.dsl.parser.parse_ctx","title":"parse_ctx","text":"<pre><code>parse_ctx(path)\n</code></pre> <p>Parse a DSL path into a list of CTX objects.</p> <p>Converts a DSL path like \"$.listBin.[0].[1]\" into a CTX array for use with secondary index context operations.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>The DSL path string (e.g., \"$.listBin.[0]\", \"$.mapBin.key.subkey\")</p> required <p>Returns:</p> Type Description <code>List[CTX]</code> <p>A list of CTX objects representing the path context.</p> <p>Raises:</p> Type Description <code>DslParseException</code> <p>If the path is invalid or unsupported.</p> Example <p>ctx = parse_ctx(\"$.listBin.[0].[1]\")</p>"},{"location":"api/dsl-parser/#aerospike_fluent.dsl.parser.parse_ctx--returns-ctxlist_index0-ctxlist_index1","title":"Returns [CTX.list_index(0), CTX.list_index(1)]","text":"<p>ctx = parse_ctx(\"$.mapBin.a.bb\")</p>"},{"location":"api/dsl-parser/#aerospike_fluent.dsl.parser.parse_ctx--returns-ctxmap_keya-ctxmap_keybb","title":"Returns [CTX.map_key(\"a\"), CTX.map_key(\"bb\")]","text":""},{"location":"api/error-strategy/","title":"Error Strategy","text":"<p>Error handling strategy for fluent operations.</p> <p>Controls how per-record errors are surfaced during execution:</p> <ul> <li>Default (no argument): single-key operations raise immediately;   batch / multi-key operations embed errors in the <code>RecordStream</code>.</li> <li>ErrorStrategy.IN_STREAM: always embed errors as <code>RecordResult</code>   entries with non-OK result codes, even for single-key operations.</li> <li>ErrorHandler callback: errors are dispatched to the callback and   excluded from the returned stream.</li> </ul>"},{"location":"api/error-strategy/#aerospike_fluent.error_strategy.ErrorHandler","title":"ErrorHandler  <code>module-attribute</code>","text":"<pre><code>ErrorHandler = Callable[\n    [\"Key\", int, \"AerospikeError\"], None\n]\n</code></pre> <p>Callback <code>(key, index, exception) -&gt; None</code> for per-record error handling.</p> <p>The callback receives the failed record's key, its position in the batch (0-based; 0 for single-key, -1 for queries), and the typed exception. Errors dispatched to the handler are excluded from the returned stream.</p>"},{"location":"api/error-strategy/#aerospike_fluent.error_strategy.OnError","title":"OnError  <code>module-attribute</code>","text":"<pre><code>OnError = Union[ErrorStrategy, ErrorHandler]\n</code></pre> <p>Type alias for the <code>on_error</code> parameter of <code>execute()</code>.</p>"},{"location":"api/error-strategy/#aerospike_fluent.error_strategy.ErrorStrategy","title":"ErrorStrategy","text":"<p>               Bases: <code>Enum</code></p> <p>Strategy for handling per-record errors during execution.</p> <p>Pass to <code>execute(on_error=...)</code> to override the default behavior.</p> <p>Example::     stream = await (         session.query(k1, k2).execute(on_error=ErrorStrategy.IN_STREAM)     )</p>"},{"location":"api/error-strategy/#aerospike_fluent.error_strategy.ErrorStrategy.IN_STREAM","title":"IN_STREAM  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>IN_STREAM = 'in_stream'\n</code></pre> <p>Embed errors in the <code>RecordStream</code> as <code>RecordResult</code> entries.</p>"},{"location":"api/exceptions/","title":"Exceptions","text":"<p>Typed exceptions for the fluent client.</p> <p>Subclasses of :class:<code>AerospikeError</code> mirror common server and client outcomes so callers can handle failures selectively (for example <code>except GenerationError</code>) instead of comparing result codes everywhere.</p> <p>At public boundaries, errors from the underlying async client are normalized with :func:<code>_convert_pac_exception</code>. Callers should chain causes explicitly: <code>raise _convert_pac_exception(exc) from exc</code>.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.AerospikeError","title":"AerospikeError","text":"<pre><code>AerospikeError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>Base class for fluent-client failures.</p> <p>Raised directly when no more specific subclass applies, including unmapped server result codes (see :func:<code>_result_code_to_exception</code>). Prefer catching concrete subclasses when you need targeted handling, and fall back to this type for all other Aerospike-related errors.</p> <p>Attributes:</p> Name Type Description <code>result_code</code> <p>Server :class:<code>~aerospike_async.exceptions.ResultCode</code> when the failure came from a result code; <code>None</code> for purely client-side issues (for example connection setup).</p> <code>in_doubt</code> <p><code>True</code> when a write may have completed on the server despite the error; safe retry usually requires a read-verify strategy.</p> <p>Example::     try:         stream = await session.query(key).bins([\"x\"]).execute()         await stream.first_or_raise()     except AerospikeError as err:         code = err.result_code         ...</p> See Also <p>:func:<code>_result_code_to_exception</code>: Maps result codes to this type or a     subclass.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.TimeoutError","title":"TimeoutError","text":"<pre><code>TimeoutError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when an operation exceeds a client or server timeout.</p> <p>Covers socket-level timeouts and server-reported timeout result codes. This type shares a name with Python's built-in :exc:<code>TimeoutError</code>; always import it from :mod:<code>aerospike_fluent</code> or this module when handling fluent client timeouts.</p> <p>Attributes:</p> Name Type Description <code>result_code</code> <p>Set when the server returned a timeout-related code; otherwise often <code>None</code> for client-side timeouts.</p> See Also <p>:class:<code>ConnectionError</code>: Cluster reachability rather than deadline     exceeded.</p> <p>Example::     try:         await stream.first_or_raise()     except TimeoutError:         ...  # retry or fall back</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.ConnectionError","title":"ConnectionError","text":"<pre><code>ConnectionError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when the client cannot establish or keep a cluster connection.</p> <p>Typical causes include refused sockets, TLS handshake failure, or loss of connectivity mid-flight. Distinct from :class:<code>TimeoutError</code>, which signals a deadline rather than an immediate transport failure.</p> <p>Attributes:</p> Name Type Description <code>result_code</code> <p>Usually <code>None</code> because the failure occurs before a server result code is available.</p> <p>Example::     try:         async with FluentClient(...) as client:             ...     except ConnectionError:         ...  # cluster unreachable</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.InvalidNodeError","title":"InvalidNodeError","text":"<pre><code>InvalidNodeError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when the chosen node is unknown, wrong role, or not usable.</p> <p>Use for diagnosing cluster topology or client routing issues rather than application-level data errors.</p> <p>Attributes:</p> Name Type Description <code>result_code</code> <p>Usually <code>None</code>.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.InvalidNamespaceError","title":"InvalidNamespaceError","text":"<pre><code>InvalidNamespaceError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when the namespace is missing or not defined on the cluster.</p> <p>Often indicates a configuration mismatch between application and cluster.</p> <p>Attributes:</p> Name Type Description <code>result_code</code> <p>Typically <code>ResultCode.INVALID_NAMESPACE</code> when mapped from a server response.</p> <p>Example::     try:         await session.query(bad_ds).execute()     except InvalidNamespaceError:         ...  # namespace not configured on cluster</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.SecurityError","title":"SecurityError","text":"<pre><code>SecurityError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Base class for authentication, authorization, and security policy errors.</p> <p>Several distinct server result codes collapse to this type when they do not warrant a dedicated subclass. Catch :class:<code>AuthenticationError</code> or :class:<code>AuthorizationError</code> first if you need finer granularity.</p> <p>Attributes:</p> Name Type Description <code>result_code</code> <p>The security-related code returned by the server, when applicable.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.AuthenticationError","title":"AuthenticationError","text":"<pre><code>AuthenticationError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>SecurityError</code></p> <p>Raised when credentials are rejected or the session is not authenticated.</p> <p>Examples include invalid user, expired password, or not authenticated responses from the server.</p> See Also <p>:class:<code>AuthorizationError</code>: Valid identity but disallowed operation.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.AuthorizationError","title":"AuthorizationError","text":"<pre><code>AuthorizationError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>SecurityError</code></p> <p>Raised when the authenticated principal may not perform the operation.</p> <p>Distinct from :class:<code>AuthenticationError</code>, which indicates identity or credential problems rather than policy denial.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.GenerationError","title":"GenerationError","text":"<pre><code>GenerationError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when a write fails due to a record generation mismatch.</p> <p>The record was modified since it was read, or the expected generation did not match. Retrying blindly usually requires re-reading the record and reapplying the logical update.</p> <p>Attributes:</p> Name Type Description <code>result_code</code> <p>Typically <code>ResultCode.GENERATION_ERROR</code>.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.session.Session.upsert</code>: Common write path     that can enforce generations on builders.</p> <p>Example::     try:         await (             session.upsert(key)                 .put({\"x\": 1})                 .ensure_generation_is(3)                 .execute()         )     except GenerationError:         ...  # record was modified by another writer</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.QuotaError","title":"QuotaError","text":"<pre><code>QuotaError(message='', *, result_code=None, in_doubt=False)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when a server-side quota or limit is exceeded.</p> <p>Handling is usually operational (throttle, increase limits, or partition workload) rather than a single-record retry.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.SerializationError","title":"SerializationError","text":"<pre><code>SerializationError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when a value cannot be encoded for the wire or decoded from it.</p> <p>Check bin types and application serializers when this appears on puts or reads.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.QueryTerminatedError","title":"QueryTerminatedError","text":"<pre><code>QueryTerminatedError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when a query stops early (aborted, canceled, or server-terminated).</p> <p>Partial rows may already have been delivered on streaming paths; this error represents the overall query outcome, not a single-key failure inside a batch.</p> <p>Attributes:</p> Name Type Description <code>result_code</code> <p>May include <code>ResultCode.QUERY_ABORTED</code> or related codes.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.BackoffError","title":"BackoffError","text":"<pre><code>BackoffError(\n    message=\"\", *, result_code=None, in_doubt=False\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when the server signals rate limiting or requires backoff.</p> <p>Callers may retry after a delay or reduce request pressure.</p>"},{"location":"api/exceptions/#aerospike_fluent.exceptions.CommitError","title":"CommitError","text":"<pre><code>CommitError(\n    message=\"\",\n    *,\n    commit_error_type=None,\n    verify_records=None,\n    roll_records=None,\n    result_code=None,\n    in_doubt=False,\n)\n</code></pre> <p>               Bases: <code>AerospikeError</code></p> <p>Raised when a multi-record transaction commit does not complete successfully.</p> <p>Additional fields expose verify or roll-forward details when the underlying client provides them.</p> <p>Attributes:</p> Name Type Description <code>commit_error_type</code> <p>Implementation-defined label for the failure phase, if available.</p> <code>verify_records</code> <p>Verify-phase records or summaries, if available.</p> <code>roll_records</code> <p>Roll-forward or rollback-phase records, if available.</p> <code>result_code</code> <p>Server or client result associated with the commit, when set.</p> <code>in_doubt</code> <p>Inherited; <code>True</code> when commit outcome may be ambiguous on the server.</p>"},{"location":"api/exceptions/#dsl-exceptions","title":"DSL Exceptions","text":"<p>Exceptions raised by the expression DSL.</p>"},{"location":"api/exceptions/#aerospike_fluent.dsl.exceptions.DslParseException","title":"DslParseException","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a filter expression string cannot be parsed or validated.</p> <p>Indicates invalid syntax, unknown operators, type mismatches, or other input that the DSL layer refuses before building a server-side filter.</p> Example <p>Handling a user-supplied filter string::</p> <pre><code>from aerospike_fluent import parse_dsl, DslParseException\n\ntry:\n    expr = parse_dsl(user_filter)\nexcept DslParseException as e:\n    raise ValueError(str(e)) from e\n</code></pre> See Also <p>:func:<code>~aerospike_fluent.parse_dsl</code>: Primary entry point that may raise     this exception.</p>"},{"location":"api/exceptions/#aerospike_fluent.dsl.exceptions.NoApplicableFilterError","title":"NoApplicableFilterError","text":"<p>               Bases: <code>Exception</code></p> <p>Internal signal that no secondary-index filter can represent an expression.</p> <p>Used inside the DSL filter visitor when a valid parse tree still cannot be lowered to a secondary-index filter. Not part of the public stable API; callers working only with :func:<code>parse_dsl</code> should expect :class:<code>DslParseException</code> instead.</p>"},{"location":"api/exp/","title":"Exp \u2014 Expression Builder","text":"<p>Expression builder utilities.</p> <p>Re-exports FilterExpression as Exp and provides a convenience val() function for creating value expressions from Python values.</p>"},{"location":"api/exp/#aerospike_fluent.exp.val","title":"val","text":"<pre><code>val(value: bool) -&gt; FilterExpression\n</code></pre><pre><code>val(value: int) -&gt; FilterExpression\n</code></pre><pre><code>val(value: float) -&gt; FilterExpression\n</code></pre><pre><code>val(value: str) -&gt; FilterExpression\n</code></pre><pre><code>val(value: bytes) -&gt; FilterExpression\n</code></pre><pre><code>val(value: bytearray) -&gt; FilterExpression\n</code></pre><pre><code>val(value: List[Any]) -&gt; FilterExpression\n</code></pre><pre><code>val(value: Dict[Any, Any]) -&gt; FilterExpression\n</code></pre><pre><code>val(value: None) -&gt; FilterExpression\n</code></pre> <pre><code>val(value)\n</code></pre> <p>Create a value expression from a Python value.</p> <p>Automatically dispatches to the appropriate FilterExpression method based on the value's type.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>A Python value (bool, int, float, str, bytes, list, dict, or None)</p> required <p>Returns:</p> Type Description <code>FilterExpression</code> <p>A FilterExpression representing the value.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the value type is not supported.</p>"},{"location":"api/exp/#aerospike_fluent.exp.in_list","title":"in_list","text":"<pre><code>in_list(value, list_exp, ctx=None)\n</code></pre> <p>Check whether value exists in list_exp.</p> <p>Returns a boolean expression equivalent to <code>value IN list</code>.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>FilterExpression</code> <p>The value to search for.</p> required <code>list_exp</code> <code>FilterExpression</code> <p>A list expression (e.g. <code>Exp.list_bin(\"tags\")</code>).</p> required <code>ctx</code> <code>Optional[List[CTX]]</code> <p>Optional CDT context for nested lists.</p> <code>None</code> <p>Example::</p> <pre><code># bin \"role\" in list bin \"allowed_roles\"\nin_list(Exp.string_bin(\"role\"), Exp.list_bin(\"allowed_roles\"))\n</code></pre>"},{"location":"api/exp/#aerospike_fluent.exp.map_keys","title":"map_keys","text":"<pre><code>map_keys(map_exp, ctx=None)\n</code></pre> <p>Return all keys of map_exp as a list expression.</p> <p>Parameters:</p> Name Type Description Default <code>map_exp</code> <code>FilterExpression</code> <p>A map expression (e.g. <code>Exp.map_bin(\"scores\")</code>).</p> required <code>ctx</code> <code>Optional[List[CTX]]</code> <p>Optional CDT context for nested maps.</p> <code>None</code> <p>Example::</p> <pre><code>map_keys(Exp.map_bin(\"scores\"))\n</code></pre>"},{"location":"api/exp/#aerospike_fluent.exp.map_values","title":"map_values","text":"<pre><code>map_values(map_exp, ctx=None)\n</code></pre> <p>Return all values of map_exp as a list expression.</p> <p>Parameters:</p> Name Type Description Default <code>map_exp</code> <code>FilterExpression</code> <p>A map expression (e.g. <code>Exp.map_bin(\"scores\")</code>).</p> required <code>ctx</code> <code>Optional[List[CTX]]</code> <p>Optional CDT context for nested maps.</p> <code>None</code> <p>Example::</p> <pre><code>map_values(Exp.map_bin(\"scores\"))\n</code></pre>"},{"location":"api/index-builder/","title":"IndexBuilder","text":""},{"location":"api/index-builder/#aerospike_fluent.aio.operations.index.IndexBuilder","title":"IndexBuilder","text":"<pre><code>IndexBuilder(client, namespace, set_name)\n</code></pre> <p>Configure a secondary index, then :meth:<code>create</code> or :meth:<code>drop</code> it.</p> <p>Typical chain for a new index: :meth:<code>on_bin</code> \u2192 :meth:<code>named</code> \u2192 :meth:<code>numeric</code> or :meth:<code>string</code> \u2192 optional :meth:<code>collection</code> or :meth:<code>context</code> \u2192 <code>await</code> :meth:<code>create</code>.</p> <p>For removal, only :meth:<code>named</code> (and namespace/set from construction) is required before <code>await</code> :meth:<code>drop</code>.</p> Example <pre><code>await (\n    client.index(namespace=\"test\", set_name=\"users\")\n    .on_bin(\"email\")\n    .named(\"email_idx\")\n    .string()\n    .create()\n)\n</code></pre> See Also <p>:meth:<code>~aerospike_fluent.aio.client.FluentClient.index</code></p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>Client</code> <p>Connected async cluster client used for admin calls.</p> required <code>namespace</code> <code>str</code> <p>Namespace containing the set to index.</p> required <code>set_name</code> <code>str</code> <p>Set name within the namespace.</p> required"},{"location":"api/index-builder/#aerospike_fluent.aio.operations.index.IndexBuilder.on_bin","title":"on_bin","text":"<pre><code>on_bin(bin_name)\n</code></pre> <p>Set which bin this secondary index covers (required before :meth:<code>create</code>).</p> <p>Parameters:</p> Name Type Description Default <code>bin_name</code> <code>str</code> <p>Name of the bin to index.</p> required <p>Returns:</p> Type Description <code>IndexBuilder</code> <p><code>self</code> for method chaining.</p>"},{"location":"api/index-builder/#aerospike_fluent.aio.operations.index.IndexBuilder.named","title":"named","text":"<pre><code>named(index_name)\n</code></pre> <p>Set the secondary index name the cluster stores (required for create and drop).</p> <p>Parameters:</p> Name Type Description Default <code>index_name</code> <code>str</code> <p>Name passed to create/drop admin calls; must match when dropping.</p> required <p>Returns:</p> Type Description <code>IndexBuilder</code> <p><code>self</code> for method chaining.</p>"},{"location":"api/index-builder/#aerospike_fluent.aio.operations.index.IndexBuilder.numeric","title":"numeric","text":"<pre><code>numeric()\n</code></pre> <p>Set the secondary index type to numeric (for numeric bin values).</p> <p>Call this or :meth:<code>string</code> before :meth:<code>create</code>, matching how the bin is stored. If both are called on the same builder, the last call wins.</p> <p>Returns:</p> Type Description <code>IndexBuilder</code> <p><code>self</code> for method chaining.</p>"},{"location":"api/index-builder/#aerospike_fluent.aio.operations.index.IndexBuilder.string","title":"string","text":"<pre><code>string()\n</code></pre> <p>Set the secondary index type to string (for string bin values).</p> <p>Call this or :meth:<code>numeric</code> before :meth:<code>create</code>. If both are called, the last call wins (see :meth:<code>numeric</code>).</p> <p>Returns:</p> Type Description <code>IndexBuilder</code> <p><code>self</code> for method chaining.</p>"},{"location":"api/index-builder/#aerospike_fluent.aio.operations.index.IndexBuilder.collection","title":"collection","text":"<pre><code>collection(collection_index_type)\n</code></pre> <p>Set the collection index variant for map or list bins (optional).</p> <p>Use together with :meth:<code>numeric</code> or :meth:<code>string</code> when indexing into collection data types.</p> <p>Parameters:</p> Name Type Description Default <code>collection_index_type</code> <code>CollectionIndexType</code> <p><code>CollectionIndexType</code> constant from the <code>aerospike_async</code> package (map- vs list-style collection indexing).</p> required <p>Returns:</p> Type Description <code>IndexBuilder</code> <p><code>self</code> for method chaining.</p>"},{"location":"api/index-builder/#aerospike_fluent.aio.operations.index.IndexBuilder.context","title":"context","text":"<pre><code>context(ctx)\n</code></pre> <p>Set a CDT context path for indexing a nested list or map element.</p> <p>Parameters:</p> Name Type Description Default <code>ctx</code> <code>List[CTX]</code> <p>One or more <code>CTX</code> entries describing the path to the nested element (e.g., <code>[CTX.map_key(\"outer\"), CTX.list_index(0)]</code>).</p> required <p>Returns:</p> Type Description <code>IndexBuilder</code> <p><code>self</code> for method chaining.</p> <p>Example::     await (         client.index(\"test\", \"events\")         .on_bin(\"payload\")         .named(\"nested_ts_idx\")         .numeric()         .context([CTX.map_key(\"meta\"), CTX.map_key(\"timestamp\")])         .create()     )</p> See Also <p>:meth:<code>~aerospike_async.Filter.context</code>: Attach the same path when querying.</p>"},{"location":"api/index-builder/#aerospike_fluent.aio.operations.index.IndexBuilder.create","title":"create  <code>async</code>","text":"<pre><code>create()\n</code></pre> <p>Create the index on the cluster.</p> <p>Example::     await (         client.index(namespace=\"test\", set_name=\"users\")         .on_bin(\"email\")         .named(\"email_idx\")         .string()         .create()     )</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>on_bin</code>, <code>named</code>, or index type was not set.</p> <code>AerospikeError</code> <p>On server or transport failure (typed subclass when the driver maps a result code).</p> See Also <p>:meth:<code>drop</code></p>"},{"location":"api/index-builder/#aerospike_fluent.aio.operations.index.IndexBuilder.drop","title":"drop  <code>async</code>","text":"<pre><code>drop()\n</code></pre> <p>Drop a previously created index by name.</p> <p>Example::     await (         client.index(namespace=\"test\", set_name=\"users\")         .named(\"email_idx\")         .drop()     )</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If :meth:<code>named</code> was not called.</p> <code>AerospikeError</code> <p>On server or transport failure.</p> Note <p>Namespace and set come from the builder constructor, not from :meth:<code>on_bin</code>.</p>"},{"location":"api/indexes-monitor/","title":"IndexesMonitor","text":""},{"location":"api/indexes-monitor/#aerospike_fluent.index_monitor.IndexesMonitor","title":"IndexesMonitor","text":"<pre><code>IndexesMonitor(refresh_interval=_DEFAULT_REFRESH_INTERVAL)\n</code></pre> <p>Async background task that caches secondary index metadata.</p> <p>Start via :meth:<code>start</code> (typically called by <code>FluentClient.connect</code>). Retrieve cached data with :meth:<code>get_index_context</code>. Stop via :meth:<code>stop</code> (called by <code>FluentClient.close</code>).</p> <p>Example::</p> <pre><code>monitor = IndexesMonitor()\nawait monitor.start(client)\nctx = monitor.get_index_context(\"test\")\nawait monitor.stop()\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>refresh_interval</code> <code>float</code> <p>Seconds between cache refreshes (default 5.0).</p> <code>_DEFAULT_REFRESH_INTERVAL</code>"},{"location":"api/indexes-monitor/#aerospike_fluent.index_monitor.IndexesMonitor.start","title":"start  <code>async</code>","text":"<pre><code>start(client)\n</code></pre> <p>Begin background refresh.  Waits up to 1 s for the first fetch.</p>"},{"location":"api/indexes-monitor/#aerospike_fluent.index_monitor.IndexesMonitor.stop","title":"stop  <code>async</code>","text":"<pre><code>stop()\n</code></pre> <p>Cancel the background refresh task.</p>"},{"location":"api/indexes-monitor/#aerospike_fluent.index_monitor.IndexesMonitor.get_index_context","title":"get_index_context","text":"<pre><code>get_index_context(namespace)\n</code></pre> <p>Return the cached <code>IndexContext</code> for namespace, or <code>None</code>.</p>"},{"location":"api/info/","title":"InfoCommands","text":""},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands","title":"InfoCommands","text":"<pre><code>InfoCommands(session)\n</code></pre> <p>Provides high-level methods to execute common Aerospike info commands.</p> <p>This class encapsulates the most commonly used Aerospike info commands and provides a convenient API for retrieving cluster information.</p> Example <pre><code>info = session.info()\n\n# Get all namespaces\nnamespaces = await info.namespaces()\n\n# Get namespace details\nns_detail = await info.namespace_details(\"test\")\n\n# Get all secondary indexes\nindexes = await info.secondary_indexes()\n</code></pre> <p>Initialize InfoCommands.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>'Session'</code> <p>The Session to use for info commands.</p> required"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.build","title":"build  <code>async</code>","text":"<pre><code>build()\n</code></pre> <p>Get the build information from all nodes in the cluster.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>A set of build strings from all nodes.</p>"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.namespaces","title":"namespaces  <code>async</code>","text":"<pre><code>namespaces()\n</code></pre> <p>Get the list of namespaces from all nodes in the cluster.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>A set of namespace names from all nodes.</p>"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.namespace_details","title":"namespace_details  <code>async</code>","text":"<pre><code>namespace_details(namespace)\n</code></pre> <p>Get detailed information about a specific namespace.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The name of the namespace.</p> required <p>Returns:</p> Type Description <code>Optional[Dict[str, str]]</code> <p>A dictionary containing namespace details, or None if not found.</p>"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.sets","title":"sets  <code>async</code>","text":"<pre><code>sets(namespace)\n</code></pre> <p>Get the list of sets in a specific namespace.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The name of the namespace.</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>A list of set names in the namespace.</p>"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.secondary_indexes","title":"secondary_indexes  <code>async</code>","text":"<pre><code>secondary_indexes(namespace=None)\n</code></pre> <p>Get information about all secondary indexes.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>Optional[str]</code> <p>Optional namespace filter. If provided, only returns       indexes for that namespace.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Dict[str, str]]</code> <p>A list of dictionaries containing secondary index information.</p>"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.secondary_index_details","title":"secondary_index_details  <code>async</code>","text":"<pre><code>secondary_index_details(namespace, index_name)\n</code></pre> <p>Get detailed information about a specific secondary index.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The namespace containing the index.</p> required <code>index_name</code> <code>str</code> <p>The name of the index.</p> required <p>Returns:</p> Type Description <code>Optional[Dict[str, str]]</code> <p>A dictionary containing index details, or None if not found.</p>"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.is_cluster_stable","title":"is_cluster_stable  <code>async</code>","text":"<pre><code>is_cluster_stable()\n</code></pre> <p>Check if all nodes agree on the current cluster state.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the cluster is stable, False otherwise.</p>"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.get_cluster_size","title":"get_cluster_size  <code>async</code>","text":"<pre><code>get_cluster_size()\n</code></pre> <p>Get the number of nodes in the cluster.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of nodes in the cluster.</p>"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.info","title":"info  <code>async</code>","text":"<pre><code>info(command)\n</code></pre> <p>Execute a raw info command against the cluster.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>str</code> <p>The info command to execute (e.g., \"statistics\", \"build\").</p> required <p>Returns:</p> Type Description <code>Dict[str, str]</code> <p>A dictionary containing the info command response as key-value pairs.</p>"},{"location":"api/info/#aerospike_fluent.aio.info.InfoCommands.info_on_all_nodes","title":"info_on_all_nodes  <code>async</code>","text":"<pre><code>info_on_all_nodes(command)\n</code></pre> <p>Execute a raw info command against all nodes in the cluster.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>str</code> <p>The info command to execute (e.g., \"statistics\", \"build\").</p> required <p>Returns:</p> Type Description <code>Dict[str, Dict[str, str]]</code> <p>A dictionary mapping node names to their response dictionaries.</p>"},{"location":"api/query-hint/","title":"QueryHint","text":""},{"location":"api/query-hint/#aerospike_fluent.aio.operations.query.QueryHint","title":"QueryHint  <code>dataclass</code>","text":"<pre><code>QueryHint(\n    index_name=None, bin_name=None, query_duration=None\n)\n</code></pre> <p>Hint for influencing secondary index selection and query scheduling.</p> <p>Provide <code>index_name</code> to force a specific named secondary index, or <code>bin_name</code> to redirect the filter to a different bin's index.  These two are mutually exclusive.  <code>query_duration</code> overrides the policy's <code>expected_duration</code> for this query only.</p> Example <p>hint = QueryHint(index_name=\"age_idx\", ...                  query_duration=QueryDuration.SHORT) stream = await ( ...     session.query(dataset) ...         .filter(Filter.equal(\"age\", 30)) ...         .with_hint(hint) ...         .execute() ... )</p> <p>Parameters:</p> Name Type Description Default <code>index_name</code> <code>Optional[str]</code> <p>Force the query to use the named secondary index.</p> <code>None</code> <code>bin_name</code> <code>Optional[str]</code> <p>Redirect the filter to use a different bin's index.</p> <code>None</code> <code>query_duration</code> <code>Optional[QueryDuration]</code> <p>Override <code>expected_duration</code> on the query policy.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If both <code>index_name</code> and <code>bin_name</code> are provided.</p> See Also <p>:meth:<code>QueryBuilder.with_hint</code></p>"},{"location":"api/query/","title":"Query Builders","text":""},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder","title":"QueryBuilder","text":"<pre><code>QueryBuilder(\n    client,\n    namespace,\n    set_name,\n    behavior=None,\n    indexes_monitor=None,\n)\n</code></pre> <p>               Bases: <code>_WriteVerbs</code></p> <p>Chain reads, writes, UDF calls, filters, and policies before <code>execute</code>.</p> <p>Start from :meth:<code>~aerospike_fluent.aio.session.Session.query</code> or :meth:<code>~aerospike_fluent.aio.client.FluentClient.query</code>. Use :meth:<code>where</code> or :meth:<code>filter_expression</code> for server-side predicates, :meth:<code>bins</code> or :meth:<code>bin</code> for projections, and transition methods such as :meth:<code>upsert</code> for writes. Await :meth:<code>execute</code> for a :class:<code>~aerospike_fluent.record_stream.RecordStream</code>.</p> Example <p>Set-wide read with filter and projection::</p> <pre><code>stream = await (\n    session.query(users)\n        .where(\"$.status == 'active'\")\n        .bins([\"user_id\", \"name\"])\n        .execute()\n)\nasync for row in stream:\n    if row.is_ok and row.record:\n        print(row.record.bins)\n</code></pre> <p>Point read on a key, then chain an upsert::</p> <pre><code>stream = await (\n    session.query(users.id(\"u1\"))\n        .bins([\"name\"])\n        .upsert(users.id(\"u1\"))\n        .put({\"last_seen\": 123})\n        .execute()\n)\n</code></pre> See Also <p>:class:<code>WriteSegmentBuilder</code>: Bin writes after a write verb. :class:<code>QueryBinBuilder</code>: Per-bin read operations.</p> <p>Initialize a QueryBuilder.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>Client</code> <p>The underlying async client.</p> required <code>namespace</code> <code>str</code> <p>The namespace name.</p> required <code>set_name</code> <code>str</code> <p>The set name.</p> required <code>behavior</code> <code>Optional[Behavior]</code> <p>Optional Behavior for deriving policies.</p> <code>None</code> <code>indexes_monitor</code> <code>Optional['IndexesMonitor']</code> <p>Optional monitor providing cached index metadata for transparent filter generation from DSL expressions.</p> <code>None</code>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.bins","title":"bins","text":"<pre><code>bins(bin_names)\n</code></pre> <p>Restrict the read to a non-empty set of bin names.</p> <p>Mutually exclusive with :meth:<code>with_no_bins</code>.</p> <p>Parameters:</p> Name Type Description Default <code>bin_names</code> <code>List[str]</code> <p>Non-empty list of bin names to return.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for method chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>bin_names</code> is empty or :meth:<code>with_no_bins</code> was already called.</p> Example <p>Restrict a query or key read to specific bins::</p> <pre><code>stream = await session.query(users.id(1)).bins([\"name\", \"email\"]).execute()\n</code></pre> See Also <p>:meth:<code>with_no_bins</code>: Metadata-only reads without bin payloads. :meth:<code>bin</code>: Per-bin operations (CDT, expressions).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.bin","title":"bin","text":"<pre><code>bin(bin_name)\n</code></pre> <p>Start a bin-level read operation.</p> <p>Returns a :class:<code>QueryBinBuilder</code> for specifying how to read from the named bin (simple get, CDT navigation, or expression read).</p> <p>Parameters:</p> Name Type Description Default <code>bin_name</code> <code>str</code> <p>The bin to operate on.</p> required <p>Returns:</p> Type Description <code>QueryBinBuilder[QueryBuilder]</code> <p>A QueryBinBuilder for method chaining.</p> <p>Example::</p> <pre><code>rs = await session.query(users.id(1)) \\\n    .bin(\"settings\").on_map_key(\"theme\").get_values() \\\n    .bin(\"age\").get() \\\n    .execute()\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.add_operation","title":"add_operation","text":"<pre><code>add_operation(op)\n</code></pre> <p>Append a read operation produced by a bin or CDT builder.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.with_write_operations","title":"with_write_operations","text":"<pre><code>with_write_operations(operations)\n</code></pre> <p>Attach scalar write operations for a background dataset task.</p> <p>Prefer :meth:<code>aerospike_fluent.aio.session.Session.background_task</code> for fluent bin writes. Use with :meth:<code>execute_background_task</code> on a dataset query (no keys). Only <code>Operation</code> and <code>ExpOperation.write</code>-style writes are valid; list, map, bit, and HLL operations are rejected before calling the client.</p> <p>Parameters:</p> Name Type Description Default <code>operations</code> <code>Sequence[Any]</code> <p>Sequence of write operations (e.g. <code>Operation.put</code>, <code>Operation.touch</code>).</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.with_no_bins","title":"with_no_bins","text":"<pre><code>with_no_bins()\n</code></pre> <p>Specify that no bins should be read (header-only query).</p> <p>This method is useful when you only need to check for record existence or get metadata like generation numbers, without reading the actual data.</p> <p>This method cannot be used together with bins().</p> <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If used together with bins().</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.filter","title":"filter","text":"<pre><code>filter(filter_obj)\n</code></pre> <p>Add a secondary index filter to the query.</p> <p>Parameters:</p> Name Type Description Default <code>filter_obj</code> <code>Filter</code> <p>The filter to add.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for method chaining.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.filter_expression","title":"filter_expression","text":"<pre><code>filter_expression(expression)\n</code></pre> <p>Set a FilterExpression for server-side filtering.</p> <p>FilterExpression allows complex server-side filtering that doesn't require secondary indexes. This is more efficient than client-side filtering as it reduces network traffic and processing.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>FilterExpression</code> <p>The FilterExpression to apply.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> Example"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.filter_expression--filter-by-multiple-conditions-server-side","title":"Filter by multiple conditions server-side","text":"<p>filter_exp = FilterExpression.and_([     FilterExpression.eq(         FilterExpression.string_bin(\"category\"),         FilterExpression.string_val(\"Shoes\")     ),     FilterExpression.eq(         FilterExpression.string_bin(\"usage\"),         FilterExpression.string_val(\"Sports\")     ) ]) recordset = await client.query(\"test\", \"products\").filter_expression(filter_exp).execute()</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.where","title":"where","text":"<pre><code>where(expression: str) -&gt; QueryBuilder\n</code></pre><pre><code>where(expression: FilterExpression) -&gt; QueryBuilder\n</code></pre> <pre><code>where(expression)\n</code></pre> <p>Apply a server-side filter for dataset queries or keyed reads that support it.</p> <p>String arguments are parsed with the fluent DSL; prefer f-strings for dynamic literals. Pass a pre-built :class:<code>~aerospike_async.FilterExpression</code> when constructing filters programmatically.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or <code>FilterExpression</code>.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for chaining.</p> Example <p>qb = session.query(ds).where(\"$.status == 'active'\") qb = session.query(ds).where(f\"$.score &gt; {min_score}\")</p> See Also <p>:meth:<code>default_where</code>: Default filter for chained operations without their own. :meth:<code>filter_expression</code>: Attach an expression without DSL parsing.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.with_index_context","title":"with_index_context","text":"<pre><code>with_index_context(index_context)\n</code></pre> <p>Explicitly override the secondary index metadata used for filter generation.</p> <p>Most applications do not need this method. The client automatically discovers and caches secondary index metadata from the cluster in the background. Use this only when you need to force a specific index context that differs from the live cluster state.</p> <p>Parameters:</p> Name Type Description Default <code>index_context</code> <code>'IndexContext'</code> <p>Index metadata for the query's namespace.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for method chaining.</p> See Also <p>:class:<code>~aerospike_fluent.dsl.filter_gen.IndexContext</code></p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.with_policy","title":"with_policy","text":"<pre><code>with_policy(policy)\n</code></pre> <p>Set the query policy.</p> <p>Parameters:</p> Name Type Description Default <code>policy</code> <code>QueryPolicy</code> <p>The query policy to use.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.with_read_policy","title":"with_read_policy","text":"<pre><code>with_read_policy(policy)\n</code></pre> <p>Set the read policy (for single key or batch key queries).</p> <p>Parameters:</p> Name Type Description Default <code>policy</code> <code>ReadPolicy</code> <p>The read policy to use.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.partition","title":"partition","text":"<pre><code>partition(partition_filter)\n</code></pre> <p>Restrict a dataset query using a PAC :class:<code>~aerospike_async.PartitionFilter</code>.</p> <p>Prefer :meth:<code>on_partition</code> or :meth:<code>on_partition_range</code> for common cases.</p> <p>Parameters:</p> Name Type Description Default <code>partition_filter</code> <code>PartitionFilter</code> <p>Built filter (all partitions, by id, by range, etc.).</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for chaining.</p> See Also <p>:meth:<code>on_partition_range</code>: Inclusive start, exclusive end partition ids.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.on_partitions","title":"on_partitions","text":"<pre><code>on_partitions(*partition_ids)\n</code></pre> <p>Set partitions to query by partition IDs.</p> <p>Parameters:</p> Name Type Description Default <code>*partition_ids</code> <code>int</code> <p>One or more partition IDs to query.</p> <code>()</code> <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> Example <pre><code>query = session.query(dataset).on_partitions(1, 2, 3)\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.on_partition","title":"on_partition","text":"<pre><code>on_partition(part_id)\n</code></pre> <p>Target a specific partition for the query.</p> <p>This method restricts the query to a single partition. This can be useful for load balancing or when you know the data distribution across partitions.</p> <p>Parameters:</p> Name Type Description Default <code>part_id</code> <code>int</code> <p>The partition ID to target (0-4095)</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If part_id is out of range</p> Example <pre><code>query = session.query(dataset).on_partition(5)\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.on_partition_range","title":"on_partition_range","text":"<pre><code>on_partition_range(start_incl, end_excl)\n</code></pre> <p>Target a range of partitions for the query.</p> <p>This method restricts the query to a specific range of partitions. This can be useful for load balancing, parallel processing, or when you know the data distribution across partitions.</p> <p>The partition range can only be set once per query. Subsequent calls with different ranges will overwrite the previous range.</p> <p>Parameters:</p> Name Type Description Default <code>start_incl</code> <code>int</code> <p>Start partition (inclusive, 0-4095)</p> required <code>end_excl</code> <code>int</code> <p>End partition (exclusive, 1-4096)</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If partition range is invalid</p> Example <pre><code># Query partitions 0-2047 (first half)\nquery = session.query(dataset).on_partition_range(0, 2048)\n\n# Query partitions 100-199\nquery = session.query(dataset).on_partition_range(100, 200)\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.chunk_size","title":"chunk_size","text":"<pre><code>chunk_size(chunk_size)\n</code></pre> <p>Tune server-side streaming chunk size (maps to query policy <code>max_records</code> chunking).</p> <pre><code>    This method controls how many records are fetched per chunk from the server\n</code></pre> <p>when using server-side streaming. The chunk size affects memory usage and network round trips (Larger values reduce round trips; smaller values bound memory per fetch).         This is distinct from client-side pagination.</p> <p>Parameters:</p> Name Type Description Default <code>chunk_size</code> <code>int</code> <p>Records per chunk; must be positive.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>chunk_size &lt;= 0</code>.</p> Example <pre><code>query = session.query(dataset).chunk_size(100)\n</code></pre> <p>See Also:     :meth:<code>max_records</code>: Cap total records returned.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.records_per_second","title":"records_per_second","text":"<pre><code>records_per_second(rps)\n</code></pre> <p>Set the maximum records per second for the query.</p> <p>Parameters:</p> Name Type Description Default <code>rps</code> <code>int</code> <p>Maximum records per second to process.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> Example <pre><code>query = session.query(dataset).records_per_second(1000)\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.max_records","title":"max_records","text":"<pre><code>max_records(max_records)\n</code></pre> <p>Set the maximum number of records to return.</p> <p>Parameters:</p> Name Type Description Default <code>max_records</code> <code>int</code> <p>Maximum number of records to return.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> Example <pre><code>query = session.query(dataset).max_records(10000)\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.limit","title":"limit","text":"<pre><code>limit(limit)\n</code></pre> <p>Set the maximum number of records to return (alias for max_records).</p> <p>This method is an alias for max_records(). It limits the total number of records returned by the query. Once the limit is reached, the query will stop processing.</p> <p>Parameters:</p> Name Type Description Default <code>limit</code> <code>int</code> <p>Maximum number of records to return (must be &gt; 0).</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If limit is &lt;= 0.</p> Example <pre><code>query = session.query(dataset).limit(100)\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.expected_duration","title":"expected_duration","text":"<pre><code>expected_duration(duration)\n</code></pre> <p>Set the expected duration of the query.</p> <p>Parameters:</p> Name Type Description Default <code>duration</code> <code>'QueryDuration'</code> <p>Expected duration (QueryDuration.LONG, QueryDuration.SHORT, or QueryDuration.LONG_RELAX_AP).</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> Example <pre><code>from aerospike_async import QueryDuration\nquery = session.query(dataset).expected_duration(QueryDuration.SHORT)\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.with_hint","title":"with_hint","text":"<pre><code>with_hint(hint)\n</code></pre> <p>Attach a query hint for secondary index selection or scheduling.</p> <p>A hint can redirect which secondary index is used (<code>index_name</code>), remap the filter to a different bin (<code>bin_name</code>), or override the expected query duration (<code>query_duration</code>).  Only one call to <code>with_hint</code> is allowed per builder.</p> Example <p>stream = await ( ...     session.query(dataset) ...         .filter(Filter.equal(\"age\", 30)) ...         .with_hint(QueryHint(index_name=\"age_idx\")) ...         .execute() ... )</p> <p>Parameters:</p> Name Type Description Default <code>hint</code> <code>QueryHint</code> <p>A :class:<code>QueryHint</code> instance.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for method chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>with_hint</code> has already been called on this builder.</p> See Also <p>:class:<code>QueryHint</code></p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.replica","title":"replica","text":"<pre><code>replica(replica)\n</code></pre> <p>Set the replica preference for the query.</p> <p>Parameters:</p> Name Type Description Default <code>replica</code> <code>'Replica'</code> <p>Replica preference (Replica.MASTER, Replica.SEQUENCE, or Replica.PREFER_RACK).</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> Example <pre><code>from aerospike_async import Replica\nquery = session.query(dataset).replica(Replica.SEQUENCE)\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.base_policy","title":"base_policy","text":"<pre><code>base_policy(base_policy)\n</code></pre> <p>Set the base policy for the query.</p> <p>Parameters:</p> Name Type Description Default <code>base_policy</code> <code>'BasePolicy'</code> <p>The base policy to use.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> Example <pre><code>from aerospike_async import BasePolicy\nbase = BasePolicy()\nquery = session.query(dataset).base_policy(base)\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.fail_on_filtered_out","title":"fail_on_filtered_out","text":"<pre><code>fail_on_filtered_out()\n</code></pre> <p>Surface rows that fail a filter as <code>FILTERED_OUT</code> instead of omitting them.</p> <p>Applies to key-based reads where a filter excludes the record. Without this flag, filtered keys may be absent from the stream depending on policy.</p> <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for chaining.</p> See Also <p>:meth:<code>respond_all_keys</code>: Include missing-key rows in batch reads.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>Ensure batch/point reads emit one row per requested key, including not-found.</p> <p>Missing keys appear as non-OK :class:<code>~aerospike_fluent.record_result.RecordResult</code> entries (typically <code>KEY_NOT_FOUND</code>) instead of being skipped.</p> <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for chaining.</p> See Also <p>:meth:<code>fail_on_filtered_out</code>: Filter mismatch vs missing key.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.default_where","title":"default_where","text":"<pre><code>default_where(expression: str) -&gt; QueryBuilder\n</code></pre><pre><code>default_where(expression: FilterExpression) -&gt; QueryBuilder\n</code></pre> <pre><code>default_where(expression)\n</code></pre> <p>Set a filter applied to any chained operation that does not call :meth:<code>where</code>.</p> <p>When a chain contains multiple operations (reads, writes, UDFs), each operation inherits this filter unless it supplies its own :meth:<code>where</code>.</p> <p>Example::</p> <pre><code>stream = await (\n    session.upsert(k1)\n        .bin(\"status\").set_to(\"active\")\n        .where(f\"$.age &gt;= {min_age}\")\n    .delete(k2, k3)\n    .upsert(k4)\n        .bin(\"flag\").set_to(True)\n    .default_where(\"$.active == true\")\n    .execute()\n)\n# upsert(k1) keeps its own where(); the delete and\n# second upsert inherit default_where.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or <code>FilterExpression</code>.</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>This builder for chaining.</p> See Also <p>:meth:<code>where</code>: Per-operation filter on the current operation.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.default_expire_record_after_seconds","title":"default_expire_record_after_seconds","text":"<pre><code>default_expire_record_after_seconds(seconds)\n</code></pre> <p>Set a default TTL applied to chained operations that lack their own.</p> <p>Parameters:</p> Name Type Description Default <code>seconds</code> <code>int</code> <p>Time-to-live in seconds (must be &gt; 0).</p> required <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>self for method chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If seconds is &lt;= 0.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.default_never_expire","title":"default_never_expire","text":"<pre><code>default_never_expire()\n</code></pre> <p>Set the default TTL to never expire (TTL = -1).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.default_with_no_change_in_expiration","title":"default_with_no_change_in_expiration","text":"<pre><code>default_with_no_change_in_expiration()\n</code></pre> <p>Set the default to preserve each record's existing TTL (TTL = -2).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.default_expiry_from_server_default","title":"default_expiry_from_server_default","text":"<pre><code>default_expiry_from_server_default()\n</code></pre> <p>Set the default TTL to the namespace's server default (TTL = 0).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.query","title":"query","text":"<pre><code>query(arg1, *more_keys)\n</code></pre> <p>Chain another query with new key(s) for batch/point stacking.</p> <p>Finalizes the current query segment and begins a new one with the given key(s).  Each segment can have its own bins, operations, and filter expression.  Dataset (index) queries cannot be stacked.</p> <p>Parameters:</p> Name Type Description Default <code>arg1</code> <code>Union[Key, List[Key]]</code> <p>A single :class:<code>Key</code> or a <code>List[Key]</code>.</p> required <code>*more_keys</code> <code>Key</code> <p>Additional keys (varargs).</p> <code>()</code> <p>Returns:</p> Type Description <code>QueryBuilder</code> <p><code>self</code> for method chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the current query is a dataset query (no keys).</p> <p>Example::</p> <pre><code>rs = await session.query(users.ids(1, 2, 3)) \\\n    .bin(\"map\").get() \\\n    .query(users.ids(4, 5, 6)) \\\n    .bin(\"name\").get() \\\n    .execute()\n</code></pre>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.execute","title":"execute  <code>async</code>","text":"<pre><code>execute(on_error=None)\n</code></pre> <p>Execute the query and return a :class:<code>RecordStream</code>.</p> <p>Handles single-key, batch-key, and dataset queries.  When a chain contains multiple operations, each operation is executed and results are combined into a single stream.</p> <p>Parameters:</p> Name Type Description Default <code>on_error</code> <code>OnError | None</code> <p>Controls how per-record errors are surfaced.</p> <ul> <li><code>None</code> (default): single-key operations raise on error;   batch / multi-key operations embed errors in the stream.</li> <li><code>ErrorStrategy.IN_STREAM</code>: always embed errors in the   stream as <code>RecordResult</code> entries.</li> <li>A callable <code>(key, index, exception) -&gt; None</code>: errors are   dispatched to the callback and excluded from the stream.</li> </ul> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>RecordStream</code> <p>class:<code>~aerospike_fluent.record_stream.RecordStream</code> of</p> <code>RecordStream</code> <p>class:<code>~aerospike_fluent.record_result.RecordResult</code> rows.</p> <p>Raises:</p> Type Description <code>AerospikeError</code> <p>If the builder mixes dataset query with per-bin read operations (unsupported combination).</p> <code>AerospikeError</code> <p>Typed subclasses for timeouts, connection failures, etc. when the client raises instead of embedding errors.</p> Example <p>Single-key read with default error handling::</p> <pre><code>stream = await session.query(key).bins([\"x\"]).execute()\nrow = await stream.first_or_raise()\n</code></pre> <p>Multi-key read, keep errors in the stream::</p> <pre><code>stream = await (\n    session.query(k1, k2, k3)\n        .execute(on_error=ErrorStrategy.IN_STREAM)\n)\nrows = await stream.collect()\n</code></pre> See Also <p>:class:<code>~aerospike_fluent.error_strategy.ErrorStrategy</code>: <code>on_error</code> options.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.execute_background_task","title":"execute_background_task  <code>async</code>","text":"<pre><code>execute_background_task()\n</code></pre> <p>Run a background write against all records matching this dataset query.</p> <p>Returns a server task handle; poll with <code>wait_till_complete</code> or <code>query_status</code>. Requires :meth:<code>with_write_operations</code>; only scalar <code>Operation</code> / expression writes are allowed.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the builder targets keys or has no write operations.</p> <code>AerospikeError</code> <p>If unsupported operation types are present.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBuilder.execute_udf_background_task","title":"execute_udf_background_task  <code>async</code>","text":"<pre><code>execute_udf_background_task(\n    package_name, function_name, args=None\n)\n</code></pre> <p>Apply a registered UDF to matching records as a background task.</p> <p>Do not use :meth:<code>with_write_operations</code> on the same builder.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the builder targets keys or has write operations set.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder","title":"WriteBinBuilder","text":"<pre><code>WriteBinBuilder(segment, bin_name)\n</code></pre> <p>               Bases: <code>_WriteVerbs</code></p> <p>Per-bin write builder inside a :class:<code>WriteSegmentBuilder</code>.</p> <p>Start with :meth:<code>WriteSegmentBuilder.bin</code>. Scalar methods delegate to the segment; <code>map_*</code> and <code>list_*</code> append collection operations; nested CDT builders capture context for maps and lists. Write verbs on this class finalize the segment and start a new one on new keys.</p> Example <p>Set a map key and append to a list within the same write::</p> <pre><code>await (\n    session.upsert(key)\n        .bin(\"config\").on_map_key(\"level\").set_to(5)\n        .bin(\"tags\").list_append(value=\"new_tag\")\n        .execute()\n)\n</code></pre> See Also <p>:class:<code>QueryBinBuilder</code>: Read-side analogue for queries.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.set_to","title":"set_to","text":"<pre><code>set_to(value)\n</code></pre> <p>Set the bin to value (<code>Operation.put</code>).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.add","title":"add","text":"<pre><code>add(value)\n</code></pre> <p>Add a numeric value to the bin (<code>Operation.add</code>).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.increment_by","title":"increment_by","text":"<pre><code>increment_by(value)\n</code></pre> <p>Alias of :meth:<code>add</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.append","title":"append","text":"<pre><code>append(value)\n</code></pre> <p>String append (<code>Operation.append</code>).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.prepend","title":"prepend","text":"<pre><code>prepend(value)\n</code></pre> <p>String prepend (<code>Operation.prepend</code>).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.remove","title":"remove","text":"<pre><code>remove()\n</code></pre> <p>Drop the bin (write <code>None</code>).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.get","title":"get","text":"<pre><code>get()\n</code></pre> <p>Return the bin value after writes complete (<code>Operation.get_bin</code>).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.list_add","title":"list_add","text":"<pre><code>list_add(value)\n</code></pre> <p>Add value to an ordered list (sorted insert).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Element to insert in sorted order.</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.list_append","title":"list_append","text":"<pre><code>list_append(value)\n</code></pre> <p>Append value to the end of an unordered list.</p> <p>Example::     .bin(\"tags\").list_append(value=\"python\")</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.map_clear","title":"map_clear","text":"<pre><code>map_clear()\n</code></pre> <p>Remove all entries from the map bin.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.map_size","title":"map_size","text":"<pre><code>map_size()\n</code></pre> <p>Return the map element count (read within operate).</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.map_upsert_items","title":"map_upsert_items","text":"<pre><code>map_upsert_items(items)\n</code></pre> <p>Put multiple map entries (create or update each key).</p> <p>Example::     .bin(\"settings\").map_upsert_items({\"theme\": \"dark\", \"lang\": \"en\"})</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.map_insert_items","title":"map_insert_items","text":"<pre><code>map_insert_items(items)\n</code></pre> <p>Put map entries only for keys that do not yet exist.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.map_update_items","title":"map_update_items","text":"<pre><code>map_update_items(items)\n</code></pre> <p>Update existing map entries only (no new keys).</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Key-value pairs to update for existing keys only.</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.map_create","title":"map_create","text":"<pre><code>map_create(order)\n</code></pre> <p>Create an empty map with the given key order.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>MapOrder</code> <p>Map key sort order.</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.map_set_policy","title":"map_set_policy","text":"<pre><code>map_set_policy(order)\n</code></pre> <p>Set map sort order policy without changing entries.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>MapOrder</code> <p>Map key sort order policy.</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.list_clear","title":"list_clear","text":"<pre><code>list_clear()\n</code></pre> <p>Remove all elements from the list bin.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.list_sort","title":"list_sort","text":"<pre><code>list_sort(flags=DEFAULT)\n</code></pre> <p>Sort the list bin.</p> <p>Parameters:</p> Name Type Description Default <code>flags</code> <code>ListSortFlags</code> <p>Sort behavior flags.</p> <code>DEFAULT</code> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.list_size","title":"list_size","text":"<pre><code>list_size()\n</code></pre> <p>Return the list element count (read within operate).</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.list_append_items","title":"list_append_items","text":"<pre><code>list_append_items(items)\n</code></pre> <p>Append values to an unordered list.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.list_add_items","title":"list_add_items","text":"<pre><code>list_add_items(items)\n</code></pre> <p>Insert values into an ordered list (sorted positions).</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Sequence of values to insert in sorted order.</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.list_create","title":"list_create","text":"<pre><code>list_create(order, *, pad=False, persist_index=False)\n</code></pre> <p>Create an empty list with the given order.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>ListOrderType</code> <p>List element order.</p> required <code>pad</code> <code>bool</code> <p>Whether to pad with None entries.</p> <code>False</code> <code>persist_index</code> <code>bool</code> <p>Whether to persist element indices.</p> <code>False</code> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.list_set_order","title":"list_set_order","text":"<pre><code>list_set_order(order)\n</code></pre> <p>Set list sort order without changing elements.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>ListOrderType</code> <p>List element order.</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>The parent :class:<code>WriteSegmentBuilder</code>.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.select_from","title":"select_from","text":"<pre><code>select_from(expression, *, ignore_eval_failure=False)\n</code></pre> <p>Read a computed value into this bin using a DSL expression.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.insert_from","title":"insert_from","text":"<pre><code>insert_from(\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result only if bin does not already exist.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.update_from","title":"update_from","text":"<pre><code>update_from(\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result only if bin already exists.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.upsert_from","title":"upsert_from","text":"<pre><code>upsert_from(\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result, creating or overwriting the bin.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_index","title":"on_map_index","text":"<pre><code>on_map_index(index)\n</code></pre> <p>Navigate to a map element by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>List index (0-based, negative counts from end).</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_key","title":"on_map_key","text":"<pre><code>on_map_key(key)\n</code></pre> <p>Navigate to a map element by key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Any</code> <p>Map key to target.</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_rank","title":"on_map_rank","text":"<pre><code>on_map_rank(rank)\n</code></pre> <p>Navigate to a map element by rank (0 = lowest value).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_value","title":"on_map_value","text":"<pre><code>on_map_value(value)\n</code></pre> <p>Navigate to map elements matching a value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_index_range","title":"on_map_index_range","text":"<pre><code>on_map_index_range(index, count=None)\n</code></pre> <p>Navigate to map elements by index range.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>List index (0-based, negative counts from end).</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_key_range","title":"on_map_key_range","text":"<pre><code>on_map_key_range(start, end)\n</code></pre> <p>Navigate to map elements by key range [start, end).</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>Any</code> <p>Inclusive range start.</p> required <code>end</code> <code>Any</code> <p>Exclusive range end.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_rank_range","title":"on_map_rank_range","text":"<pre><code>on_map_rank_range(rank, count=None)\n</code></pre> <p>Navigate to map elements by rank range.</p> <p>Parameters:</p> Name Type Description Default <code>rank</code> <code>int</code> <p>Rank position (0 = lowest value).</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_value_range","title":"on_map_value_range","text":"<pre><code>on_map_value_range(start, end)\n</code></pre> <p>Navigate to map elements by value range [start, end).</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>Any</code> <p>Inclusive range start.</p> required <code>end</code> <code>Any</code> <p>Exclusive range end.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_key_relative_index_range","title":"on_map_key_relative_index_range","text":"<pre><code>on_map_key_relative_index_range(key, index, count=None)\n</code></pre> <p>Navigate to map entries by index range relative to an anchor key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Any</code> <p>Map key to target.</p> required <code>index</code> <code>int</code> <p>Index offset from the anchor key.</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_value_relative_rank_range","title":"on_map_value_relative_rank_range","text":"<pre><code>on_map_value_relative_rank_range(value, rank, count=None)\n</code></pre> <p>Navigate to map entries by value rank range relative to an anchor value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to match.</p> required <code>rank</code> <code>int</code> <p>Rank offset from the anchor value.</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_key_list","title":"on_map_key_list","text":"<pre><code>on_map_key_list(keys)\n</code></pre> <p>Navigate to map elements matching a list of keys.</p> <p>Parameters:</p> Name Type Description Default <code>keys</code> <code>List[Any]</code> <p>Map keys to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_map_value_list","title":"on_map_value_list","text":"<pre><code>on_map_value_list(values)\n</code></pre> <p>Navigate to map elements matching a list of values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[Any]</code> <p>Values to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_list_index","title":"on_list_index","text":"<pre><code>on_list_index(index)\n</code></pre> <p>Navigate to a list element by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>List index (0-based, negative counts from end).</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element(s).</p> <p>Example::     .bin(\"items\").on_list_index(0).set_to(\"first\")</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_list_rank","title":"on_list_rank","text":"<pre><code>on_list_rank(rank)\n</code></pre> <p>Navigate to a list element by rank (0 = lowest value).</p> <p>Parameters:</p> Name Type Description Default <code>rank</code> <code>int</code> <p>Rank position (0 = lowest value).</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_list_value","title":"on_list_value","text":"<pre><code>on_list_value(value)\n</code></pre> <p>Navigate to list elements matching a value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_list_index_range","title":"on_list_index_range","text":"<pre><code>on_list_index_range(index, count=None)\n</code></pre> <p>Navigate to list elements by index range.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>List index (0-based, negative counts from end).</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_list_rank_range","title":"on_list_rank_range","text":"<pre><code>on_list_rank_range(rank, count=None)\n</code></pre> <p>Navigate to list elements by rank range.</p> <p>Parameters:</p> Name Type Description Default <code>rank</code> <code>int</code> <p>Rank position (0 = lowest value).</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_list_value_range","title":"on_list_value_range","text":"<pre><code>on_list_value_range(start, end)\n</code></pre> <p>Navigate to list elements by value range [start, end).</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>Any</code> <p>Inclusive range start.</p> required <code>end</code> <code>Any</code> <p>Exclusive range end.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_list_value_relative_rank_range","title":"on_list_value_relative_rank_range","text":"<pre><code>on_list_value_relative_rank_range(value, rank, count=None)\n</code></pre> <p>Navigate to list elements by value rank range relative to an anchor value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to match.</p> required <code>rank</code> <code>int</code> <p>Rank offset from the anchor value.</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.on_list_value_list","title":"on_list_value_list","text":"<pre><code>on_list_value_list(values)\n</code></pre> <p>Navigate to list elements matching a list of values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[Any]</code> <p>Values to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[WriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.bin","title":"bin","text":"<pre><code>bin(bin_name)\n</code></pre> <p>Start the next bin operation without leaving the write segment.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.query","title":"query","text":"<pre><code>query(arg1, *more_keys)\n</code></pre> <p>Shortcut: finalize write segment and start a read segment.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.WriteBinBuilder.execute","title":"execute  <code>async</code>","text":"<pre><code>execute(on_error=None)\n</code></pre> <p>Shortcut: execute all accumulated specs.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder","title":"QueryBinBuilder","text":"<pre><code>QueryBinBuilder(parent, bin_name)\n</code></pre> <p>               Bases: <code>_WriteVerbs</code>, <code>Generic[_T]</code></p> <p>Per-bin reads and CDT navigation for :class:<code>QueryBuilder</code> (and sync twin).</p> <p>The type parameter is the parent builder type; the parent must implement <code>add_operation</code>. Use :meth:<code>get</code> for whole-bin reads, :meth:<code>select_from</code> for expression reads, <code>on_map_*</code> / <code>on_list_*</code> for paths, then :meth:<code>QueryBuilder.execute</code>. Write verbs delegate to the parent to chain writes after reads.</p> Example <p>Read map keys and list size in a single query::</p> <pre><code>stream = await (\n    session.query(key)\n        .bin(\"settings\").on_map_key(\"theme\").get_values()\n        .bin(\"tags\").list_size()\n        .execute()\n)\n</code></pre> See Also <p>:class:<code>WriteBinBuilder</code>: Per-bin write builder. :class:<code>~aerospike_fluent.aio.operations.cdt_read.CdtReadBuilder</code>: Nested reads.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.get","title":"get","text":"<pre><code>get()\n</code></pre> <p>Include the bin value in the read result.</p> <p>Returns:</p> Type Description <code>_T</code> <p>The parent builder for chaining.</p> See Also <p>:meth:<code>select_from</code>: Virtual bin from an expression.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.map_size","title":"map_size","text":"<pre><code>map_size()\n</code></pre> <p>Return the number of entries in the map.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.list_size","title":"list_size","text":"<pre><code>list_size()\n</code></pre> <p>Read list length into the operate/read result.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.select_from","title":"select_from","text":"<pre><code>select_from(expression, *, ignore_eval_failure=False)\n</code></pre> <p>Read a computed value into this bin using a DSL expression.</p> <p>The result appears as a virtual bin in the returned record.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or pre-built FilterExpression.</p> required <code>ignore_eval_failure</code> <code>bool</code> <p>If True, silently return None when the expression cannot be evaluated (e.g. missing bin).</p> <code>False</code> <p>Returns:</p> Type Description <code>_T</code> <p>The parent builder for method chaining.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_index","title":"on_map_index","text":"<pre><code>on_map_index(index)\n</code></pre> <p>Navigate to a map element by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>List index (0-based, negative counts from end).</p> required <p>Returns:</p> Type Description <code>CdtReadBuilder[_T]</code> <p>class:<code>CdtReadBuilder</code> for reading the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_key","title":"on_map_key","text":"<pre><code>on_map_key(key)\n</code></pre> <p>Navigate to a map element by key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Any</code> <p>Map key to target.</p> required <p>Returns:</p> Type Description <code>CdtReadBuilder[_T]</code> <p>class:<code>CdtReadBuilder</code> for reading the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_rank","title":"on_map_rank","text":"<pre><code>on_map_rank(rank)\n</code></pre> <p>Navigate to a map element by rank (0 = lowest value).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_value","title":"on_map_value","text":"<pre><code>on_map_value(value)\n</code></pre> <p>Navigate to map elements matching a value.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_index_range","title":"on_map_index_range","text":"<pre><code>on_map_index_range(index, count=None)\n</code></pre> <p>Navigate to map elements by index range.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_key_range","title":"on_map_key_range","text":"<pre><code>on_map_key_range(start, end)\n</code></pre> <p>Navigate to map elements by key range [start, end).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_rank_range","title":"on_map_rank_range","text":"<pre><code>on_map_rank_range(rank, count=None)\n</code></pre> <p>Navigate to map elements by rank range.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_value_range","title":"on_map_value_range","text":"<pre><code>on_map_value_range(start, end)\n</code></pre> <p>Navigate to map elements by value range [start, end).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_key_relative_index_range","title":"on_map_key_relative_index_range","text":"<pre><code>on_map_key_relative_index_range(key, index, count=None)\n</code></pre> <p>Navigate to map entries by index range relative to an anchor key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Any</code> <p>Map key to target.</p> required <code>index</code> <code>int</code> <p>List index (0-based, negative counts from end).</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtReadInvertableBuilder[_T]</code> <p>class:<code>CdtReadInvertableBuilder</code> for reading the targeted element(s).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_value_relative_rank_range","title":"on_map_value_relative_rank_range","text":"<pre><code>on_map_value_relative_rank_range(value, rank, count=None)\n</code></pre> <p>Navigate to map entries by value rank range relative to an anchor value.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_key_list","title":"on_map_key_list","text":"<pre><code>on_map_key_list(keys)\n</code></pre> <p>Navigate to map elements matching a list of keys.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_map_value_list","title":"on_map_value_list","text":"<pre><code>on_map_value_list(values)\n</code></pre> <p>Navigate to map elements matching a list of values.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_list_index","title":"on_list_index","text":"<pre><code>on_list_index(index)\n</code></pre> <p>Navigate to a list element by index.</p> <p>Example::     .bin(\"items\").on_list_index(-1).get_values()</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_list_rank","title":"on_list_rank","text":"<pre><code>on_list_rank(rank)\n</code></pre> <p>Navigate to a list element by rank (0 = lowest value).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_list_value","title":"on_list_value","text":"<pre><code>on_list_value(value)\n</code></pre> <p>Navigate to list elements matching a value.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_list_index_range","title":"on_list_index_range","text":"<pre><code>on_list_index_range(index, count=None)\n</code></pre> <p>Navigate to list elements by index range.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_list_rank_range","title":"on_list_rank_range","text":"<pre><code>on_list_rank_range(rank, count=None)\n</code></pre> <p>Navigate to list elements by rank range.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_list_value_range","title":"on_list_value_range","text":"<pre><code>on_list_value_range(start, end)\n</code></pre> <p>Navigate to list elements by value range [start, end).</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_list_value_relative_rank_range","title":"on_list_value_relative_rank_range","text":"<pre><code>on_list_value_relative_rank_range(value, rank, count=None)\n</code></pre> <p>Navigate to list elements by value rank range relative to an anchor value.</p>"},{"location":"api/query/#aerospike_fluent.aio.operations.query.QueryBinBuilder.on_list_value_list","title":"on_list_value_list","text":"<pre><code>on_list_value_list(values)\n</code></pre> <p>Navigate to list elements matching a list of values.</p>"},{"location":"api/record-result/","title":"RecordResult","text":""},{"location":"api/record-result/#aerospike_fluent.record_result.RecordResult","title":"RecordResult  <code>dataclass</code>","text":"<pre><code>RecordResult(\n    key,\n    record,\n    result_code,\n    in_doubt=False,\n    index=-1,\n    exception=None,\n    udf_result=None,\n)\n</code></pre> <p>One row from a batch, query stream, or single-key fluent call.</p> <p>Inspect :attr:<code>is_ok</code> and :attr:<code>result_code</code> for outcome; use :meth:<code>or_raise</code> or :meth:<code>record_or_raise</code> when failures should throw. Foreground UDF success values appear in :attr:<code>udf_result</code> when returned by the server.</p> <p>Attributes:</p> Name Type Description <code>key</code> <code>Key</code> <p>Target :class:<code>~aerospike_async.Key</code> for this row.</p> <code>record</code> <code>Record | None</code> <p>:class:<code>~aerospike_async.Record</code> payload, or <code>None</code> if not returned (errors, not found, or UDF error rows).</p> <code>result_code</code> <code>ResultCode</code> <p>Server :class:<code>~aerospike_async.exceptions.ResultCode</code>.</p> <code>in_doubt</code> <code>bool</code> <p><code>True</code> when a write may have completed despite an error.</p> <code>index</code> <code>int</code> <p>Batch position, or <code>0</code> / <code>-1</code> depending on origin.</p> <code>exception</code> <code>AerospikeError | None</code> <p>Embedded :class:<code>~aerospike_fluent.exceptions.AerospikeError</code> when the client placed an error in-stream instead of raising.</p> <code>udf_result</code> <code>Any | None</code> <p>Lua return value for successful foreground UDF calls.</p> Example <p>Inspect a row from a stream::</p> <pre><code>row = await stream.first()\nif row and row.is_ok:\n    bins = row.record.bins if row.record else {}\nelif row:\n    row.or_raise()\n</code></pre> See Also <p>:class:<code>~aerospike_fluent.record_stream.RecordStream</code>: Async iteration of results.</p>"},{"location":"api/record-result/#aerospike_fluent.record_result.RecordResult.is_ok","title":"is_ok  <code>property</code>","text":"<pre><code>is_ok\n</code></pre> <p>Whether :attr:<code>result_code</code> is <code>ResultCode.OK</code>.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> on success; <code>False</code> for any other result code.</p> Example <p>row = await stream.first() if row is not None and row.is_ok and row.record:     bins = row.record.bins</p>"},{"location":"api/record-result/#aerospike_fluent.record_result.RecordResult.or_raise","title":"or_raise","text":"<pre><code>or_raise()\n</code></pre> <p>Return <code>self</code> if successful, else raise from :attr:<code>exception</code> or result code.</p> <p>Returns:</p> Type Description <code>RecordResult</code> <p>This instance when :attr:<code>is_ok</code> is true.</p> <p>Raises:</p> Type Description <code>AerospikeError</code> <p>If :attr:<code>exception</code> is set (embedded client error).</p> <code>AerospikeError</code> <p>Otherwise, from :attr:<code>result_code</code> via :func:<code>~aerospike_fluent.exceptions._result_code_to_exception</code> (usually a specific subclass; unmapped codes use the base type).</p> Example <p>row = await stream.first() if row is not None:     row.or_raise()</p>"},{"location":"api/record-result/#aerospike_fluent.record_result.RecordResult.record_or_raise","title":"record_or_raise","text":"<pre><code>record_or_raise()\n</code></pre> <p>Return :attr:<code>record</code>, raising if the result is not OK.</p> <p>Returns:</p> Type Description <code>Record</code> <p>The non-<code>None</code> :class:<code>~aerospike_async.Record</code>.</p> <p>Raises:</p> Type Description <code>Same as </code> <p>meth:<code>or_raise</code>, plus <code>ValueError</code> if the result is OK</p> <code>but</code> <p>attr:<code>record</code> is <code>None</code> (unexpected empty payload).</p> Example <p>rec = (await stream.first_or_raise()).record_or_raise()</p>"},{"location":"api/record-result/#aerospike_fluent.record_result.RecordResult.as_bool","title":"as_bool","text":"<pre><code>as_bool()\n</code></pre> <p>Interpret the row as an existence check (for example after :meth:<code>Session.exists</code>).</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the key exists (OK result).</p> <code>bool</code> <p><code>False</code> if the result is key-not-found.</p> <p>Raises:</p> Type Description <code>AerospikeError</code> <p>For any other non-OK code (via :meth:<code>or_raise</code>).</p> Example <p>found = (await exists_stream.first_or_raise()).as_bool()</p>"},{"location":"api/record-stream/","title":"RecordStream","text":""},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream","title":"RecordStream","text":"<pre><code>RecordStream(source)\n</code></pre> <p>Async iterator of :class:<code>~aerospike_fluent.record_result.RecordResult</code> rows.</p> <p>Produced by <code>await session.query(...).execute()</code> and similar APIs. Prefer <code>async for row in stream</code>, or helpers such as :meth:<code>collect</code> and :meth:<code>first</code>. Do not call <code>RecordStream(...)</code> directly; use factories like :meth:<code>from_list</code> or :meth:<code>from_batch_records</code>.</p> Example <p>Typical consumption with <code>async for</code>::</p> <pre><code>stream = await session.query(key).bins([\"name\"]).execute()\nasync for row in stream:\n    if row.is_ok and row.record:\n        print(row.record.bins)\n</code></pre> See Also <p>:meth:<code>first_or_raise</code>: Assert a single OK row.</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.from_list","title":"from_list  <code>classmethod</code>","text":"<pre><code>from_list(results)\n</code></pre> <p>Wrap an already-materialised list of results.</p> <p>Example::     stream = RecordStream.from_list([row1, row2])     rows = await stream.collect()</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.chain","title":"chain  <code>classmethod</code>","text":"<pre><code>chain(streams)\n</code></pre> <p>Yield all results from each stream in order.</p> <p>Example::     combined = RecordStream.chain([stream_a, stream_b])</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.from_batch_records","title":"from_batch_records  <code>classmethod</code>","text":"<pre><code>from_batch_records(batch_records)\n</code></pre> <p>Wrap a sequence of async-client <code>BatchRecord</code> objects.</p> <p>Example::     stream = RecordStream.from_batch_records(batch_records)</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.from_recordset","title":"from_recordset  <code>classmethod</code>","text":"<pre><code>from_recordset(recordset)\n</code></pre> <p>Wrap a <code>Recordset</code> (async iterable of <code>Record</code>).</p> <p>Each yielded <code>Record</code> is converted to a :class:<code>RecordResult</code> with <code>result_code=OK</code> and <code>index=-1</code> (queries have no positional index).</p> <p>Example::     stream = RecordStream.from_recordset(recordset)</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.from_chunked_recordset","title":"from_chunked_recordset  <code>classmethod</code>","text":"<pre><code>from_chunked_recordset(recordset, reexecute, limit=0)\n</code></pre> <p>Wrap a <code>Recordset</code> for chunked iteration.</p> <p>The stream yields records from the current chunk.  Call :meth:<code>has_more_chunks</code> to advance to the next server chunk.</p> <p>Parameters:</p> Name Type Description Default <code>recordset</code> <code>Any</code> <p>The PAC <code>Recordset</code> from the first query call.</p> required <code>reexecute</code> <code>Callable[[PartitionFilter], Awaitable[Any]]</code> <p>An async callable that accepts an updated <code>PartitionFilter</code> and returns a new <code>Recordset</code>.</p> required <code>limit</code> <code>int</code> <p>Optional overall record limit (0 = unlimited).</p> <code>0</code>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.from_single","title":"from_single  <code>classmethod</code>","text":"<pre><code>from_single(key, record)\n</code></pre> <p>Wrap a single-key result.</p> <p>Example::     stream = RecordStream.from_single(key, record)</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.from_error","title":"from_error  <code>classmethod</code>","text":"<pre><code>from_error(\n    key, result_code, in_doubt=False, exception=None\n)\n</code></pre> <p>Wrap a single-key error as a one-element stream.</p> <p>Example::     stream = RecordStream.from_error(key, ResultCode.TIMEOUT)</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.__aiter__","title":"__aiter__","text":"<pre><code>__aiter__()\n</code></pre> <p>Return <code>self</code> for <code>async for</code> iteration.</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.__anext__","title":"__anext__  <code>async</code>","text":"<pre><code>__anext__()\n</code></pre> <p>Yield the next :class:<code>~aerospike_fluent.record_result.RecordResult</code>.</p> <p>Raises:</p> Type Description <code>StopAsyncIteration</code> <p>When the stream is exhausted or :meth:<code>close</code> was called before more rows were read.</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.has_more_chunks","title":"has_more_chunks  <code>async</code>","text":"<pre><code>has_more_chunks()\n</code></pre> <p>Check whether more server-side chunks remain.</p> <p>On the first call this returns <code>True</code> so the caller enters the iteration loop for the already-loaded first chunk.  Subsequent calls inspect the server's <code>PartitionFilter</code> cursor: if more partitions remain, a new query round-trip is issued transparently and <code>True</code> is returned.</p> <p>Returns <code>False</code> when: * the server cursor is done (all partitions scanned), or * the overall <code>limit</code> has been reached, or * the stream was not created with :meth:<code>from_chunked_recordset</code>.</p> <p>Example::</p> <pre><code>stream = await session.query(SET).chunk_size(10).execute()\nchunk = 0\nwhile await stream.has_more_chunks():\n    chunk += 1\n    print(f\"Chunk: {chunk}\")\n    async for rr in stream:\n        print(rr.record.bins)\n</code></pre>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.first","title":"first  <code>async</code>","text":"<pre><code>first()\n</code></pre> <p>Consume and return the first row, or <code>None</code> if there are no rows.</p> <p>Returns:</p> Type Description <code>RecordResult | None</code> <p>The first :class:<code>~aerospike_fluent.record_result.RecordResult</code>, or</p> <code>RecordResult | None</code> <p><code>None</code> when the stream is empty.</p> Note <p>This advances the iterator; remaining rows are left for further <code>async for</code> or other helpers only if the underlying source allows partial consumption (most fluent streams are single-pass).</p> <p>Example::     stream = await session.query(key).execute()     row = await stream.first()     if row is None:         ...</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.first_or_raise","title":"first_or_raise  <code>async</code>","text":"<pre><code>first_or_raise()\n</code></pre> <p>Return the first row and require success (see :meth:<code>RecordResult.or_raise</code>).</p> <p>Returns:</p> Type Description <code>RecordResult</code> <p>The first OK :class:<code>~aerospike_fluent.record_result.RecordResult</code>.</p> <p>Raises:</p> Type Description <code>StopAsyncIteration</code> <p>If the stream yields no rows (empty).</p> <code>AerospikeError</code> <p>If the first row is not OK (from :meth:<code>RecordResult.or_raise</code>).</p> Example <p>rec = (await stream.first_or_raise()).record_or_raise()</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.first_udf_result","title":"first_udf_result  <code>async</code>","text":"<pre><code>first_udf_result()\n</code></pre> <p>Scan forward for the first non-<code>None</code> :attr:<code>~aerospike_fluent.record_result.RecordResult.udf_result</code>.</p> <p>Returns:</p> Type Description <code>Any | None</code> <p>The UDF return value, or <code>None</code> if no row carries a UDF result.</p> <p>Example::     value = await stream.first_udf_result()</p> See Also <p>:meth:<code>Session.execute_udf</code>: Produces streams with UDF results.</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.collect","title":"collect  <code>async</code>","text":"<pre><code>collect()\n</code></pre> <p>Drain the stream into a list (order preserved).</p> <p>Returns:</p> Type Description <code>list[RecordResult]</code> <p>All remaining :class:<code>~aerospike_fluent.record_result.RecordResult</code></p> <code>list[RecordResult]</code> <p>instances.</p> Example <p>rows = await stream.collect() oks = [r for r in rows if r.is_ok]</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.failures","title":"failures  <code>async</code>","text":"<pre><code>failures()\n</code></pre> <p>Drain the stream and return rows where :attr:<code>~aerospike_fluent.record_result.RecordResult.is_ok</code> is false.</p> <p>Returns:</p> Type Description <code>list[RecordResult]</code> <p>Only error or non-OK rows.</p> Note <p>Like :meth:<code>collect</code>, this consumes the entire stream.</p>"},{"location":"api/record-stream/#aerospike_fluent.record_stream.RecordStream.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Mark the stream closed; further :meth:<code>__anext__</code> calls stop iteration.</p> <p>Idempotent. Use when abandoning a stream early to cooperate with resource cleanup where supported.</p>"},{"location":"api/record-stream/#syncrecordstream","title":"SyncRecordStream","text":""},{"location":"api/record-stream/#aerospike_fluent.sync.record_stream.SyncRecordStream","title":"SyncRecordStream","text":"<pre><code>SyncRecordStream(stream, loop_manager)\n</code></pre> <p>Blocking view of :class:<code>~aerospike_fluent.record_stream.RecordStream</code>.</p> <p>Iterator protocol and helpers (<code>first</code>, <code>collect</code>, \u2026) call into the underlying async stream via the parent client's event-loop manager, so each step may block the calling thread until data is ready.</p> <p>Behavior vs async: Ordering and per-row semantics match :class:<code>~aerospike_fluent.record_stream.RecordStream</code>; only the API surface is synchronous. <code>close()</code> closes the async stream directly without crossing the loop (releases resources; pending async iteration should stop).</p> <p>Example::     with SyncFluentClient(\"localhost:3000\") as client:         session = client.create_session()         for row in session.query(ns, set).bins([\"name\"]).execute():             if row.record:                 print(row.record.bins)</p> See Also <p>:class:<code>~aerospike_fluent.record_stream.RecordStream</code></p> <p>Wrap <code>stream</code> for synchronous consumption (internal use).</p>"},{"location":"api/record-stream/#aerospike_fluent.sync.record_stream.SyncRecordStream.first","title":"first","text":"<pre><code>first()\n</code></pre> <p>Return the first row, or <code>None</code> if the stream is empty.</p> See Also <p>:meth:<code>~aerospike_fluent.record_stream.RecordStream.first</code></p>"},{"location":"api/record-stream/#aerospike_fluent.sync.record_stream.SyncRecordStream.first_or_raise","title":"first_or_raise","text":"<pre><code>first_or_raise()\n</code></pre> <p>Return the first row or raise if the stream is empty or not OK.</p> <p>Raises:</p> Type Description <code>AerospikeError</code> <p>On a failed row or empty-stream error from the async layer. See :meth:<code>~aerospike_fluent.record_stream.RecordStream.first_or_raise</code>.</p>"},{"location":"api/record-stream/#aerospike_fluent.sync.record_stream.SyncRecordStream.first_udf_result","title":"first_udf_result","text":"<pre><code>first_udf_result()\n</code></pre> <p>Return the first non-<code>None</code> :attr:<code>~RecordResult.udf_result</code>.</p>"},{"location":"api/record-stream/#aerospike_fluent.sync.record_stream.SyncRecordStream.collect","title":"collect","text":"<pre><code>collect()\n</code></pre> <p>Drain the stream into a list (blocks until complete).</p>"},{"location":"api/record-stream/#aerospike_fluent.sync.record_stream.SyncRecordStream.failures","title":"failures","text":"<pre><code>failures()\n</code></pre> <p>Collect rows whose :attr:<code>~RecordResult.result_code</code> is not OK.</p>"},{"location":"api/record-stream/#aerospike_fluent.sync.record_stream.SyncRecordStream.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Release the underlying async stream.</p>"},{"location":"api/session/","title":"Session","text":""},{"location":"api/session/#aerospike_fluent.aio.session.Session","title":"Session","text":"<pre><code>Session(client, behavior)\n</code></pre> <p>Perform reads and writes against Aerospike with a fixed :class:<code>~aerospike_fluent.policy.behavior.Behavior</code>.</p> <p>A session binds a connected :class:<code>FluentClient</code> to policy defaults (timeouts, retries, replica preferences) for every operation started from it. Create sessions with :meth:<code>FluentClient.create_session</code>; do not construct <code>Session</code> directly.</p> Example <p>async with FluentClient(\"localhost:3000\") as client:     session = client.create_session(Behavior.DEFAULT)     users = DataSet.of(\"test\", \"users\")     stream = await session.query(users.id(1)).execute()     first = await stream.first_or_raise()     await session.upsert(users.id(2)).put({\"name\": \"Tim\"}).execute()</p> See Also <p>:meth:<code>FluentClient.create_session</code>: How to obtain a session. :meth:<code>query</code>: Point reads, batch reads, and secondary-index queries. :meth:<code>upsert</code>: Create-or-update writes.</p> <p>Attach a client and behavior; prefer :meth:<code>FluentClient.create_session</code>.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>FluentClient</code> <p>Connected (or not yet connected) :class:<code>FluentClient</code>.</p> required <code>behavior</code> <code>Behavior</code> <p>Policy bundle for operations from this session.</p> required Note <p>Application code should not call <code>Session(...)</code> directly.</p> See Also <p>:meth:<code>FluentClient.create_session</code>.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.behavior","title":"behavior  <code>property</code>","text":"<pre><code>behavior\n</code></pre> <p>Policy bundle applied to operations created from this session.</p> <p>Returns:</p> Name Type Description <code>The</code> <code>Behavior</code> <p>class:<code>~aerospike_fluent.policy.behavior.Behavior</code> passed to</p> <code>Behavior</code> <p>meth:<code>FluentClient.create_session</code>.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.client","title":"client  <code>property</code>","text":"<pre><code>client\n</code></pre> <p>Fluent client that owns the connection used by this session.</p> <p>Returns:</p> Type Description <code>FluentClient</code> <p>The parent :class:<code>FluentClient</code>.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.batch","title":"batch","text":"<pre><code>batch()\n</code></pre> <p>Start a multi-key batch of mixed write operations executed in one server round trip.</p> <p>Chain <code>insert</code>, <code>update</code>, <code>upsert</code>, <code>replace</code>, <code>delete</code>, and related bin builders, then <code>await ...execute()</code> to obtain per-key outcomes.</p> <p>Returns:</p> Name Type Description <code>A</code> <code>'BatchOperationBuilder'</code> <p>class:<code>~aerospike_fluent.aio.operations.batch.BatchOperationBuilder</code></p> <code>'BatchOperationBuilder'</code> <p>for chaining operations.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the client is not connected.</p> Example <p>results = await (     session.batch()     .insert(key1).put({\"name\": \"Alice\", \"age\": 25})     .update(key2).bin(\"counter\").add(1)     .upsert(key3).put({\"status\": \"active\"})     .delete(key4)     .execute() ) for row in results:     print(row.key, row.result_code)</p> See Also <p>:meth:<code>upsert</code>: Single-record writes without batching.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.background_task","title":"background_task","text":"<pre><code>background_task()\n</code></pre> <p>Configure a server-side background job (query + scan scope) on a dataset.</p> <p>Call <code>update</code>, <code>delete</code>, <code>touch</code>, or <code>execute_udf</code> on the returned object, add optional filters (for example <code>where</code> on supported builders), then <code>await ...execute()</code> to start work and receive an async task handle.</p> <p>Returns:</p> Name Type Description <code>A</code> <code>'BackgroundTaskSession'</code> <p>class:<code>~aerospike_fluent.aio.background.BackgroundTaskSession</code></p> <code>'BackgroundTaskSession'</code> <p>for chaining the operation type and execution.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the client is not connected.</p> Example <p>task = await (     session.background_task()     .delete(DataSet.of(\"test\", \"scratch\"))     .where(\"$.flag == 1\")     .execute() ) await task.wait_till_complete(sleep_time=0.2, max_attempts=50)</p> See Also <p>:meth:<code>execute_udf</code>: Foreground UDF on explicit keys.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.execute_udf","title":"execute_udf","text":"<pre><code>execute_udf(*keys)\n</code></pre> <p>Run a registered server-side UDF on one or more keys (foreground).</p> <p>Chain <code>function(package, name)</code> (package is the registered module name without <code>.lua</code>), optional <code>passing(*args)</code> for Lua parameters, optional <code>where</code> for a filter expression, then <code>await ...execute()</code> to obtain a :class:<code>~aerospike_fluent.record_stream.RecordStream</code>. Multiple keys use a batch UDF; results preserve per-key order where applicable.</p> <p>Parameters:</p> Name Type Description Default <code>*keys</code> <code>Key</code> <p>One or more :class:<code>~aerospike_async.Key</code> targets in the same namespace and set.</p> <code>()</code> <p>Returns:</p> Type Description <code>'UdfFunctionBuilder'</code> <p>class:<code>~aerospike_fluent.aio.operations.udf.UdfFunctionBuilder</code> \u2014</p> <code>'UdfFunctionBuilder'</code> <p>call <code>function</code> next.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are given.</p> <code>RuntimeError</code> <p>If the client is not connected.</p> Example <p>users = DataSet.of(\"test\", \"users\") stream = await (     session.execute_udf(users.id(\"a\"))     .function(\"my_module\", \"my_fn\")     .passing(\"binName\", 42)     .execute() ) value = await stream.first_udf_result()</p> See Also <p>:meth:<code>query</code>: Read bins without UDF. :meth:<code>background_task</code>: Dataset-scoped background UDF.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.query","title":"query","text":"<pre><code>query(dataset: DataSet) -&gt; QueryBuilder\n</code></pre><pre><code>query(key: Key) -&gt; QueryBuilder\n</code></pre><pre><code>query(keys: List[Key]) -&gt; QueryBuilder\n</code></pre><pre><code>query(*keys: Key) -&gt; QueryBuilder\n</code></pre><pre><code>query(namespace: str, set_name: str) -&gt; QueryBuilder\n</code></pre> <pre><code>query(\n    arg1=None,\n    arg2=None,\n    *keys,\n    namespace=None,\n    set_name=None,\n    dataset=None,\n    key=None,\n    keys_list=None,\n)\n</code></pre> <p>Start a read or secondary-index query for keys or a whole set.</p> <p>This session's :attr:<code>behavior</code> is applied to the underlying :class:<code>~aerospike_fluent.aio.operations.query.QueryBuilder</code>. Supported shapes include a :class:<code>~aerospike_fluent.dataset.DataSet</code> (set-wide query), a single :class:<code>~aerospike_async.Key</code>, multiple keys (list or varargs), or explicit <code>namespace</code> / <code>set_name</code> for index scans.</p> <p>Parameters:</p> Name Type Description Default <code>arg1</code> <code>Optional[Union[DataSet, Key, List[Key], str]]</code> <p>Positional dataset, key, list of keys, or namespace string (when paired with <code>arg2</code> as set name).</p> <code>None</code> <code>arg2</code> <code>Optional[str]</code> <p>When <code>arg1</code> is a namespace, the set name; otherwise may be a second key when passing multiple keys positionally.</p> <code>None</code> <code>*keys</code> <code>Key</code> <p>Additional keys when the first positional argument is a key.</p> <code>()</code> <code>namespace</code> <code>Optional[str]</code> <p>Keyword namespace (with <code>set_name</code>) when not using a dataset.</p> <code>None</code> <code>set_name</code> <code>Optional[str]</code> <p>Keyword set name (with <code>namespace</code>).</p> <code>None</code> <code>dataset</code> <code>Optional[DataSet]</code> <p>Keyword :class:<code>~aerospike_fluent.dataset.DataSet</code>.</p> <code>None</code> <code>key</code> <code>Optional[Key]</code> <p>Keyword single key.</p> <code>None</code> <code>keys_list</code> <code>Optional[List[Key]]</code> <p>Keyword list of keys when not using <code>arg1</code> or varargs; forwarded to the client as <code>keys</code>.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>QueryBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.QueryBuilder</code> to</p> <code>QueryBuilder</code> <p>chain <code>where</code>, <code>bins</code>, <code>execute</code>, etc.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If positional types do not match the supported overloads.</p> <code>ValueError</code> <p>If a key list is empty or arguments are inconsistent.</p> Example <p>users = DataSet.of(\"test\", \"users\") rs = await session.query(users.id(1)).bins([\"name\"]).execute() row = await rs.first_or_raise()</p> Example <p>users = DataSet.of(\"test\", \"users\") rs = await session.query(users.ids(1, 2, 3)).bins([\"name\"]).execute() rows = await rs.collect()</p> See Also <p>:meth:<code>FluentClient.query</code>: Same shapes without session behavior. :meth:<code>upsert</code>: Writes for the same keys.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.index","title":"index","text":"<pre><code>index(*, dataset: DataSet) -&gt; IndexBuilder\n</code></pre><pre><code>index(namespace: str, set_name: str) -&gt; IndexBuilder\n</code></pre> <pre><code>index(namespace=None, set_name=None, *, dataset=None)\n</code></pre> <p>Create a secondary index builder for a namespace and set.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>Optional[str]</code> <p>Namespace name when not using <code>dataset</code>.</p> <code>None</code> <code>set_name</code> <code>Optional[str]</code> <p>Set name when not using <code>dataset</code>.</p> <code>None</code> <code>dataset</code> <code>Optional[DataSet]</code> <p>Optional :class:<code>~aerospike_fluent.dataset.DataSet</code> that supplies namespace and set.</p> <code>None</code> <p>Returns:</p> Type Description <code>IndexBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.index.IndexBuilder</code> for chaining index definition and creation.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>dataset</code> is not given and <code>namespace</code> or <code>set_name</code> is missing.</p> <p>Example::</p> <pre><code>users = DataSet.of(\"test\", \"users\")\nawait session.index(dataset=users).on_bin(\"age\").named(\"age_idx\").numeric().create()\n</code></pre> See Also <p>:meth:<code>FluentClient.index</code></p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.transaction_session","title":"transaction_session","text":"<pre><code>transaction_session()\n</code></pre> <p>Create a transactional session for multi-record atomic operations.</p> <p>Returns:</p> Type Description <code>TransactionalSession</code> <p>class:<code>~aerospike_fluent.aio.transactional_session.TransactionalSession</code>.</p> See Also <p>:meth:<code>FluentClient.transaction_session</code></p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.info","title":"info","text":"<pre><code>info() -&gt; InfoCommands\n</code></pre><pre><code>info(command: str) -&gt; Awaitable[Dict[str, str]]\n</code></pre> <pre><code>info(command=None)\n</code></pre> <p>Execute info commands or get the InfoCommands helper.</p> <p>With no argument, returns an InfoCommands instance for high-level helpers (namespaces(), namespace_details(), etc.) and for info_on_all_nodes().</p> <p>With a command string, runs the raw info command and returns its result (awaitable).</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>Optional[str]</code> <p>Optional. If given, the raw info command to run (e.g. \"sindex-list\", \"build\").</p> <code>None</code> <p>Returns:</p> Type Description <code>Union[InfoCommands, Awaitable[Dict[str, str]]]</code> <p>If command is None: InfoCommands instance.</p> <code>Union[InfoCommands, Awaitable[Dict[str, str]]]</code> <p>If command is given: awaitable dict (node -&gt; response).</p> Example <pre><code># Raw command (no double .info)\nresponse = await session.info(\"sindex-list\")\n\n# High-level helpers\ninfo = session.info()\nnamespaces = await info.namespaces()\nby_node = await info.info_on_all_nodes(\"build\")\n</code></pre>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.is_namespace_sc","title":"is_namespace_sc  <code>async</code>","text":"<pre><code>is_namespace_sc(namespace)\n</code></pre> <p>Check if a namespace is in strong consistency (SC) mode.</p> <p>Strong consistency mode provides linearizable reads and writes at the cost of availability during network partitions.</p> <p>Parameters:</p> Name Type Description Default <code>namespace</code> <code>str</code> <p>The namespace name to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the namespace is in strong consistency mode, False otherwise.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the namespace is unknown or the info command fails.</p> Example <pre><code>if await session.is_namespace_sc(\"test\"):\n    print(\"Namespace 'test' is in strong consistency mode\")\nelse:\n    print(\"Namespace 'test' is in AP (availability) mode\")\n</code></pre>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.do_in_transaction","title":"do_in_transaction  <code>async</code>","text":"<pre><code>do_in_transaction(operation)\n</code></pre> <p>Execute an operation within a transaction.</p> <p>This is a convenience method that creates a TransactionalSession, executes the operation, and handles commit/rollback automatically.</p> <p>Parameters:</p> Name Type Description Default <code>operation</code> <code>Callable[[TransactionalSession], Awaitable[Any]]</code> <p>An async function that takes a TransactionalSession       and performs operations within it.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The result of the operation function.</p> Example <pre><code>async def transfer_funds(tx_session):\n    await tx_session.upsert(accounts.id(\"acc1\")).put({\"balance\": 100}).execute()\n    await tx_session.upsert(accounts.id(\"acc2\")).put({\"balance\": 200}).execute()\n\nawait session.do_in_transaction(transfer_funds)\n</code></pre>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.upsert","title":"upsert","text":"<pre><code>upsert(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Start a create-or-replace write for one or more keys.</p> <p>If the record exists, bins are merged according to the chained operations; if it does not exist, it is created. Use :meth:<code>insert</code> when the record must not already exist.</p> <p>Parameters:</p> Name Type Description Default <code>arg1</code> <code>Optional[Union[Key, List[Key]]]</code> <p>A single :class:<code>~aerospike_async.Key</code>, a list of keys, or omit and pass <code>key</code> / <code>dataset</code> + <code>key_value</code> / <code>namespace</code> + <code>set_name</code> + <code>key_value</code>.</p> <code>None</code> <code>arg2</code> <code>Optional[Key]</code> <p>Optional second key when passing multiple keys positionally.</p> <code>None</code> <code>*keys</code> <code>Key</code> <p>Additional keys when the first positional is a key.</p> <code>()</code> <code>key</code> <code>Optional[Key]</code> <p>Single key (keyword form).</p> <code>None</code> <code>dataset</code> <code>Optional[DataSet]</code> <p>Dataset used with <code>key_value</code> to build a key.</p> <code>None</code> <code>namespace</code> <code>Optional[str]</code> <p>Namespace used with <code>set_name</code> and <code>key_value</code>.</p> <code>None</code> <code>set_name</code> <code>Optional[str]</code> <p>Set name used with <code>namespace</code> and <code>key_value</code>.</p> <code>None</code> <code>key_value</code> <code>Optional[Union[str, int, bytes]]</code> <p>User key value with <code>dataset</code> or <code>namespace</code>/<code>set_name</code>.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>WriteSegmentBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder</code></p> <code>WriteSegmentBuilder</code> <p>for <code>put</code>, <code>bin</code>, <code>where</code>, <code>execute</code>, etc.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are resolved or lists are empty.</p> <code>TypeError</code> <p>If positional arguments are not keys or lists of keys.</p> Example <p>users = DataSet.of(\"test\", \"users\") await session.upsert(users.id(1)).put({\"name\": \"Tim\", \"age\": 30}).execute()</p> See Also <p>:meth:<code>insert</code>: Fails if the record already exists. :meth:<code>update</code>: Fails if the record does not exist. :meth:<code>replace</code>: Replace-entire-record semantics when configured.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.insert","title":"insert","text":"<pre><code>insert(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Start a create-only write; fails on execute if the record already exists.</p> <p>Key resolution matches :meth:<code>upsert</code>.</p> <p>Returns:</p> Name Type Description <code>A</code> <code>WriteSegmentBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are resolved.</p> <code>TypeError</code> <p>If positional arguments are invalid.</p> Example <p>users = DataSet.of(\"test\", \"users\") await session.insert(users.id(99)).put({\"name\": \"new\"}).execute()</p> See Also <p>:meth:<code>upsert</code>: Create or update.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.update","title":"update","text":"<pre><code>update(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Start an update-only write; fails on execute if the record is missing.</p> <p>Key resolution matches :meth:<code>upsert</code>.</p> <p>Returns:</p> Name Type Description <code>A</code> <code>WriteSegmentBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are resolved.</p> <code>TypeError</code> <p>If positional arguments are invalid.</p> See Also <p>:meth:<code>upsert</code>: Create if missing. :meth:<code>replace_if_exists</code>: Replace semantics when the record exists.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.replace","title":"replace","text":"<pre><code>replace(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Start a full-record replace write (bins replaced per builder rules).</p> <p>Key resolution matches :meth:<code>upsert</code>. Prefer :meth:<code>replace_if_exists</code> when the record must already exist.</p> <p>Returns:</p> Name Type Description <code>A</code> <code>WriteSegmentBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are resolved.</p> <code>TypeError</code> <p>If positional arguments are invalid.</p> See Also <p>:meth:<code>replace_if_exists</code>: Replace only when the record exists.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.replace_if_exists","title":"replace_if_exists","text":"<pre><code>replace_if_exists(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Start a replace write that requires an existing record.</p> <p>Key resolution matches :meth:<code>upsert</code>. On execute, missing keys surface as errors according to error strategy (default may raise).</p> <p>Returns:</p> Name Type Description <code>A</code> <code>WriteSegmentBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are resolved.</p> <code>TypeError</code> <p>If positional arguments are invalid.</p> See Also <p>:meth:<code>replace</code>: Unconditional replace semantics.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.delete","title":"delete","text":"<pre><code>delete(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Start a delete for one or more keys.</p> <p>Key resolution matches :meth:<code>upsert</code>. Chain filters or durable-delete options on the builder, then <code>await ...execute()</code>.</p> <p>Returns:</p> Name Type Description <code>A</code> <code>WriteSegmentBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are resolved.</p> <code>TypeError</code> <p>If positional arguments are invalid.</p> Example <p>users = DataSet.of(\"test\", \"users\") await session.delete(users.id(1)).execute() await session.delete(users.ids(10, 11)).execute()</p> See Also <p>:meth:<code>background_task</code>: Delete many records via a server job.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.touch","title":"touch","text":"<pre><code>touch(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Start a touch to refresh TTL without changing bins.</p> <p>Key resolution matches :meth:<code>upsert</code>. Use the builder to set TTL or related policy, then <code>await ...execute()</code>.</p> <p>Returns:</p> Name Type Description <code>A</code> <code>WriteSegmentBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are resolved.</p> <code>TypeError</code> <p>If positional arguments are invalid.</p> See Also <p>:meth:<code>upsert</code>: Writes that can also set expiration via the builder.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.exists","title":"exists","text":"<pre><code>exists(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Start an existence check for one or more keys.</p> <p>Key resolution matches :meth:<code>upsert</code>. After <code>execute</code>, use :meth:<code>~aerospike_fluent.record_result.RecordResult.as_bool</code> on each :class:<code>~aerospike_fluent.record_result.RecordResult</code> or inspect <code>result_code</code>.</p> <p>Returns:</p> Name Type Description <code>A</code> <code>WriteSegmentBuilder</code> <p>class:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are resolved.</p> <code>TypeError</code> <p>If positional arguments are invalid.</p> Example <p>users = DataSet.of(\"test\", \"users\") rs = await session.exists(users.id(1)).execute() exists = (await rs.first()).as_bool()</p> See Also <p>:meth:<code>query</code>: Read record data when the key is known to exist.</p>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.truncate","title":"truncate  <code>async</code>","text":"<pre><code>truncate(dataset, before_nanos=None)\n</code></pre> <p>Truncate (delete all records) from a set; this cannot be undone.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>DataSet</code> <p>The DataSet to truncate.</p> required <code>before_nanos</code> <code>Optional[int]</code> <p>Optional timestamp in nanoseconds. Only records with last update time (LUT) less than this value are truncated. If None, all records in the set are truncated.</p> <code>None</code> <p>Returns:</p> Type Description <code>None</code> <p>None</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the client is not connected.</p> <p>Example::</p> <pre><code>users = DataSet.of(\"test\", \"users\")\nawait session.truncate(users)\n\ncutoff_time = time.time_ns() - (24 * 60 * 60 * 10**9)  # 24 hours ago\nawait session.truncate(users, before_nanos=cutoff_time)\n</code></pre>"},{"location":"api/session/#aerospike_fluent.aio.session.Session.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the session.</p>"},{"location":"api/system-settings/","title":"SystemSettings","text":""},{"location":"api/system-settings/#aerospike_fluent.policy.system_settings.SystemSettings","title":"SystemSettings  <code>dataclass</code>","text":"<pre><code>SystemSettings(\n    min_connections_per_node=None,\n    max_connections_per_node=None,\n    conn_pools_per_node=None,\n    max_socket_idle_time=None,\n    tend_interval=None,\n)\n</code></pre> <p>Cluster-wide settings that apply to an entire cluster instance.</p> <p>These settings cannot vary per Behavior -- they are inherently global to the connection pool and cluster maintenance.</p> <p>Example::</p> <pre><code>settings = SystemSettings(\n    max_connections_per_node=200,\n    max_socket_idle_time=timedelta(seconds=30),\n)\ncluster = await ClusterDefinition(\"localhost\", 3000) \\\n    .with_system_settings(settings) \\\n    .connect()\n</code></pre>"},{"location":"api/system-settings/#aerospike_fluent.policy.system_settings.SystemSettings.apply_to","title":"apply_to","text":"<pre><code>apply_to(policy)\n</code></pre> <p>Apply non-None fields to policy, returning the same object.</p>"},{"location":"api/tls-builder/","title":"TlsBuilder","text":""},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder","title":"TlsBuilder","text":"<pre><code>TlsBuilder(parent)\n</code></pre> <p>Builder class for configuring TLS settings for Aerospike cluster connections.</p> <p>This class provides a fluent API for configuring TLS parameters such as TLS name, CA file, protocols, ciphers, and other TLS-specific options.</p> Simple example usage <pre><code>cluster = ClusterDefinition(\"localhost\", 3100)            .with_tls_config_of()                .tls_name(\"myTlsName\")                .ca_file(\"myCaFile\")            .done()            .with_native_credentials(\"myUser\", \"password\")\n</code></pre> <p>Initialize a TlsBuilder.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>ClusterDefinition</code> <p>The parent ClusterDefinition</p> required"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.tls_name","title":"tls_name","text":"<pre><code>tls_name(tls_name)\n</code></pre> <p>Sets the TLS name for server certificate validation and hostname verification.</p> <p>This TLS name will be applied to all Host objects that don't already have a TLS name set. The TLS name is used for: - Certificate validation: Verifies the server certificate matches this name - SNI (Server Name Indication): Tells the server which certificate to present - Hostname override: Allows validation against a different name than the connection address</p> <p>Parameters:</p> Name Type Description Default <code>tls_name</code> <code>str</code> <p>The TLS name for certificate validation and hostname verification</p> required <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.ca_file","title":"ca_file","text":"<pre><code>ca_file(ca_file)\n</code></pre> <p>Sets the path to the Certificate Authority (CA) PEM file.</p> <p>The CA file contains the certificates used to verify the server's identity. This method supports PEM-formatted certificate files for easy certificate management.</p> <p>Parameters:</p> Name Type Description Default <code>ca_file</code> <code>str</code> <p>The path to the CA certificate PEM file</p> required <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.client_cert_file","title":"client_cert_file","text":"<pre><code>client_cert_file(cert_file)\n</code></pre> <p>Sets the path to the client certificate PEM file for mutual TLS (mTLS).</p> <p>Parameters:</p> Name Type Description Default <code>cert_file</code> <code>str</code> <p>The path to the client certificate PEM file</p> required <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.client_key_file","title":"client_key_file","text":"<pre><code>client_key_file(key_file)\n</code></pre> <p>Sets the path to the client private key PEM file for mutual TLS (mTLS).</p> <p>Parameters:</p> Name Type Description Default <code>key_file</code> <code>str</code> <p>The path to the client private key PEM file</p> required <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.protocols","title":"protocols","text":"<pre><code>protocols(*protocols)\n</code></pre> <p>Sets the allowed TLS protocols.</p> <p>Parameters:</p> Name Type Description Default <code>*protocols</code> <code>str</code> <p>The TLS protocols to allow (e.g., \"TLSv1.2\", \"TLSv1.3\")</p> <code>()</code> <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.ciphers","title":"ciphers","text":"<pre><code>ciphers(*ciphers)\n</code></pre> <p>Sets the allowed TLS cipher suites.</p> <p>Parameters:</p> Name Type Description Default <code>*ciphers</code> <code>str</code> <p>The cipher suite names to allow</p> <code>()</code> <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.for_login_only","title":"for_login_only","text":"<pre><code>for_login_only(for_login_only=True)\n</code></pre> <p>Sets whether TLS should be used only for login/authentication.</p> <p>Parameters:</p> Name Type Description Default <code>for_login_only</code> <code>bool</code> <p>If True, TLS is only used for authentication</p> <code>True</code> <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.done","title":"done","text":"<pre><code>done()\n</code></pre> <p>Completes TLS configuration and returns to the parent ClusterDefinition.</p> <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>The parent ClusterDefinition for continued method chaining</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.is_tls_enabled","title":"is_tls_enabled","text":"<pre><code>is_tls_enabled()\n</code></pre> <p>Check if TLS is enabled.</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.get_tls_name","title":"get_tls_name","text":"<pre><code>get_tls_name()\n</code></pre> <p>Get the TLS name.</p>"},{"location":"api/tls-builder/#aerospike_fluent.aio.tls_builder.TlsBuilder.build_tls_config","title":"build_tls_config","text":"<pre><code>build_tls_config()\n</code></pre> <p>Build a PAC TlsConfig from the builder state.</p> <p>Returns:</p> Type Description <code>Any</code> <p>A configured <code>aerospike_async.TlsConfig</code>, or <code>None</code> when</p> <code>Any</code> <p>no CA file has been set (TLS cannot be configured without one).</p>"},{"location":"api/transactional-session/","title":"TransactionalSession","text":""},{"location":"api/transactional-session/#aerospike_fluent.aio.transactional_session.TransactionalSession","title":"TransactionalSession","text":"<pre><code>TransactionalSession(client)\n</code></pre> <p>Session for transactional operations.</p> <p>This session manages transaction state and ensures all operations within the session are part of the same transaction. When the session exits, the transaction is automatically committed (or rolled back on error).</p> <p>Note: Full transaction support depends on Aerospike server capabilities. This is a placeholder for future transaction support.</p>"},{"location":"api/transactional-session/#aerospike_fluent.aio.transactional_session.TransactionalSession.commit","title":"commit  <code>async</code>","text":"<pre><code>commit()\n</code></pre> <p>Commit the transaction.</p> <p>Note: Currently a no-op. Will be implemented when transaction support is available in the underlying client.</p>"},{"location":"api/transactional-session/#aerospike_fluent.aio.transactional_session.TransactionalSession.rollback","title":"rollback  <code>async</code>","text":"<pre><code>rollback()\n</code></pre> <p>Rollback the transaction.</p> <p>Note: Currently a no-op. Will be implemented when transaction support is available in the underlying client.</p>"},{"location":"api/transactional-session/#aerospike_fluent.aio.transactional_session.TransactionalSession.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__()\n</code></pre> <p>Async context manager entry.</p>"},{"location":"api/transactional-session/#aerospike_fluent.aio.transactional_session.TransactionalSession.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(exc_type, exc_val, exc_tb)\n</code></pre> <p>Async context manager exit.</p>"},{"location":"api/udf/","title":"UDF Builders","text":"<p>Foreground UDF execution builders (single-key, batch, and chained operations).</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfFunctionBuilder","title":"UdfFunctionBuilder","text":"<pre><code>UdfFunctionBuilder(qb)\n</code></pre> <p>First step of foreground UDF chaining: choose package and Lua function name.</p> <p>Produced by :meth:<code>~aerospike_fluent.aio.session.Session.execute_udf</code> or :meth:<code>UdfBuilder.execute_udf</code>. Call :meth:<code>function</code> before :meth:<code>UdfBuilder.passing</code> or :meth:<code>execute</code>.</p> <p>Example::     stream = await (         session.execute_udf(key)             .function(\"my_module\", \"my_func\")             .execute()     )</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfFunctionBuilder.function","title":"function","text":"<pre><code>function(package, function_name)\n</code></pre> <p>Select the registered module and function to invoke.</p> <p>Parameters:</p> Name Type Description Default <code>package</code> <code>str</code> <p>Server-side module name (no <code>.lua</code> suffix).</p> required <code>function_name</code> <code>str</code> <p>Lua function symbol exported by the module.</p> required <p>Returns:</p> Type Description <code>UdfBuilder</code> <p>class:<code>UdfBuilder</code> for arguments and execution.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>package</code> or <code>function_name</code> is empty.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder","title":"UdfBuilder","text":"<pre><code>UdfBuilder(qb)\n</code></pre> <p>Supply UDF arguments, optional filter, then execute or chain another operation.</p> <p>After :meth:<code>UdfFunctionBuilder.function</code>, call :meth:<code>passing</code> with values passed to Lua (after the implicit record argument). Use :meth:<code>execute_udf</code> to append another UDF segment, or :meth:<code>query</code> / write verbs to switch operation type. Await :meth:<code>execute</code> to run the accumulated chain.</p> <p>Example::     stream = await (         session.execute_udf(key)             .function(\"my_pkg\", \"my_func\")             .passing(1, \"x\")             .execute()     )</p> See Also <p>:meth:<code>~aerospike_fluent.aio.session.Session.execute_udf</code>: Entry point.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.passing","title":"passing","text":"<pre><code>passing(*args)\n</code></pre> <p>Set positional arguments forwarded to the Lua function.</p> <p>The Aerospike server automatically passes the record as the first argument to the UDF; values provided here follow it.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>Any</code> <p>Values serialized by the async client (scalars, lists, maps, bytes).</p> <code>()</code> <p>Returns:</p> Type Description <code>UdfBuilder</code> <p>This builder for chaining.</p> <p>Example::     builder.passing(\"binName\", 42)</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.where","title":"where","text":"<pre><code>where(expression: str) -&gt; UdfBuilder\n</code></pre><pre><code>where(expression: FilterExpression) -&gt; UdfBuilder\n</code></pre> <pre><code>where(expression)\n</code></pre> <p>Apply a filter expression so the UDF runs only when the predicate matches.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or <code>FilterExpression</code>.</p> required <p>Returns:</p> Type Description <code>UdfBuilder</code> <p>This builder for chaining.</p> See Also <p>:meth:<code>QueryBuilder.where</code>: Same DSL for reads.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>For batch UDF, emit a row per requested key (including not-found).</p> <p>Returns:</p> Type Description <code>UdfBuilder</code> <p>This builder for chaining.</p> See Also <p>:meth:<code>QueryBuilder.respond_all_keys</code>: Same flag for reads.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.execute_udf","title":"execute_udf","text":"<pre><code>execute_udf(*keys)\n</code></pre> <p>Finalize this UDF operation and start another on keys.</p> <p>Parameters:</p> Name Type Description Default <code>*keys</code> <code>Key</code> <p>One or more keys for the next UDF segment.</p> <code>()</code> <p>Returns:</p> Type Description <code>UdfFunctionBuilder</code> <p>A new :class:<code>UdfFunctionBuilder</code> to call :meth:<code>function</code> again.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no keys are provided.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.query","title":"query","text":"<pre><code>query(arg1, *more_keys)\n</code></pre> <p>Close the UDF operation and begin a read :class:<code>QueryBuilder</code> segment.</p> <p>Parameters:</p> Name Type Description Default <code>arg1</code> <code>Union[Key, List[Key]]</code> <p>One key or a list of keys.</p> required <code>*more_keys</code> <code>Key</code> <p>Additional keys when <code>arg1</code> is a single key.</p> <code>()</code> <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>class:<code>QueryBuilder</code> for chaining.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.upsert","title":"upsert","text":"<pre><code>upsert(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF operation and start an upsert :class:<code>WriteSegmentBuilder</code>.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>class:<code>WriteSegmentBuilder</code> for chaining.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.insert","title":"insert","text":"<pre><code>insert(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF operation and start an insert-only write segment.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>class:<code>WriteSegmentBuilder</code> for chaining.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.update","title":"update","text":"<pre><code>update(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF operation and start an update-only write segment.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>class:<code>WriteSegmentBuilder</code> for chaining.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.replace","title":"replace","text":"<pre><code>replace(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF operation and start a replace write segment.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>class:<code>WriteSegmentBuilder</code> for chaining.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.replace_if_exists","title":"replace_if_exists","text":"<pre><code>replace_if_exists(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF operation and start a replace-if-exists segment.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>class:<code>WriteSegmentBuilder</code> for chaining.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.delete","title":"delete","text":"<pre><code>delete(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF operation and start a delete segment.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>class:<code>WriteSegmentBuilder</code> for chaining.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.touch","title":"touch","text":"<pre><code>touch(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF operation and start a touch segment.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>class:<code>WriteSegmentBuilder</code> for chaining.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.exists","title":"exists","text":"<pre><code>exists(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF operation and start an exists-check segment.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>class:<code>WriteSegmentBuilder</code> for chaining.</p>"},{"location":"api/udf/#aerospike_fluent.aio.operations.udf.UdfBuilder.execute","title":"execute  <code>async</code>","text":"<pre><code>execute(on_error=None)\n</code></pre> <p>Run the current builder state and return a :class:<code>~aerospike_fluent.record_stream.RecordStream</code>.</p> <p>Requires :meth:<code>function</code> to have been called for the pending UDF operation.</p> <p>Parameters:</p> Name Type Description Default <code>on_error</code> <code>OnError | None</code> <p>Same as :meth:<code>QueryBuilder.execute</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>RecordStream</code> <p>Stream of per-key results and optional <code>udf_result</code> fields.</p> <p>Example::     stream = await (         session.execute_udf(k1, k2)             .function(\"pkg\", \"fn\")             .execute()     )</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no UDF function was selected before execute.</p>"},{"location":"api/write-segment/","title":"WriteSegmentBuilder","text":""},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder","title":"WriteSegmentBuilder","text":"<pre><code>WriteSegmentBuilder(qb)\n</code></pre> <p>               Bases: <code>_WriteVerbs</code></p> <p>Accumulate scalar and CDT writes for the current operation's key(s).</p> <p>Obtained from :class:<code>QueryBuilder</code> after a write verb or from :class:<code>WriteBinBuilder</code> when chaining. Call :meth:<code>put</code>, :meth:<code>bin</code>, expression helpers, optional :meth:<code>where</code> / TTL / generation guards, then :meth:<code>execute</code> on this object or transition with :meth:<code>query</code> / another write verb on the mixin.</p> Example <p>Upsert two bins, then read the stream of results::</p> <pre><code>stream = await (\n    session.upsert(key)\n        .put({\"name\": \"Ada\", \"score\": 100})\n        .execute()\n)\n</code></pre> See Also <p>:meth:<code>QueryBuilder.execute</code>: Runs all chained operations.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.bin","title":"bin","text":"<pre><code>bin(bin_name)\n</code></pre> <p>Start a bin-level write operation.</p> <p>Parameters:</p> Name Type Description Default <code>bin_name</code> <code>str</code> <p>The bin to operate on.</p> required <p>Returns:</p> Type Description <code>WriteBinBuilder</code> <p>A WriteBinBuilder for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.put","title":"put","text":"<pre><code>put(bins)\n</code></pre> <p>Apply <code>Operation.put</code> for each bin in the mapping.</p> <p>Parameters:</p> Name Type Description Default <code>bins</code> <code>dict</code> <p>Map of bin name to value.</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>This segment for chaining.</p> <p>Example::     await session.upsert(key).put({\"email\": \"a@b.com\", \"age\": 30}).execute()</p> See Also <p>:meth:<code>bin</code>: Per-bin CDT or scalar follow-ups.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.set_bins","title":"set_bins","text":"<pre><code>set_bins(bins)\n</code></pre> <p>Alias for :meth:<code>put</code>.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.add_operation","title":"add_operation","text":"<pre><code>add_operation(op)\n</code></pre> <p>Append an operation (used by CDT action builders).</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.set_to","title":"set_to","text":"<pre><code>set_to(bin_name, value)\n</code></pre> <p>Set a bin to value.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.add","title":"add","text":"<pre><code>add(bin_name, value)\n</code></pre> <p>Add a numeric value to a bin.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.increment_by","title":"increment_by","text":"<pre><code>increment_by(bin_name, value)\n</code></pre> <p>Alias for :meth:<code>add</code>.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.get","title":"get","text":"<pre><code>get(bin_name)\n</code></pre> <p>Read a bin value back within a write operate.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.append","title":"append","text":"<pre><code>append(bin_name, value)\n</code></pre> <p>Append a string to a bin.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.prepend","title":"prepend","text":"<pre><code>prepend(bin_name, value)\n</code></pre> <p>Prepend a string to a bin.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.remove_bin","title":"remove_bin","text":"<pre><code>remove_bin(bin_name)\n</code></pre> <p>Delete a bin from the record.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.select_from","title":"select_from","text":"<pre><code>select_from(\n    bin_name, expression, *, ignore_eval_failure=False\n)\n</code></pre> <p>Read a computed value into a bin using a DSL expression.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.insert_from","title":"insert_from","text":"<pre><code>insert_from(\n    bin_name,\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result only if bin does not already exist.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.update_from","title":"update_from","text":"<pre><code>update_from(\n    bin_name,\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result only if bin already exists.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.upsert_from","title":"upsert_from","text":"<pre><code>upsert_from(\n    bin_name,\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result, creating or overwriting the bin.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.query","title":"query","text":"<pre><code>query(arg1, *more_keys)\n</code></pre> <p>Finalize current write segment and start a read segment.</p> <p>Parameters:</p> Name Type Description Default <code>arg1</code> <code>Union[Key, List[Key]]</code> <p>A single Key or List[Key].</p> required <code>*more_keys</code> <code>Key</code> <p>Additional keys (varargs).</p> <code>()</code> <p>Returns:</p> Type Description <code>QueryBuilder</code> <p>The parent QueryBuilder for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.where","title":"where","text":"<pre><code>where(expression: str) -&gt; WriteSegmentBuilder\n</code></pre><pre><code>where(expression: FilterExpression) -&gt; WriteSegmentBuilder\n</code></pre> <pre><code>where(expression)\n</code></pre> <p>Set a filter expression on the current write segment.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or pre-built FilterExpression.</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.expire_record_after_seconds","title":"expire_record_after_seconds","text":"<pre><code>expire_record_after_seconds(seconds)\n</code></pre> <p>Set the TTL on the current write segment.</p> <p>Parameters:</p> Name Type Description Default <code>seconds</code> <code>int</code> <p>Time-to-live in seconds (must be &gt; 0).</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If seconds is &lt;= 0.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.never_expire","title":"never_expire","text":"<pre><code>never_expire()\n</code></pre> <p>Set this record to never expire (TTL = -1).</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.with_no_change_in_expiration","title":"with_no_change_in_expiration","text":"<pre><code>with_no_change_in_expiration()\n</code></pre> <p>Preserve the record's existing TTL (TTL = -2).</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.expiry_from_server_default","title":"expiry_from_server_default","text":"<pre><code>expiry_from_server_default()\n</code></pre> <p>Use the namespace's default TTL for this record (TTL = 0).</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.ensure_generation_is","title":"ensure_generation_is","text":"<pre><code>ensure_generation_is(generation)\n</code></pre> <p>Set expected generation for optimistic locking on the current segment.</p> <p>Parameters:</p> Name Type Description Default <code>generation</code> <code>int</code> <p>The expected generation number (must be &gt; 0).</p> required <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If generation is &lt;= 0.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.durably_delete","title":"durably_delete","text":"<pre><code>durably_delete()\n</code></pre> <p>Enable durable delete on the current segment.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>Include results for missing keys in the stream.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.fail_on_filtered_out","title":"fail_on_filtered_out","text":"<pre><code>fail_on_filtered_out()\n</code></pre> <p>Mark filtered-out records with <code>FILTERED_OUT</code> result code.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.replace_only","title":"replace_only","text":"<pre><code>replace_only()\n</code></pre> <p>Change the current segment to replace-if-exists semantics.</p> <p>The record must already exist; the operation fails with <code>KEY_NOT_FOUND_ERROR</code> if it does not.  All existing bins are removed and only the bins specified in this segment are written.</p> <p>Returns:</p> Type Description <code>WriteSegmentBuilder</code> <p>self for method chaining.</p>"},{"location":"api/write-segment/#aerospike_fluent.aio.operations.query.WriteSegmentBuilder.execute","title":"execute  <code>async</code>","text":"<pre><code>execute(on_error=None)\n</code></pre> <p>Run the parent :class:<code>QueryBuilder</code> stack (same as <code>self._qb.execute</code>).</p> <p>Parameters:</p> Name Type Description Default <code>on_error</code> <code>OnError | None</code> <p>Optional :class:<code>~aerospike_fluent.error_strategy.ErrorStrategy</code> or error callback; see :meth:<code>QueryBuilder.execute</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>RecordStream</code> <p>class:<code>~aerospike_fluent.record_stream.RecordStream</code> of results.</p> <p>Example::     stream = await session.upsert(key).put({\"x\": 1}).execute()     await stream.first_or_raise()</p> <p>Raises:</p> Type Description <code>Same as </code> <p>meth:<code>QueryBuilder.execute</code>.</p>"},{"location":"api/sync/background/","title":"Sync Background Task Builders","text":"<p>Synchronous background dataset task builders (delegate to <code>aio.background</code>).</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundWriteBinBuilder","title":"SyncBackgroundWriteBinBuilder","text":"<pre><code>SyncBackgroundWriteBinBuilder(inner, loop_manager)\n</code></pre> <p>Per-bin scalar write inside a background operation (sync).</p> See Also <p>:class:<code>~aerospike_fluent.aio.background.BackgroundWriteBinBuilder</code></p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundWriteBinBuilder.set_to","title":"set_to","text":"<pre><code>set_to(value)\n</code></pre> <p>Set the bin to value (sync wrapper).</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundWriteBinBuilder.add","title":"add","text":"<pre><code>add(value)\n</code></pre> <p>Numeric increment (sync wrapper).</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundOperationBuilder","title":"SyncBackgroundOperationBuilder","text":"<pre><code>SyncBackgroundOperationBuilder(inner, loop_manager)\n</code></pre> <p>Configure a background update/delete/touch job (sync).</p> See Also <p>:class:<code>~aerospike_fluent.aio.background.BackgroundOperationBuilder</code></p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundOperationBuilder.where","title":"where","text":"<pre><code>where(expression: str) -&gt; SyncBackgroundOperationBuilder\n</code></pre><pre><code>where(\n    expression: FilterExpression,\n) -&gt; SyncBackgroundOperationBuilder\n</code></pre> <pre><code>where(expression)\n</code></pre> <p>Restrict the scan with a DSL or filter predicate.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundOperationBuilder.expire_record_after_seconds","title":"expire_record_after_seconds","text":"<pre><code>expire_record_after_seconds(seconds)\n</code></pre> <p>Set record TTL for the background job.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundOperationBuilder.records_per_second","title":"records_per_second","text":"<pre><code>records_per_second(rps)\n</code></pre> <p>Store a throttle hint.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundOperationBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>Unsupported for background tasks.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundOperationBuilder.execute","title":"execute","text":"<pre><code>execute()\n</code></pre> <p>Submit the job and return a task handle (blocks until accepted).</p> See Also <p>:meth:<code>~aerospike_fluent.aio.background.BackgroundOperationBuilder.execute</code></p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundUdfFunctionBuilder","title":"SyncBackgroundUdfFunctionBuilder","text":"<pre><code>SyncBackgroundUdfFunctionBuilder(inner, loop_manager)\n</code></pre> <p>Select UDF package/function for a background dataset run (sync).</p> See Also <p>:class:<code>~aerospike_fluent.aio.background.BackgroundUdfFunctionBuilder</code></p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundUdfFunctionBuilder.function","title":"function","text":"<pre><code>function(package_name, function_name)\n</code></pre> <p>Select the UDF package and Lua function.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundUdfBuilder","title":"SyncBackgroundUdfBuilder","text":"<pre><code>SyncBackgroundUdfBuilder(inner, loop_manager)\n</code></pre> <p>Arguments, filters, and throttle for background UDF execution (sync).</p> See Also <p>:class:<code>~aerospike_fluent.aio.background.BackgroundUdfBuilder</code></p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundUdfBuilder.where","title":"where","text":"<pre><code>where(expression: str) -&gt; SyncBackgroundUdfBuilder\n</code></pre><pre><code>where(\n    expression: FilterExpression,\n) -&gt; SyncBackgroundUdfBuilder\n</code></pre> <pre><code>where(expression)\n</code></pre> <p>Optional predicate limiting which records invoke the UDF.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundUdfBuilder.fail_on_filtered_out","title":"fail_on_filtered_out","text":"<pre><code>fail_on_filtered_out()\n</code></pre> <p>Unsupported for background tasks.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundUdfBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>Unsupported for background tasks.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundUdfBuilder.execute","title":"execute","text":"<pre><code>execute()\n</code></pre> <p>Submit the background UDF job (blocks until accepted).</p> See Also <p>:meth:<code>~aerospike_fluent.aio.background.BackgroundUdfBuilder.execute</code></p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundTaskSession","title":"SyncBackgroundTaskSession","text":"<pre><code>SyncBackgroundTaskSession(inner, loop_manager)\n</code></pre> <p>Sync entry for server-side dataset background operations.</p> <p>Obtained from :meth:<code>~aerospike_fluent.sync.session.SyncSession.background_task</code>. Each method returns a sync builder that mirrors :class:<code>~aerospike_fluent.aio.background.BackgroundTaskSession</code>.</p> See Also <p>:class:<code>~aerospike_fluent.aio.background.BackgroundTaskSession</code></p> <p>Examples:</p> <p>session.background_task().update(dataset).bin(\"x\").set_to(1).execute() session.background_task().execute_udf(dataset).function(\"pkg\", \"fn\").execute()</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundTaskSession.delete","title":"delete","text":"<pre><code>delete(dataset)\n</code></pre> <p>Start a background delete over dataset.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundTaskSession.touch","title":"touch","text":"<pre><code>touch(dataset)\n</code></pre> <p>Start a background touch (TTL refresh) over dataset.</p>"},{"location":"api/sync/background/#aerospike_fluent.sync.background.SyncBackgroundTaskSession.execute_udf","title":"execute_udf","text":"<pre><code>execute_udf(dataset)\n</code></pre> <p>Start a background UDF over dataset.</p>"},{"location":"api/sync/client/","title":"SyncFluentClient","text":""},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient","title":"SyncFluentClient","text":"<pre><code>SyncFluentClient(seeds, policy=None)\n</code></pre> <p>Connect to Aerospike and run the fluent API without <code>async</code>/<code>await</code>.</p> <p>Method shapes match :class:<code>~aerospike_fluent.aio.client.FluentClient</code>; return types are synchronous counterparts (for example :class:<code>~aerospike_fluent.sync.operations.query.SyncQueryBuilder</code>, :class:<code>~aerospike_fluent.sync.session.SyncSession</code>).</p> Example <pre><code>with SyncFluentClient(\"localhost:3000\") as client:\n    for row in client.query(\n        namespace=\"test\",\n        set_name=\"users\"\n    ).execute():\n        if row.record:\n            print(row.record.bins)\n</code></pre> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If constructed or used while an asyncio event loop is already running in the thread (use :class:<code>~aerospike_fluent.aio.client.FluentClient</code> instead).</p> See Also <p>:class:<code>~aerospike_fluent.aio.client.FluentClient</code>: Native async client. :meth:<code>create_session</code>: Session-scoped :class:<code>~aerospike_fluent.policy.behavior.Behavior</code>.</p> <p>Initialize a SyncFluentClient.</p> <p>Parameters:</p> Name Type Description Default <code>seeds</code> <code>str</code> <p>Aerospike cluster seed addresses (e.g., \"localhost:3000\")</p> required <code>policy</code> <code>Optional[ClientPolicy]</code> <p>Optional client policy. If None, a default policy is used.</p> <code>None</code>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.is_connected","title":"is_connected  <code>property</code>","text":"<pre><code>is_connected\n</code></pre> <p>Check if the client is connected.</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.connect","title":"connect","text":"<pre><code>connect()\n</code></pre> <p>Connect to the Aerospike cluster synchronously.</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Close the connection to the Aerospike cluster.</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.__enter__","title":"__enter__","text":"<pre><code>__enter__()\n</code></pre> <p>Context manager entry.</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.__exit__","title":"__exit__","text":"<pre><code>__exit__(exc_type, exc_val, exc_tb)\n</code></pre> <p>Context manager exit.</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.query","title":"query","text":"<pre><code>query(dataset: DataSet)\n</code></pre><pre><code>query(key: Key)\n</code></pre><pre><code>query(keys: List[Key])\n</code></pre><pre><code>query(*keys: Key)\n</code></pre><pre><code>query(namespace: str, set_name: str)\n</code></pre> <pre><code>query(\n    arg1=None,\n    arg2=None,\n    *keys,\n    namespace=None,\n    set_name=None,\n    dataset=None,\n    key=None,\n    keys_list=None,\n    behavior=None,\n)\n</code></pre> <p>Create a query builder (synchronous).</p> <p>Supports the same calling styles as :meth:<code>FluentClient.query &lt;aerospike_fluent.aio.client.FluentClient.query&gt;</code>, including multi-key varargs. Returns a wrapper that executes queries synchronously.</p> <p>Parameters:</p> Name Type Description Default <code>arg1</code> <code>Optional[Union[DataSet, Key, List[Key], str]]</code> <p>Optional first positional (DataSet, key, list of keys, or namespace string when paired with <code>arg2</code> as set name).</p> <code>None</code> <code>arg2</code> <code>Optional[str]</code> <p>When <code>arg1</code> is a namespace, the set name; otherwise may be a second key when passing multiple keys positionally.</p> <code>None</code> <code>*keys</code> <code>Key</code> <p>Additional keys when the first positional argument is a key.</p> <code>()</code> <code>namespace</code> <code>Optional[str]</code> <p>Keyword namespace (with <code>set_name</code>).</p> <code>None</code> <code>set_name</code> <code>Optional[str]</code> <p>Keyword set name (with <code>namespace</code>).</p> <code>None</code> <code>dataset</code> <code>Optional[DataSet]</code> <p>Keyword :class:<code>~aerospike_fluent.dataset.DataSet</code>.</p> <code>None</code> <code>key</code> <code>Optional[Key]</code> <p>Keyword single key.</p> <code>None</code> <code>keys_list</code> <code>Optional[List[Key]]</code> <p>Keyword list of keys for batch read.</p> <code>None</code> <code>behavior</code> <code>Optional[Behavior]</code> <p>Optional :class:<code>~aerospike_fluent.policy.behavior.Behavior</code> for this builder (same semantics as <code>FluentClient.query</code>).</p> <code>None</code> <p>Returns:</p> Name Type Description <code>SyncQueryBuilder</code> <code>SyncQueryBuilder</code> <p>Configured for the requested namespace, set, and/or keys.</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.index","title":"index","text":"<pre><code>index(*, dataset: DataSet)\n</code></pre><pre><code>index(namespace: str, set_name: str)\n</code></pre> <pre><code>index(namespace=None, set_name=None, *, dataset=None)\n</code></pre> <p>Create a secondary-index builder (synchronous).</p> <p>Same arguments as :meth:<code>~aerospike_fluent.aio.client.FluentClient.index</code>. <code>create()</code> / <code>drop()</code> run on the client's event loop.</p> <p>Returns:</p> Name Type Description <code>SyncIndexBuilder</code> <code>SyncIndexBuilder</code> <p>Configured for the requested namespace and set.</p> See Also <p>:class:<code>~aerospike_fluent.aio.operations.index.IndexBuilder</code>: Async implementation.</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.truncate","title":"truncate","text":"<pre><code>truncate(dataset, before_nanos=None)\n</code></pre> <p>Truncate (delete all records) from a set (synchronous).</p> <p>This method deletes all records in the specified set. This operation cannot be undone.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>DataSet</code> <p>The DataSet to truncate.</p> required <code>before_nanos</code> <code>Optional[int]</code> <p>Optional timestamp in nanoseconds. Only records with          last update time (LUT) less than this value will be          truncated. If None, all records in the set are truncated.</p> <code>None</code> Example <pre><code>users = DataSet.of(\"test\", \"users\")\nclient.truncate(users)\n\n# Truncate only records older than a specific time\nimport time\ncutoff_time = time.time_ns() - (24 * 60 * 60 * 10**9)  # 24 hours ago\nclient.truncate(users, before_nanos=cutoff_time)\n</code></pre>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.register_udf","title":"register_udf","text":"<pre><code>register_udf(\n    body, server_path, language=LUA, *, policy=None\n)\n</code></pre> <p>Register a UDF module from bytes (synchronous).</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.register_udf_from_file","title":"register_udf_from_file","text":"<pre><code>register_udf_from_file(\n    client_path, server_path, language=LUA, *, policy=None\n)\n</code></pre> <p>Register a UDF module from a local file (synchronous).</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.remove_udf","title":"remove_udf","text":"<pre><code>remove_udf(server_path, *, policy=None)\n</code></pre> <p>Remove a UDF module from the cluster (synchronous).</p>"},{"location":"api/sync/client/#aerospike_fluent.sync.client.SyncFluentClient.create_session","title":"create_session","text":"<pre><code>create_session(behavior=None)\n</code></pre> <p>Create a session with the specified behavior (synchronous).</p> <p>A session represents a logical connection to the cluster with specific behavior settings that control how operations are performed (timeouts, retry policies, consistency levels, etc.).</p> <p>Parameters:</p> Name Type Description Default <code>behavior</code> <code>Optional[Behavior]</code> <p>The behavior configuration for the session.      If None, uses Behavior.DEFAULT.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>'SyncSession'</code> <p>class:<code>~aerospike_fluent.sync.session.SyncSession</code> sharing this</p> <code>'SyncSession'</code> <p>client's event-loop manager and applying <code>behavior</code> to operations.</p> Example <pre><code>session = client.create_session()\nfrom datetime import timedelta\nfast = Behavior.DEFAULT.derive_with_changes(\n    name=\"fast\",\n    total_timeout=timedelta(seconds=5),\n)\nsession = client.create_session(fast)\n</code></pre> See Also <p>:meth:<code>~aerospike_fluent.aio.client.FluentClient.create_session</code>:     Async equivalent.</p>"},{"location":"api/sync/cluster-definition/","title":"Sync ClusterDefinition &amp; Host","text":"<p>ClusterDefinition - Builder for configuring Aerospike cluster connections (sync version).</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.Host","title":"Host","text":"<pre><code>Host(name, port, tls_name=None)\n</code></pre> <p>Represents an Aerospike server host.</p> <p>Initialize a Host.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Hostname or IP address</p> required <code>port</code> <code>int</code> <p>Port number</p> required <code>tls_name</code> <code>Optional[str]</code> <p>Optional TLS name for certificate validation</p> <code>None</code>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.Host.of","title":"of  <code>staticmethod</code>","text":"<pre><code>of(name, port)\n</code></pre> <p>Create a Host instance.</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.Host.parse_hosts","title":"parse_hosts  <code>staticmethod</code>","text":"<pre><code>parse_hosts(host_string, default_port)\n</code></pre> <p>Parse a host string into a list of Host objects.</p> <p>Format: \"host1:port1,host2:port2\" or \"host1,host2\" (uses default_port)</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition","title":"ClusterDefinition","text":"<pre><code>ClusterDefinition(hostname=None, port=None, hosts=None)\n</code></pre> <p>Sync builder for seeds, auth, TLS, and validation; :meth:<code>connect</code> returns :class:<code>Cluster</code>.</p> <p>Mirrors :class:<code>~aerospike_fluent.aio.cluster_definition.ClusterDefinition</code> with blocking :meth:<code>connect</code> and context-manager support on :class:<code>~aerospike_fluent.sync.cluster.Cluster</code>.</p> Example <pre><code>with (\n    ClusterDefinition(\"localhost\", 3100)\n    .with_native_credentials(\"username\", \"password\")\n    .connect()\n) as cluster:\n    session = cluster.create_session(Behavior.DEFAULT)\n</code></pre> See Also <p>:class:<code>~aerospike_fluent.aio.cluster_definition.ClusterDefinition</code></p> <p>Create a cluster definition.</p> <p>Parameters:</p> Name Type Description Default <code>hostname</code> <code>Optional[str]</code> <p>Hostname or IP address (if single host)</p> <code>None</code> <code>port</code> <code>Optional[int]</code> <p>Port number (if single host)</p> <code>None</code> <code>hosts</code> <code>Optional[Union[List[Host], tuple[Host, ...]]]</code> <p>List of Host objects (if multiple hosts)</p> <code>None</code> <p>Examples:</p> <p>ClusterDefinition(\"localhost\", 3000) ClusterDefinition(hosts=[Host.of(\"host1\", 3000), Host.of(\"host2\", 3000)])</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.auth_mode","title":"auth_mode  <code>property</code>","text":"<pre><code>auth_mode\n</code></pre> <p>The current authentication mode.</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.with_native_credentials","title":"with_native_credentials","text":"<pre><code>with_native_credentials(user_name, password)\n</code></pre> <p>Sets authentication credentials using Aerospike's internal authentication.</p> <p>Hashed password is stored on the server. Pass empty strings for both parameters to disable authentication.</p> <p>Parameters:</p> Name Type Description Default <code>user_name</code> <code>str</code> <p>The username for authentication</p> required <code>password</code> <code>str</code> <p>The password for authentication</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.with_external_credentials","title":"with_external_credentials","text":"<pre><code>with_external_credentials(user_name, password)\n</code></pre> <p>Sets authentication credentials using external authentication (e.g. LDAP).</p> <p>External authentication is configured on the server. If TLS is configured, the clear password is sent on node login via TLS. Raises an error at connect time if TLS is not configured.</p> <p>Parameters:</p> Name Type Description Default <code>user_name</code> <code>str</code> <p>The username for authentication</p> required <code>password</code> <code>str</code> <p>The password for authentication</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.with_certificate_credentials","title":"with_certificate_credentials","text":"<pre><code>with_certificate_credentials()\n</code></pre> <p>Configures certificate-based (PKI) authentication.</p> <p>Uses client certificates instead of username/password credentials. Automatically enables TLS if not already configured. Requires server version 5.7.0+.</p> <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If any host is missing a TLS name</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.validate_cluster_name_is","title":"validate_cluster_name_is","text":"<pre><code>validate_cluster_name_is(cluster_name)\n</code></pre> <p>Validates that the cluster name matches the expected value.</p> <p>This enables cluster name validation to ensure the client connects to the expected cluster. If the actual cluster name doesn't match, the connection will fail.</p> <p>Parameters:</p> Name Type Description Default <code>cluster_name</code> <code>str</code> <p>The expected cluster name to validate against</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.preferring_racks","title":"preferring_racks","text":"<pre><code>preferring_racks(*racks)\n</code></pre> <p>Sets preferred racks for rack-aware operations.</p> <p>This enables rack awareness and specifies which racks should be preferred for read operations. Rack awareness helps improve performance by reading from local racks when possible.</p> <p>Parameters:</p> Name Type Description Default <code>*racks</code> <code>int</code> <p>The rack IDs to prefer, in order of preference</p> <code>()</code> <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.using_services_alternate","title":"using_services_alternate","text":"<pre><code>using_services_alternate()\n</code></pre> <p>Enables the use of alternate services for cluster discovery.</p> <p>When enabled, the client will use alternate service endpoints for cluster discovery, which can be useful in certain network configurations or when using service mesh solutions.</p> <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.fail_if_not_connected","title":"fail_if_not_connected","text":"<pre><code>fail_if_not_connected(fail)\n</code></pre> <p>Controls whether <code>connect()</code> raises if the cluster is unreachable.</p> <p>If <code>True</code> (the default), <code>connect()</code> raises a <code>ConnectionError</code> when all seed connections fail or a seed connects but none of its peers are reachable.</p> <p>If <code>False</code>, a partial cluster is created and the client will automatically connect to the remaining nodes when they become available.</p> <p>Parameters:</p> Name Type Description Default <code>fail</code> <code>bool</code> <p>Whether to raise on connection failure.</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.with_ip_map","title":"with_ip_map","text":"<pre><code>with_ip_map(ip_map)\n</code></pre> <p>Sets an IP address translation table for cluster node discovery.</p> <p>Used when clients from different networks need different IP addresses to reach the same server nodes (e.g. inside vs. outside a VPN or NAT). The key is the IP address returned from server info requests; the value is the actual IP address the client should connect to.</p> <p>Consider using <code>using_services_alternate()</code> instead, which lets the server handle address translation without client-side configuration.</p> <p>Parameters:</p> Name Type Description Default <code>ip_map</code> <code>dict[str, str]</code> <p>Mapping of server-reported IPs to actual connection IPs</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.with_system_settings","title":"with_system_settings","text":"<pre><code>with_system_settings(settings)\n</code></pre> <p>Set cluster-wide system settings (connection pool, tend interval, etc.).</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>SystemSettings</code> <p>The SystemSettings to apply.</p> required <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>This ClusterDefinition for method chaining.</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.with_tls_config_of","title":"with_tls_config_of","text":"<pre><code>with_tls_config_of()\n</code></pre> <p>Begins TLS configuration using a fluent builder pattern.</p> <p>This method returns a TlsBuilder that allows you to configure various TLS settings such as TLS name, CA file, protocols, ciphers, and other TLS-specific options. Call done() on the TlsBuilder to return to this ClusterDefinition for further configuration.</p> <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>A TlsBuilder for configuring TLS settings</p>"},{"location":"api/sync/cluster-definition/#aerospike_fluent.sync.cluster_definition.ClusterDefinition.connect","title":"connect","text":"<pre><code>connect()\n</code></pre> <p>Establishes a connection to the Aerospike cluster (synchronously).</p> <p>This method creates and returns a Cluster instance using the configured parameters. The returned Cluster should be closed when no longer needed to properly release resources.</p> Example with context manager <pre><code>with ClusterDefinition(\"localhost\", 3100).connect() as cluster:\n    session = cluster.create_session(Behavior.DEFAULT)\n    # Use the session...\n</code></pre> <p>Returns:</p> Type Description <code>Cluster</code> <p>A connected Cluster instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If PKI auth is configured but hosts are missing TLS names</p> <code>ConnectionError</code> <p>If <code>fail_if_not_connected</code> is True (default) and the cluster is unreachable</p>"},{"location":"api/sync/cluster/","title":"Sync Cluster","text":""},{"location":"api/sync/cluster/#aerospike_fluent.sync.cluster.Cluster","title":"Cluster","text":"<pre><code>Cluster(fluent_client)\n</code></pre> <p>Synchronous cluster handle from <code>sync.cluster_definition.ClusterDefinition.connect</code>.</p> <p>Mirrors :class:<code>~aerospike_fluent.aio.cluster.Cluster</code> but uses :class:<code>~aerospike_fluent.sync.client.SyncFluentClient</code> and :class:<code>~aerospike_fluent.sync.session.SyncSession</code>.</p> Example <pre><code>with ClusterDefinition(\"localhost\", 3100).connect() as cluster:\n    session = cluster.create_session(Behavior.DEFAULT)\n</code></pre> See Also <p>:class:<code>~aerospike_fluent.aio.cluster.Cluster</code></p> <p>Initialize a Cluster instance.</p> <p>Parameters:</p> Name Type Description Default <code>fluent_client</code> <code>SyncFluentClient</code> <p>The underlying SyncFluentClient instance</p> required Note <p>This should not be called directly. Use ClusterDefinition.connect() instead.</p>"},{"location":"api/sync/cluster/#aerospike_fluent.sync.cluster.Cluster.__enter__","title":"__enter__","text":"<pre><code>__enter__()\n</code></pre> <p>Context manager entry.</p>"},{"location":"api/sync/cluster/#aerospike_fluent.sync.cluster.Cluster.__exit__","title":"__exit__","text":"<pre><code>__exit__(exc_type, exc_val, exc_tb)\n</code></pre> <p>Context manager exit.</p>"},{"location":"api/sync/cluster/#aerospike_fluent.sync.cluster.Cluster.create_session","title":"create_session","text":"<pre><code>create_session(behavior=None)\n</code></pre> <p>Return a :class:<code>~aerospike_fluent.sync.session.SyncSession</code> for this cluster.</p> <p>A session represents a logical connection to the cluster with specific behavior settings that control how operations are performed (timeouts, retry policies, consistency levels, etc.). Args:     behavior: Defaults to :attr:<code>~aerospike_fluent.policy.behavior.Behavior.DEFAULT</code>.</p> <p>Returns:</p> Type Description <code>SyncSession</code> <p>A new sync session bound to this cluster's client.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.cluster.Cluster.create_session</code></p>"},{"location":"api/sync/cluster/#aerospike_fluent.sync.cluster.Cluster.create_transactional_session","title":"create_transactional_session","text":"<pre><code>create_transactional_session(behavior=None)\n</code></pre> <p>Creates a new transactional session with the specified behavior.</p> <p>Parameters:</p> Name Type Description Default <code>behavior</code> <code>Optional[Behavior]</code> <p>The behavior configuration for the session. If None, uses Behavior.DEFAULT.      Note: Currently TransactionalSession doesn't use behavior, but parameter      is included for API consistency.</p> <code>None</code> <p>Returns:</p> Type Description <code>NoReturn</code> <p>A new transactional session instance</p> Note <p>SyncTransactionalSession may not be implemented yet. This is a placeholder for API consistency with the async version.</p>"},{"location":"api/sync/cluster/#aerospike_fluent.sync.cluster.Cluster.is_connected","title":"is_connected","text":"<pre><code>is_connected()\n</code></pre> <p>Checks if the cluster connection is currently active.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the connection is active, False otherwise</p>"},{"location":"api/sync/cluster/#aerospike_fluent.sync.cluster.Cluster.close","title":"close","text":"<pre><code>close()\n</code></pre> <p>Closes the cluster connection and releases all associated resources.</p> <p>This method closes the underlying client connection. It should be called when the cluster is no longer needed to ensure proper resource cleanup.</p> This method is automatically called when using context manager <pre><code>with ClusterDefinition(\"localhost\", 3100).connect() as cluster:\n    # Use the cluster...\n# cluster.close() is automatically called here\n</code></pre>"},{"location":"api/sync/index-builder/","title":"SyncIndexBuilder","text":""},{"location":"api/sync/index-builder/#aerospike_fluent.sync.operations.index.SyncIndexBuilder","title":"SyncIndexBuilder","text":"<pre><code>SyncIndexBuilder(\n    async_client, namespace, set_name, loop_manager\n)\n</code></pre> <p>Synchronous fa\u00e7ade over :class:<code>~aerospike_fluent.aio.operations.index.IndexBuilder</code>.</p> <p>Chain :meth:<code>on_bin</code>, :meth:<code>named</code>, :meth:<code>numeric</code> / :meth:<code>string</code>, optional :meth:<code>collection</code>, then :meth:<code>create</code> or :meth:<code>drop</code>; each mutating step is stored locally and replayed onto a fresh async builder when executing.</p> See Also <p>:class:<code>~aerospike_fluent.aio.operations.index.IndexBuilder</code></p> <p>Pair with <code>namespace</code>/<code>set</code> and the parent's loop manager.</p>"},{"location":"api/sync/index-builder/#aerospike_fluent.sync.operations.index.SyncIndexBuilder.on_bin","title":"on_bin","text":"<pre><code>on_bin(bin_name)\n</code></pre> <p>Set which bin this secondary index covers.</p>"},{"location":"api/sync/index-builder/#aerospike_fluent.sync.operations.index.SyncIndexBuilder.named","title":"named","text":"<pre><code>named(index_name)\n</code></pre> <p>Set the secondary index name the cluster uses (required for create and drop).</p>"},{"location":"api/sync/index-builder/#aerospike_fluent.sync.operations.index.SyncIndexBuilder.numeric","title":"numeric","text":"<pre><code>numeric()\n</code></pre> <p>Set the secondary index type to numeric; use :meth:<code>string</code> for string bins.</p>"},{"location":"api/sync/index-builder/#aerospike_fluent.sync.operations.index.SyncIndexBuilder.string","title":"string","text":"<pre><code>string()\n</code></pre> <p>Set the secondary index type to string; use :meth:<code>numeric</code> for numeric bins.</p>"},{"location":"api/sync/index-builder/#aerospike_fluent.sync.operations.index.SyncIndexBuilder.collection","title":"collection","text":"<pre><code>collection(collection_index_type)\n</code></pre> <p>Set the collection index variant for map or list bins.</p> <p>Parameters:</p> Name Type Description Default <code>collection_index_type</code> <code>CollectionIndexType</code> <p>Same as :meth:<code>~aerospike_fluent.aio.operations.index.IndexBuilder.collection</code>.</p> required"},{"location":"api/sync/index-builder/#aerospike_fluent.sync.operations.index.SyncIndexBuilder.context","title":"context","text":"<pre><code>context(ctx)\n</code></pre> <p>Set a CDT context path for indexing a nested list or map element.</p> <p>Parameters:</p> Name Type Description Default <code>ctx</code> <code>List[CTX]</code> <p>CDT path entries (e.g., <code>[CTX.map_key(\"outer\")]</code>).</p> required <p>Returns:</p> Type Description <code>SyncIndexBuilder</code> <p><code>self</code> for method chaining.</p>"},{"location":"api/sync/index-builder/#aerospike_fluent.sync.operations.index.SyncIndexBuilder.create","title":"create","text":"<pre><code>create()\n</code></pre> <p>Create the index (blocks until the admin call completes).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Same validation as async :meth:<code>~aerospike_fluent.aio.operations.index.IndexBuilder.create</code>.</p> <code>AerospikeError</code> <p>On failure from the cluster (typed when mapped).</p>"},{"location":"api/sync/index-builder/#aerospike_fluent.sync.operations.index.SyncIndexBuilder.drop","title":"drop","text":"<pre><code>drop()\n</code></pre> <p>Drop the index (blocks until the admin call completes).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the index name was not set via :meth:<code>named</code>.</p> <code>AerospikeError</code> <p>On failure from the cluster.</p>"},{"location":"api/sync/info/","title":"SyncInfoCommands","text":""},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands","title":"SyncInfoCommands","text":"<pre><code>SyncInfoCommands(async_info, loop_manager)\n</code></pre> <p>Synchronous wrapper for InfoCommands.</p> <p>Provides the same API as the async InfoCommands but executes operations synchronously by running them on an event loop.</p> <p>Initialize a SyncInfoCommands.</p> <p>Parameters:</p> Name Type Description Default <code>async_info</code> <code>InfoCommands</code> <p>The async InfoCommands to wrap.</p> required <code>loop_manager</code> <code>_EventLoopManager</code> <p>The event loop manager for executing async operations.</p> required"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.build","title":"build","text":"<pre><code>build()\n</code></pre> <p>Get the build information from all nodes in the cluster (synchronous).</p>"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.namespaces","title":"namespaces","text":"<pre><code>namespaces()\n</code></pre> <p>Get the list of namespaces from all nodes in the cluster (synchronous).</p>"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.namespace_details","title":"namespace_details","text":"<pre><code>namespace_details(namespace)\n</code></pre> <p>Get detailed information about a specific namespace (synchronous).</p>"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.sets","title":"sets","text":"<pre><code>sets(namespace)\n</code></pre> <p>Get the list of sets in a specific namespace (synchronous).</p>"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.secondary_indexes","title":"secondary_indexes","text":"<pre><code>secondary_indexes(namespace=None)\n</code></pre> <p>Get information about all secondary indexes (synchronous).</p>"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.secondary_index_details","title":"secondary_index_details","text":"<pre><code>secondary_index_details(namespace, index_name)\n</code></pre> <p>Get detailed information about a specific secondary index (synchronous).</p>"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.is_cluster_stable","title":"is_cluster_stable","text":"<pre><code>is_cluster_stable()\n</code></pre> <p>Check if all nodes agree on the current cluster state (synchronous).</p>"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.get_cluster_size","title":"get_cluster_size","text":"<pre><code>get_cluster_size()\n</code></pre> <p>Get the number of nodes in the cluster (synchronous).</p>"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.info","title":"info","text":"<pre><code>info(command)\n</code></pre> <p>Execute a raw info command against the cluster (synchronous).</p>"},{"location":"api/sync/info/#aerospike_fluent.sync.info.SyncInfoCommands.info_on_all_nodes","title":"info_on_all_nodes","text":"<pre><code>info_on_all_nodes(command)\n</code></pre> <p>Execute a raw info command against all nodes in the cluster (synchronous).</p>"},{"location":"api/sync/query/","title":"Sync Query Builders","text":"<p>Synchronous query and write-verb builders delegating to <code>aio.operations.query</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder","title":"SyncQueryBuilder","text":"<pre><code>SyncQueryBuilder(\n    async_client,\n    namespace,\n    set_name,\n    loop_manager,\n    query_builder=None,\n)\n</code></pre> <p>               Bases: <code>_SyncWriteVerbs</code></p> <p>Configure and run reads, queries, and write segments synchronously.</p> <p>Every chain method forwards to :class:<code>~aerospike_fluent.aio.operations.query.QueryBuilder</code>. :meth:<code>execute</code> blocks on the owning loop manager and returns :class:<code>~aerospike_fluent.sync.record_stream.SyncRecordStream</code>. Detailed parameter semantics (filters, policies, CDT, <code>on_error</code>) are documented on the async builder.</p> See Also <p>:class:<code>~aerospike_fluent.aio.operations.query.QueryBuilder</code></p> <p>Attach or create an async :class:<code>QueryBuilder</code> and the sync loop.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.bins","title":"bins","text":"<pre><code>bins(bin_names)\n</code></pre> <p>Specify which bins to retrieve.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.bin","title":"bin","text":"<pre><code>bin(bin_name)\n</code></pre> <p>Start a bin-level read operation.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.add_operation","title":"add_operation","text":"<pre><code>add_operation(op)\n</code></pre> <p>Append a read operation produced by a bin or CDT builder.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.with_write_operations","title":"with_write_operations","text":"<pre><code>with_write_operations(operations)\n</code></pre> <p>Attach scalar write operations for a background dataset task.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.with_no_bins","title":"with_no_bins","text":"<pre><code>with_no_bins()\n</code></pre> <p>Specify that no bins should be read (header-only query).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.filter","title":"filter","text":"<pre><code>filter(filter_obj)\n</code></pre> <p>Add a filter to the query.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.filter_expression","title":"filter_expression","text":"<pre><code>filter_expression(expression)\n</code></pre> <p>Set a FilterExpression for server-side filtering.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.where","title":"where","text":"<pre><code>where(expression: str) -&gt; SyncQueryBuilder\n</code></pre><pre><code>where(expression: FilterExpression) -&gt; SyncQueryBuilder\n</code></pre> <pre><code>where(expression)\n</code></pre> <p>Set the query filter from a DSL string or FilterExpression.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.with_policy","title":"with_policy","text":"<pre><code>with_policy(policy)\n</code></pre> <p>Set the query policy.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.with_read_policy","title":"with_read_policy","text":"<pre><code>with_read_policy(policy)\n</code></pre> <p>Set the read policy (for single key or batch key queries).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.partition","title":"partition","text":"<pre><code>partition(partition_filter)\n</code></pre> <p>Set the partition filter.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.on_partitions","title":"on_partitions","text":"<pre><code>on_partitions(*partition_ids)\n</code></pre> <p>Set partitions to query by partition IDs.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.on_partition","title":"on_partition","text":"<pre><code>on_partition(part_id)\n</code></pre> <p>Target a specific partition for the query.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.on_partition_range","title":"on_partition_range","text":"<pre><code>on_partition_range(start_incl, end_excl)\n</code></pre> <p>Target a range of partitions for the query.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.chunk_size","title":"chunk_size","text":"<pre><code>chunk_size(chunk_size)\n</code></pre> <p>Set the chunk size for server-side streaming.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.records_per_second","title":"records_per_second","text":"<pre><code>records_per_second(rps)\n</code></pre> <p>Set the maximum records per second for the query.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.max_records","title":"max_records","text":"<pre><code>max_records(max_records)\n</code></pre> <p>Set the maximum number of records to return.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.limit","title":"limit","text":"<pre><code>limit(limit)\n</code></pre> <p>Set the maximum number of records to return (alias for max_records).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.expected_duration","title":"expected_duration","text":"<pre><code>expected_duration(duration)\n</code></pre> <p>Set the expected duration of the query.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.with_hint","title":"with_hint","text":"<pre><code>with_hint(hint)\n</code></pre> <p>Attach a query hint for secondary index selection or scheduling.</p> <p>Forwards to :meth:<code>~aerospike_fluent.aio.operations.query.QueryBuilder.with_hint</code>.</p> <p>Parameters:</p> Name Type Description Default <code>hint</code> <code>QueryHint</code> <p>A :class:<code>QueryHint</code> instance.</p> required <p>Returns:</p> Type Description <code>SyncQueryBuilder</code> <p>This builder for method chaining.</p> See Also <p>:class:<code>~aerospike_fluent.aio.operations.query.QueryHint</code></p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.with_index_context","title":"with_index_context","text":"<pre><code>with_index_context(index_context)\n</code></pre> <p>Explicitly override the secondary index metadata used for filter generation.</p> <p>Most applications do not need this method. The client automatically discovers and caches secondary index metadata in the background.</p> <p>Parameters:</p> Name Type Description Default <code>index_context</code> <code>IndexContext</code> <p>Index metadata for the query's namespace.</p> required <p>Returns:</p> Type Description <code>SyncQueryBuilder</code> <p>This builder for method chaining.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.replica","title":"replica","text":"<pre><code>replica(replica)\n</code></pre> <p>Set the replica preference for the query.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.base_policy","title":"base_policy","text":"<pre><code>base_policy(base_policy)\n</code></pre> <p>Set the base policy for the query.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.fail_on_filtered_out","title":"fail_on_filtered_out","text":"<pre><code>fail_on_filtered_out()\n</code></pre> <p>Include filtered-out records in the stream with FILTERED_OUT code.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>Return null for missing keys instead of omitting them.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.default_where","title":"default_where","text":"<pre><code>default_where(expression: str) -&gt; SyncQueryBuilder\n</code></pre><pre><code>default_where(\n    expression: FilterExpression,\n) -&gt; SyncQueryBuilder\n</code></pre> <pre><code>default_where(expression)\n</code></pre> <p>Set a default filter for all chained operations that lack their own.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.default_expire_record_after_seconds","title":"default_expire_record_after_seconds","text":"<pre><code>default_expire_record_after_seconds(seconds)\n</code></pre> <p>Set a default TTL for all chained operations that lack their own.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.default_never_expire","title":"default_never_expire","text":"<pre><code>default_never_expire()\n</code></pre> <p>Set the default TTL to never expire (TTL = -1).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.default_with_no_change_in_expiration","title":"default_with_no_change_in_expiration","text":"<pre><code>default_with_no_change_in_expiration()\n</code></pre> <p>Set the default to preserve each record's existing TTL (TTL = -2).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.default_expiry_from_server_default","title":"default_expiry_from_server_default","text":"<pre><code>default_expiry_from_server_default()\n</code></pre> <p>Set the default TTL to the namespace's server default (TTL = 0).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.query","title":"query","text":"<pre><code>query(arg1, *more_keys)\n</code></pre> <p>Chain another query with new key(s) for batch/point stacking.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.execute_background_task","title":"execute_background_task","text":"<pre><code>execute_background_task()\n</code></pre> <p>Run a background write for this dataset query (see <code>QueryBuilder</code>).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.execute_udf_background_task","title":"execute_udf_background_task","text":"<pre><code>execute_udf_background_task(\n    package_name, function_name, args=None\n)\n</code></pre> <p>Run a background UDF for this dataset query (see <code>QueryBuilder</code>).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncQueryBuilder.execute","title":"execute","text":"<pre><code>execute(on_error=None)\n</code></pre> <p>Run the configured query or write chain and block until the stream is ready.</p> <p>Parameters:</p> Name Type Description Default <code>on_error</code> <code>OnError | None</code> <p>Same as :meth:<code>~aerospike_fluent.aio.operations.query.QueryBuilder.execute</code> (:class:<code>~aerospike_fluent.error_strategy.ErrorStrategy</code> or callback).</p> <code>None</code> <p>Returns:</p> Type Description <code>SyncRecordStream</code> <p>class:<code>~aerospike_fluent.sync.record_stream.SyncRecordStream</code>.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.operations.query.QueryBuilder.execute</code></p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder","title":"SyncWriteSegmentBuilder","text":"<pre><code>SyncWriteSegmentBuilder(wsb, loop_manager)\n</code></pre> <p>               Bases: <code>_SyncWriteVerbs</code></p> <p>Synchronous multi-key write segment (mirrors :class:<code>WriteSegmentBuilder</code>).</p> <p>Bin scalars, CDT, expressions, and policies delegate to the embedded async segment; :meth:<code>execute</code> returns :class:<code>~aerospike_fluent.sync.record_stream.SyncRecordStream</code>.</p> See Also <p>:class:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder</code></p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.bin","title":"bin","text":"<pre><code>bin(bin_name)\n</code></pre> <p>Start a bin-level write operation.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.add_operation","title":"add_operation","text":"<pre><code>add_operation(op)\n</code></pre> <p>Append an operation (used by CDT action builders).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.put","title":"put","text":"<pre><code>put(bins)\n</code></pre> <p>Set multiple bins at once.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.set_bins","title":"set_bins","text":"<pre><code>set_bins(bins)\n</code></pre> <p>Alias for :meth:<code>put</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.set_to","title":"set_to","text":"<pre><code>set_to(bin_name, value)\n</code></pre> <p>Set a bin to value.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.add","title":"add","text":"<pre><code>add(bin_name, value)\n</code></pre> <p>Add a numeric value to a bin.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.increment_by","title":"increment_by","text":"<pre><code>increment_by(bin_name, value)\n</code></pre> <p>Alias for :meth:<code>add</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.get","title":"get","text":"<pre><code>get(bin_name)\n</code></pre> <p>Read a bin value back within a write operate.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.append","title":"append","text":"<pre><code>append(bin_name, value)\n</code></pre> <p>Append a string to a bin.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.prepend","title":"prepend","text":"<pre><code>prepend(bin_name, value)\n</code></pre> <p>Prepend a string to a bin.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.remove_bin","title":"remove_bin","text":"<pre><code>remove_bin(bin_name)\n</code></pre> <p>Delete a bin from the record.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.select_from","title":"select_from","text":"<pre><code>select_from(\n    bin_name, expression, *, ignore_eval_failure=False\n)\n</code></pre> <p>Read a computed value into a bin using a DSL expression.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.insert_from","title":"insert_from","text":"<pre><code>insert_from(\n    bin_name,\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result only if bin does not already exist.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.update_from","title":"update_from","text":"<pre><code>update_from(\n    bin_name,\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result only if bin already exists.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.upsert_from","title":"upsert_from","text":"<pre><code>upsert_from(\n    bin_name,\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result, creating or overwriting the bin.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.query","title":"query","text":"<pre><code>query(arg1, *more_keys)\n</code></pre> <p>Finalize current write segment and start a read segment.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.where","title":"where","text":"<pre><code>where(expression)\n</code></pre> <p>Set a filter expression on the current write segment.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.expire_record_after_seconds","title":"expire_record_after_seconds","text":"<pre><code>expire_record_after_seconds(seconds)\n</code></pre> <p>Set the TTL on the current write segment.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.never_expire","title":"never_expire","text":"<pre><code>never_expire()\n</code></pre> <p>Set this record to never expire (TTL = -1).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.with_no_change_in_expiration","title":"with_no_change_in_expiration","text":"<pre><code>with_no_change_in_expiration()\n</code></pre> <p>Preserve the record's existing TTL (TTL = -2).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.expiry_from_server_default","title":"expiry_from_server_default","text":"<pre><code>expiry_from_server_default()\n</code></pre> <p>Use the namespace's default TTL for this record (TTL = 0).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.ensure_generation_is","title":"ensure_generation_is","text":"<pre><code>ensure_generation_is(generation)\n</code></pre> <p>Set expected generation for optimistic locking.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.durably_delete","title":"durably_delete","text":"<pre><code>durably_delete()\n</code></pre> <p>Enable durable delete on the current segment.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>Include results for missing keys in the stream.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.fail_on_filtered_out","title":"fail_on_filtered_out","text":"<pre><code>fail_on_filtered_out()\n</code></pre> <p>Mark filtered-out records with <code>FILTERED_OUT</code> result code.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.replace_only","title":"replace_only","text":"<pre><code>replace_only()\n</code></pre> <p>Change the current segment to replace-if-exists semantics.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteSegmentBuilder.execute","title":"execute","text":"<pre><code>execute(on_error=None)\n</code></pre> <p>Flush accumulated write operations and return a synchronous result stream.</p> <p>Parameters:</p> Name Type Description Default <code>on_error</code> <code>OnError | None</code> <p>Same as :meth:<code>SyncQueryBuilder.execute</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>SyncRecordStream</code> <p>class:<code>~aerospike_fluent.sync.record_stream.SyncRecordStream</code>.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.operations.query.WriteSegmentBuilder.execute</code></p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder","title":"SyncWriteBinBuilder","text":"<pre><code>SyncWriteBinBuilder(sync_segment, bin_name)\n</code></pre> <p>               Bases: <code>_SyncWriteVerbs</code></p> <p>Synchronous wrapper for bin-level write operations.</p> <p>Per-bin write builder that captures a bin name and delegates all operations to the parent <code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.set_to","title":"set_to","text":"<pre><code>set_to(value)\n</code></pre> <p>Set the bin to value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>New value to store.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.add","title":"add","text":"<pre><code>add(value)\n</code></pre> <p>Add a numeric value to the bin.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Numeric value to add.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.increment_by","title":"increment_by","text":"<pre><code>increment_by(value)\n</code></pre> <p>Alias for :meth:<code>add</code>.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Numeric value to add.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.append","title":"append","text":"<pre><code>append(value)\n</code></pre> <p>Append a string to the bin.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>String to append.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.prepend","title":"prepend","text":"<pre><code>prepend(value)\n</code></pre> <p>Prepend a string to the bin.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>String to prepend.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.remove","title":"remove","text":"<pre><code>remove()\n</code></pre> <p>Delete the bin from the record.</p> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.get","title":"get","text":"<pre><code>get()\n</code></pre> <p>Read the bin value back within a write operate.</p> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.list_add","title":"list_add","text":"<pre><code>list_add(value)\n</code></pre> <p>Add value to an ordered list (sorted insert).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to insert.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.list_append","title":"list_append","text":"<pre><code>list_append(value)\n</code></pre> <p>Append value to the end of an unordered list.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to append.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.map_clear","title":"map_clear","text":"<pre><code>map_clear()\n</code></pre> <p>Remove all entries from the map bin.</p> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.map_size","title":"map_size","text":"<pre><code>map_size()\n</code></pre> <p>Return the map element count (read within operate).</p> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.map_upsert_items","title":"map_upsert_items","text":"<pre><code>map_upsert_items(items)\n</code></pre> <p>Put multiple map entries (create or update each key).</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Mapping or sequence of <code>(key, value)</code> pairs.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.map_insert_items","title":"map_insert_items","text":"<pre><code>map_insert_items(items)\n</code></pre> <p>Put map entries only for keys that do not yet exist.</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Mapping or sequence of <code>(key, value)</code> pairs.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.map_update_items","title":"map_update_items","text":"<pre><code>map_update_items(items)\n</code></pre> <p>Update existing map entries only (no new keys).</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Mapping or sequence of <code>(key, value)</code> pairs.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.map_create","title":"map_create","text":"<pre><code>map_create(order)\n</code></pre> <p>Create an empty map with the given key order.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>MapOrder</code> <p>Key sort order for the map.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.map_set_policy","title":"map_set_policy","text":"<pre><code>map_set_policy(order)\n</code></pre> <p>Set map sort order policy without changing entries.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>MapOrder</code> <p>Key sort order to apply.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.list_clear","title":"list_clear","text":"<pre><code>list_clear()\n</code></pre> <p>Remove all elements from the list bin.</p> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.list_sort","title":"list_sort","text":"<pre><code>list_sort(flags=DEFAULT)\n</code></pre> <p>Sort the list bin.</p> <p>Parameters:</p> Name Type Description Default <code>flags</code> <code>ListSortFlags</code> <p>Sort behavior flags (default <code>DEFAULT</code>).</p> <code>DEFAULT</code> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.list_size","title":"list_size","text":"<pre><code>list_size()\n</code></pre> <p>Return the list element count (read within operate).</p> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.list_append_items","title":"list_append_items","text":"<pre><code>list_append_items(items)\n</code></pre> <p>Append values to an unordered list.</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Values to append.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.list_add_items","title":"list_add_items","text":"<pre><code>list_add_items(items)\n</code></pre> <p>Insert values into an ordered list (sorted positions).</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>Any</code> <p>Values to insert.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.list_create","title":"list_create","text":"<pre><code>list_create(order, *, pad=False, persist_index=False)\n</code></pre> <p>Create an empty list with the given order.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>ListOrderType</code> <p>Element ordering.</p> required <code>pad</code> <code>bool</code> <p>If <code>True</code>, allow sparse indexes.</p> <code>False</code> <code>persist_index</code> <code>bool</code> <p>If <code>True</code>, maintain a persistent index.</p> <code>False</code> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.list_set_order","title":"list_set_order","text":"<pre><code>list_set_order(order)\n</code></pre> <p>Set list sort order without changing elements.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>ListOrderType</code> <p>Sort order to apply.</p> required <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_index","title":"on_map_index","text":"<pre><code>on_map_index(index)\n</code></pre> <p>Navigate to a map element by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Map index to target.</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_key","title":"on_map_key","text":"<pre><code>on_map_key(key)\n</code></pre> <p>Navigate to a map element by key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Any</code> <p>Map key to target.</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_rank","title":"on_map_rank","text":"<pre><code>on_map_rank(rank)\n</code></pre> <p>Navigate to a map element by rank (0 = lowest value).</p> <p>Parameters:</p> Name Type Description Default <code>rank</code> <code>int</code> <p>Rank position (0 = lowest value).</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_value","title":"on_map_value","text":"<pre><code>on_map_value(value)\n</code></pre> <p>Navigate to map elements matching a value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_index_range","title":"on_map_index_range","text":"<pre><code>on_map_index_range(index, count=None)\n</code></pre> <p>Navigate to map elements by index range.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Start index.</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_key_range","title":"on_map_key_range","text":"<pre><code>on_map_key_range(start, end)\n</code></pre> <p>Navigate to map elements by key range [start, end).</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>Any</code> <p>Inclusive range start.</p> required <code>end</code> <code>Any</code> <p>Exclusive range end.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_rank_range","title":"on_map_rank_range","text":"<pre><code>on_map_rank_range(rank, count=None)\n</code></pre> <p>Navigate to map elements by rank range.</p> <p>Parameters:</p> Name Type Description Default <code>rank</code> <code>int</code> <p>Start rank (0 = lowest value).</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries to select; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_value_range","title":"on_map_value_range","text":"<pre><code>on_map_value_range(start, end)\n</code></pre> <p>Navigate to map elements by value range [start, end).</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>Any</code> <p>Inclusive range start.</p> required <code>end</code> <code>Any</code> <p>Exclusive range end.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_key_relative_index_range","title":"on_map_key_relative_index_range","text":"<pre><code>on_map_key_relative_index_range(key, index, count=None)\n</code></pre> <p>Navigate to map entries by index range relative to an anchor key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>Any</code> <p>Anchor key.</p> required <code>index</code> <code>int</code> <p>Relative index offset from the anchor.</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_value_relative_rank_range","title":"on_map_value_relative_rank_range","text":"<pre><code>on_map_value_relative_rank_range(value, rank, count=None)\n</code></pre> <p>Navigate to map entries by value rank range relative to an anchor value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Anchor value.</p> required <code>rank</code> <code>int</code> <p>Relative rank offset from the anchor.</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_key_list","title":"on_map_key_list","text":"<pre><code>on_map_key_list(keys)\n</code></pre> <p>Navigate to map elements matching a list of keys.</p> <p>Parameters:</p> Name Type Description Default <code>keys</code> <code>List[Any]</code> <p>Map keys to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_map_value_list","title":"on_map_value_list","text":"<pre><code>on_map_value_list(values)\n</code></pre> <p>Navigate to map elements matching a list of values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[Any]</code> <p>Values to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_list_index","title":"on_list_index","text":"<pre><code>on_list_index(index)\n</code></pre> <p>Navigate to a list element by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>List index (0-based, negative counts from end).</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_list_rank","title":"on_list_rank","text":"<pre><code>on_list_rank(rank)\n</code></pre> <p>Navigate to a list element by rank (0 = lowest value).</p> <p>Parameters:</p> Name Type Description Default <code>rank</code> <code>int</code> <p>Rank position (0 = lowest value).</p> required <p>Returns:</p> Type Description <code>CdtWriteBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteBuilder</code> for writing the targeted element.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_list_value","title":"on_list_value","text":"<pre><code>on_list_value(value)\n</code></pre> <p>Navigate to list elements matching a value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_list_index_range","title":"on_list_index_range","text":"<pre><code>on_list_index_range(index, count=None)\n</code></pre> <p>Navigate to list elements by index range.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>Start index.</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_list_rank_range","title":"on_list_rank_range","text":"<pre><code>on_list_rank_range(rank, count=None)\n</code></pre> <p>Navigate to list elements by rank range.</p> <p>Parameters:</p> Name Type Description Default <code>rank</code> <code>int</code> <p>Start rank (0 = lowest value).</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_list_value_range","title":"on_list_value_range","text":"<pre><code>on_list_value_range(start, end)\n</code></pre> <p>Navigate to list elements by value range [start, end).</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>Any</code> <p>Inclusive range start.</p> required <code>end</code> <code>Any</code> <p>Exclusive range end.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_list_value_relative_rank_range","title":"on_list_value_relative_rank_range","text":"<pre><code>on_list_value_relative_rank_range(value, rank, count=None)\n</code></pre> <p>Navigate to list elements by value rank range relative to an anchor value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Anchor value.</p> required <code>rank</code> <code>int</code> <p>Relative rank offset from the anchor.</p> required <code>count</code> <code>Optional[int]</code> <p>Maximum entries; <code>None</code> for all remaining.</p> <code>None</code> <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.on_list_value_list","title":"on_list_value_list","text":"<pre><code>on_list_value_list(values)\n</code></pre> <p>Navigate to list elements matching a list of values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[Any]</code> <p>Values to match.</p> required <p>Returns:</p> Type Description <code>CdtWriteInvertableBuilder[SyncWriteSegmentBuilder]</code> <p>class:<code>CdtWriteInvertableBuilder</code> for writing the targeted element(s).</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.select_from","title":"select_from","text":"<pre><code>select_from(expression, *, ignore_eval_failure=False)\n</code></pre> <p>Read a computed value into this bin using a DSL expression.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or <code>FilterExpression</code>.</p> required <code>ignore_eval_failure</code> <code>bool</code> <p>If <code>True</code>, suppress evaluation errors.</p> <code>False</code> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.insert_from","title":"insert_from","text":"<pre><code>insert_from(\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result only if bin does not already exist.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or <code>FilterExpression</code>.</p> required <code>ignore_op_failure</code> <code>bool</code> <p>If <code>True</code>, suppress operation failures.</p> <code>False</code> <code>ignore_eval_failure</code> <code>bool</code> <p>If <code>True</code>, suppress evaluation errors.</p> <code>False</code> <code>delete_if_null</code> <code>bool</code> <p>If <code>True</code>, delete the bin when the expression evaluates to null.</p> <code>False</code> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.update_from","title":"update_from","text":"<pre><code>update_from(\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result only if bin already exists.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or <code>FilterExpression</code>.</p> required <code>ignore_op_failure</code> <code>bool</code> <p>If <code>True</code>, suppress operation failures.</p> <code>False</code> <code>ignore_eval_failure</code> <code>bool</code> <p>If <code>True</code>, suppress evaluation errors.</p> <code>False</code> <code>delete_if_null</code> <code>bool</code> <p>If <code>True</code>, delete the bin when the expression evaluates to null.</p> <code>False</code> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.upsert_from","title":"upsert_from","text":"<pre><code>upsert_from(\n    expression,\n    *,\n    ignore_op_failure=False,\n    ignore_eval_failure=False,\n    delete_if_null=False,\n)\n</code></pre> <p>Write expression result, creating or overwriting the bin.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>Union[str, FilterExpression]</code> <p>DSL string or <code>FilterExpression</code>.</p> required <code>ignore_op_failure</code> <code>bool</code> <p>If <code>True</code>, suppress operation failures.</p> <code>False</code> <code>ignore_eval_failure</code> <code>bool</code> <p>If <code>True</code>, suppress evaluation errors.</p> <code>False</code> <code>delete_if_null</code> <code>bool</code> <p>If <code>True</code>, delete the bin when the expression evaluates to null.</p> <code>False</code> <p>Returns:</p> Type Description <code>SyncWriteSegmentBuilder</code> <p>The parent :class:<code>SyncWriteSegmentBuilder</code>.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.bin","title":"bin","text":"<pre><code>bin(bin_name)\n</code></pre> <p>Start the next bin operation.</p> <p>Parameters:</p> Name Type Description Default <code>bin_name</code> <code>str</code> <p>Name of the next bin.</p> required <p>Returns:</p> Type Description <code>SyncWriteBinBuilder</code> <p>class:<code>SyncWriteBinBuilder</code> for the named bin.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.query","title":"query","text":"<pre><code>query(arg1, *more_keys)\n</code></pre> <p>Shortcut: finalize write segment and start a read segment.</p> <p>Parameters:</p> Name Type Description Default <code>arg1</code> <code>Union[Key, List[Key]]</code> <p>Key or list of keys.</p> required <code>more_keys</code> <code>Key</code> <p>Additional keys.</p> <code>()</code> <p>Returns:</p> Type Description <code>SyncQueryBuilder</code> <p>class:<code>SyncQueryBuilder</code> for read operations.</p>"},{"location":"api/sync/query/#aerospike_fluent.sync.operations.query.SyncWriteBinBuilder.execute","title":"execute","text":"<pre><code>execute(on_error=None)\n</code></pre> <p>Shortcut: execute all accumulated operations.</p> <p>Parameters:</p> Name Type Description Default <code>on_error</code> <code>OnError | None</code> <p>Error handling strategy.</p> <code>None</code> <p>Returns:</p> Type Description <code>SyncRecordStream</code> <p>class:<code>SyncRecordStream</code> with operation results.</p>"},{"location":"api/sync/session/","title":"SyncSession","text":""},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession","title":"SyncSession","text":"<pre><code>SyncSession(async_session, loop_manager)\n</code></pre> <p>Run session-scoped reads and writes without <code>async</code>/<code>await</code>.</p> <p>Constructed by :meth:<code>SyncFluentClient.create_session &lt;aerospike_fluent.sync.client.SyncFluentClient.create_session&gt;</code>, not by calling <code>SyncSession(...)</code> directly. Each method delegates to :class:<code>~aerospike_fluent.aio.session.Session</code> on a shared per-thread loop; return types are sync wrappers where the async API would return a coroutine or async stream.</p> See Also <p>:class:<code>~aerospike_fluent.aio.session.Session</code>: Async API and behavior     semantics.</p> <p>Wrap <code>async_session</code>; use :meth:<code>SyncFluentClient.create_session</code> instead.</p> <p>Parameters:</p> Name Type Description Default <code>async_session</code> <code>Session</code> <p>Connected async session (same behavior binding).</p> required <code>loop_manager</code> <code>_EventLoopManager</code> <p>Loop manager shared with the parent :class:<code>~aerospike_fluent.sync.client.SyncFluentClient</code>.</p> required"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.behavior","title":"behavior  <code>property</code>","text":"<pre><code>behavior\n</code></pre> <p>Get the behavior configuration for this session.</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.client","title":"client  <code>property</code>","text":"<pre><code>client\n</code></pre> <p>Get the underlying FluentClient.</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.query","title":"query","text":"<pre><code>query(\n    arg1=None,\n    arg2=None,\n    *keys,\n    namespace=None,\n    set_name=None,\n    dataset=None,\n    key=None,\n    keys_list=None,\n)\n</code></pre> <p>Start a read or secondary-index query (synchronous session).</p> <p>Same shapes as :meth:<code>aerospike_fluent.aio.session.Session.query</code>, with this session's behavior applied on the underlying async builder.</p> <p>Parameters:</p> Name Type Description Default <code>arg1</code> <code>Optional[Union[DataSet, Key, List[Key], str]]</code> <p>Positional dataset, key, list of keys, or namespace string (when paired with <code>arg2</code> as set name).</p> <code>None</code> <code>arg2</code> <code>Optional[str]</code> <p>When <code>arg1</code> is a namespace, the set name; otherwise may be a second key when passing multiple keys positionally.</p> <code>None</code> <code>*keys</code> <code>Key</code> <p>Additional keys when the first positional argument is a key.</p> <code>()</code> <code>namespace</code> <code>Optional[str]</code> <p>Keyword namespace (with <code>set_name</code>) when not using a dataset.</p> <code>None</code> <code>set_name</code> <code>Optional[str]</code> <p>Keyword set name (with <code>namespace</code>).</p> <code>None</code> <code>dataset</code> <code>Optional[DataSet]</code> <p>Keyword :class:<code>~aerospike_fluent.dataset.DataSet</code>.</p> <code>None</code> <code>key</code> <code>Optional[Key]</code> <p>Keyword single key.</p> <code>None</code> <code>keys_list</code> <code>Optional[List[Key]]</code> <p>Keyword list of keys for batch read.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>'SyncQueryBuilder'</code> <p>class:<code>~aerospike_fluent.sync.operations.query.SyncQueryBuilder</code>.</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.background_task","title":"background_task","text":"<pre><code>background_task()\n</code></pre> <p>Start a background dataset task chain (synchronous).</p> <p>Returns:</p> Type Description <code>'SyncBackgroundTaskSession'</code> <p>class:<code>~aerospike_fluent.sync.background.SyncBackgroundTaskSession</code>.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.session.Session.background_task</code>.</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.execute_udf","title":"execute_udf","text":"<pre><code>execute_udf(*keys)\n</code></pre> <p>Begin a foreground UDF invocation on the given keys (synchronous).</p> <p>Returns:</p> Type Description <code>'SyncUdfFunctionBuilder'</code> <p>class:<code>~aerospike_fluent.sync.operations.udf.SyncUdfFunctionBuilder</code>.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.session.Session.execute_udf</code>.</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.index","title":"index","text":"<pre><code>index(namespace=None, set_name=None, *, dataset=None)\n</code></pre> <p>Create a secondary-index builder for this namespace/set (synchronous).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>namespace</code> and <code>set_name</code> are missing and no <code>dataset</code> is provided.</p> <p>Returns:</p> Type Description <code>'SyncIndexBuilder'</code> <p>class:<code>~aerospike_fluent.sync.operations.index.SyncIndexBuilder</code>.</p> See Also <p>:meth:<code>~aerospike_fluent.aio.session.Session.index</code>.</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.truncate","title":"truncate","text":"<pre><code>truncate(dataset, before_nanos=None)\n</code></pre> <p>Truncate (delete all records) from a set (synchronous).</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.info","title":"info","text":"<pre><code>info() -&gt; 'SyncInfoCommands'\n</code></pre><pre><code>info(command: str) -&gt; Dict[str, str]\n</code></pre> <pre><code>info(command=None)\n</code></pre> <p>Execute info commands or get the SyncInfoCommands helper (synchronous).</p> <p>With no argument, returns SyncInfoCommands for high-level helpers and info_on_all_nodes(). With a command string, runs the raw info command and returns its result.</p> Example <pre><code>response = session.info(\"sindex-list\")\ninfo = session.info()\nby_node = info.info_on_all_nodes(\"build\")\n</code></pre>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.upsert","title":"upsert","text":"<pre><code>upsert(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Create an upsert write segment (synchronous).</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.insert","title":"insert","text":"<pre><code>insert(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Create an insert write segment (synchronous).</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.update","title":"update","text":"<pre><code>update(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Create an update write segment (synchronous).</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.replace","title":"replace","text":"<pre><code>replace(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Create a replace write segment (synchronous).</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.replace_if_exists","title":"replace_if_exists","text":"<pre><code>replace_if_exists(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Create a replace-if-exists write segment (synchronous).</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.delete","title":"delete","text":"<pre><code>delete(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Create a delete write segment (synchronous).</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.touch","title":"touch","text":"<pre><code>touch(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Create a touch write segment (synchronous).</p>"},{"location":"api/sync/session/#aerospike_fluent.sync.session.SyncSession.exists","title":"exists","text":"<pre><code>exists(\n    arg1=None,\n    arg2=None,\n    *keys,\n    key=None,\n    dataset=None,\n    namespace=None,\n    set_name=None,\n    key_value=None,\n)\n</code></pre> <p>Create an exists-check write segment (synchronous).</p>"},{"location":"api/sync/tls-builder/","title":"Sync TlsBuilder","text":""},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder","title":"TlsBuilder","text":"<pre><code>TlsBuilder(parent)\n</code></pre> <p>Builder class for configuring TLS settings for Aerospike cluster connections (sync version).</p> <p>This class provides a fluent API for configuring TLS parameters such as TLS name, CA file, protocols, ciphers, and other TLS-specific options.</p> Simple example usage <pre><code>cluster = ClusterDefinition(\"localhost\", 3100)            .with_tls_config_of()                .tls_name(\"myTlsName\")                .ca_file(\"myCaFile\")            .done()            .with_native_credentials(\"myUser\", \"password\")\n</code></pre> <p>Initialize a TlsBuilder.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>ClusterDefinition</code> <p>The parent ClusterDefinition</p> required"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.tls_name","title":"tls_name","text":"<pre><code>tls_name(tls_name)\n</code></pre> <p>Sets the TLS name for server certificate validation and hostname verification.</p> <p>This TLS name will be applied to all Host objects that don't already have a TLS name set. The TLS name is used for: - Certificate validation: Verifies the server certificate matches this name - SNI (Server Name Indication): Tells the server which certificate to present - Hostname override: Allows validation against a different name than the connection address</p> <p>Parameters:</p> Name Type Description Default <code>tls_name</code> <code>str</code> <p>The TLS name for certificate validation and hostname verification</p> required <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.ca_file","title":"ca_file","text":"<pre><code>ca_file(ca_file)\n</code></pre> <p>Sets the path to the Certificate Authority (CA) PEM file.</p> <p>The CA file contains the certificates used to verify the server's identity. This method supports PEM-formatted certificate files for easy certificate management.</p> <p>Parameters:</p> Name Type Description Default <code>ca_file</code> <code>str</code> <p>The path to the CA certificate PEM file</p> required <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.client_cert_file","title":"client_cert_file","text":"<pre><code>client_cert_file(cert_file)\n</code></pre> <p>Sets the path to the client certificate PEM file for mutual TLS (mTLS).</p> <p>Parameters:</p> Name Type Description Default <code>cert_file</code> <code>str</code> <p>The path to the client certificate PEM file</p> required <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.client_key_file","title":"client_key_file","text":"<pre><code>client_key_file(key_file)\n</code></pre> <p>Sets the path to the client private key PEM file for mutual TLS (mTLS).</p> <p>Parameters:</p> Name Type Description Default <code>key_file</code> <code>str</code> <p>The path to the client private key PEM file</p> required <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.protocols","title":"protocols","text":"<pre><code>protocols(*protocols)\n</code></pre> <p>Sets the allowed TLS protocols.</p> <p>Parameters:</p> Name Type Description Default <code>*protocols</code> <code>str</code> <p>The TLS protocols to allow (e.g., \"TLSv1.2\", \"TLSv1.3\")</p> <code>()</code> <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.ciphers","title":"ciphers","text":"<pre><code>ciphers(*ciphers)\n</code></pre> <p>Sets the allowed TLS cipher suites.</p> <p>Parameters:</p> Name Type Description Default <code>*ciphers</code> <code>str</code> <p>The cipher suite names to allow</p> <code>()</code> <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.for_login_only","title":"for_login_only","text":"<pre><code>for_login_only(for_login_only=True)\n</code></pre> <p>Sets whether TLS should be used only for login/authentication.</p> <p>Parameters:</p> Name Type Description Default <code>for_login_only</code> <code>bool</code> <p>If True, TLS is only used for authentication</p> <code>True</code> <p>Returns:</p> Type Description <code>TlsBuilder</code> <p>This TlsBuilder for method chaining</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.done","title":"done","text":"<pre><code>done()\n</code></pre> <p>Completes TLS configuration and returns to the parent ClusterDefinition.</p> <p>Returns:</p> Type Description <code>ClusterDefinition</code> <p>The parent ClusterDefinition for continued method chaining</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.is_tls_enabled","title":"is_tls_enabled","text":"<pre><code>is_tls_enabled()\n</code></pre> <p>Check if TLS is enabled.</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.get_tls_name","title":"get_tls_name","text":"<pre><code>get_tls_name()\n</code></pre> <p>Get the TLS name.</p>"},{"location":"api/sync/tls-builder/#aerospike_fluent.sync.tls_builder.TlsBuilder.build_tls_config","title":"build_tls_config","text":"<pre><code>build_tls_config()\n</code></pre> <p>Build a PAC TlsConfig from the builder state.</p> <p>Returns:</p> Type Description <code>Any</code> <p>A configured <code>aerospike_async.TlsConfig</code>, or <code>None</code> when</p> <code>Any</code> <p>no CA file has been set (TLS cannot be configured without one).</p>"},{"location":"api/sync/udf/","title":"Sync UDF Builders","text":"<p>Synchronous foreground UDF builders delegating to <code>aio.operations.udf</code>.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfFunctionBuilder","title":"SyncUdfFunctionBuilder","text":"<pre><code>SyncUdfFunctionBuilder(inner, loop_manager, fluent_client)\n</code></pre> <p>First step after <code>execute_udf</code>: select package and function name.</p> See Also <p>:class:<code>~aerospike_fluent.aio.operations.udf.UdfFunctionBuilder</code></p> <p>Examples:</p> <p>session.execute_udf(key).function(\"pkg\", \"fn\")</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfFunctionBuilder.function","title":"function","text":"<pre><code>function(package, function_name)\n</code></pre> <p>Select the UDF package and Lua function.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder","title":"SyncUdfBuilder","text":"<pre><code>SyncUdfBuilder(inner, loop_manager, fluent_client)\n</code></pre> <p>Chain UDF arguments, optional filter, and execution (sync).</p> See Also <p>:class:<code>~aerospike_fluent.aio.operations.udf.UdfBuilder</code></p> <p>Examples:</p> <p>session.execute_udf(key).function(\"pkg\", \"fn\").passing(1, 2).execute() session.execute_udf(key).function(\"pkg\", \"fn\").query(key).where(\"true\").execute()</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.passing","title":"passing","text":"<pre><code>passing(*args)\n</code></pre> <p>Forward arguments to the server UDF (chainable).</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.where","title":"where","text":"<pre><code>where(expression: str) -&gt; SyncUdfBuilder\n</code></pre><pre><code>where(expression: FilterExpression) -&gt; SyncUdfBuilder\n</code></pre> <pre><code>where(expression)\n</code></pre> <p>Restrict rows with a DSL string or :class:<code>~aerospike_async.FilterExpression</code>.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.respond_all_keys","title":"respond_all_keys","text":"<pre><code>respond_all_keys()\n</code></pre> <p>Include results for missing keys in the stream.</p> <p>Returns:</p> Type Description <code>SyncUdfBuilder</code> <p>self for method chaining.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.execute_udf","title":"execute_udf","text":"<pre><code>execute_udf(*keys)\n</code></pre> <p>Finalize this UDF spec and start another on keys.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.upsert","title":"upsert","text":"<pre><code>upsert(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF spec and start an upsert write segment.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.insert","title":"insert","text":"<pre><code>insert(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF spec and start an insert-only write segment.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.replace","title":"replace","text":"<pre><code>replace(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF spec and start a replace write segment.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.delete","title":"delete","text":"<pre><code>delete(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF spec and start a delete segment.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.touch","title":"touch","text":"<pre><code>touch(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF spec and start a touch segment.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.exists","title":"exists","text":"<pre><code>exists(arg1, *more_keys)\n</code></pre> <p>Finalize the UDF spec and start an exists-check segment.</p>"},{"location":"api/sync/udf/#aerospike_fluent.sync.operations.udf.SyncUdfBuilder.execute","title":"execute","text":"<pre><code>execute(on_error=None)\n</code></pre> <p>Run the UDF and return a :class:<code>~aerospike_fluent.sync.record_stream.SyncRecordStream</code>.</p> <p>Parameters:</p> Name Type Description Default <code>on_error</code> <code>OnError | None</code> <p>Same semantics as query/write :meth:<code>~aerospike_fluent.sync.operations.query.SyncQueryBuilder.execute</code>.</p> <code>None</code> See Also <p>:meth:<code>~aerospike_fluent.aio.operations.udf.UdfBuilder.execute</code></p>"},{"location":"guide/background-udf/","title":"Background Tasks &amp; UDF","text":""},{"location":"guide/background-udf/#background-tasks","title":"Background Tasks","text":"<p>Background tasks execute server-side operations across a dataset without streaming results back. Use them for bulk updates, deletes, or touches.</p>"},{"location":"guide/background-udf/#bulk-delete","title":"Bulk Delete","text":"<pre><code>users = DataSet.of(\"test\", \"users\")\n\ntask = await (\n    session.background_task()\n    .delete(users)\n    .where(\"$.status == 'inactive'\")\n    .execute()\n)\nawait task.wait_till_complete(sleep_time=0.5, max_attempts=60)\n</code></pre>"},{"location":"guide/background-udf/#bulk-update","title":"Bulk Update","text":"<pre><code>task = await (\n    session.background_task()\n    .update(users)\n    .where(\"$.tier == 'free'\")\n    .bin(\"trial_expired\").set_to(True)\n    .execute()\n)\nawait task.wait_till_complete()\n</code></pre>"},{"location":"guide/background-udf/#bulk-touch-reset-ttl","title":"Bulk Touch (Reset TTL)","text":"<pre><code>task = await (\n    session.background_task()\n    .touch(users)\n    .where(\"$.active == true\")\n    .execute()\n)\nawait task.wait_till_complete()\n</code></pre>"},{"location":"guide/background-udf/#background-udf","title":"Background UDF","text":"<p>Run a Lua UDF across matching records:</p> <pre><code>task = await (\n    session.background_task()\n    .execute_udf(users)\n    .function(\"my_module\", \"transform_record\")\n    .passing(\"arg1\", \"arg2\")\n    .execute()\n)\nawait task.wait_till_complete()\n</code></pre>"},{"location":"guide/background-udf/#foreground-udf","title":"Foreground UDF","text":"<p>Execute a Lua UDF on specific keys and get results back:</p>"},{"location":"guide/background-udf/#single-key","title":"Single Key","text":"<pre><code>stream = await (\n    session.execute_udf(users.id(1))\n    .function(\"my_module\", \"get_computed_value\")\n    .passing(42)\n    .execute()\n)\nresult = await stream.first_or_raise()\nprint(result.udf_result)  # return value from Lua\n</code></pre>"},{"location":"guide/background-udf/#multiple-keys-batch-udf","title":"Multiple Keys (Batch UDF)","text":"<pre><code>stream = await (\n    session.execute_udf(*users.ids(1, 2, 3))\n    .function(\"my_module\", \"process_record\")\n    .execute()\n)\nasync for result in stream:\n    print(result.record.key, result.udf_result)\n</code></pre>"},{"location":"guide/background-udf/#udf-registration","title":"UDF Registration","text":"<p>Register and remove Lua modules:</p> <pre><code>info = session.info()\n\n# Register a UDF module\nawait info.register_udf(\"/path/to/my_module.lua\")\n\n# Remove a UDF module\nawait info.remove_udf(\"my_module.lua\")\n</code></pre>"},{"location":"guide/background-udf/#monitoring-tasks","title":"Monitoring Tasks","text":"<p><code>ExecuteTask</code> provides polling-based completion monitoring:</p> <pre><code>task = await (\n    session.background_task()\n    .delete(users)\n    .where(\"$.expired == true\")\n    .execute()\n)\n\n# Poll with custom intervals\nawait task.wait_till_complete(\n    sleep_time=0.2,       # seconds between polls\n    max_attempts=100,     # max poll attempts\n)\n</code></pre>"},{"location":"guide/cdt-operations/","title":"CDT Operations","text":"<p>Complex Data Type (CDT) operations let you read and modify nested lists and maps within a single bin, server-side, without fetching the whole record.</p>"},{"location":"guide/cdt-operations/#reading-cdt-data","title":"Reading CDT Data","text":"<p>Navigate into a bin's structure using <code>on_list()</code> and <code>on_map_key()</code>:</p> <pre><code>users = DataSet.of(\"test\", \"users\")\n\n# Read a map value by key\nstream = await (\n    session.query(users.id(1))\n    .bin(\"settings\").on_map_key(\"theme\").get()\n    .execute()\n)\n\n# Read a list element by index\nstream = await (\n    session.query(users.id(1))\n    .bin(\"scores\").on_list_index(0).get()\n    .execute()\n)\n\n# Read a nested value: $.profile.address.city\nstream = await (\n    session.query(users.id(1))\n    .bin(\"profile\").on_map_key(\"address\").on_map_key(\"city\").get()\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#list-ranges","title":"List Ranges","text":"<pre><code># Get items by index range\nstream = await (\n    session.query(users.id(1))\n    .bin(\"scores\").on_list_index_range(0, 3).get()\n    .execute()\n)\n\n# Get items by value range\nstream = await (\n    session.query(users.id(1))\n    .bin(\"scores\").on_list_value_range(10, 100).get()\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#map-ranges","title":"Map Ranges","text":"<pre><code># Get map entries by key range\nstream = await (\n    session.query(users.id(1))\n    .bin(\"metrics\").on_map_key_range(\"a\", \"m\").get()\n    .execute()\n)\n\n# Get by rank (top N values)\nstream = await (\n    session.query(users.id(1))\n    .bin(\"scores\").on_map_value_rank_range(-3).get()\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#collection-metadata","title":"Collection Metadata","text":"<pre><code># Get list size\nstream = await (\n    session.query(users.id(1))\n    .bin(\"scores\").on_list().size()\n    .execute()\n)\n\n# Check if key exists in map\nstream = await (\n    session.query(users.id(1))\n    .bin(\"settings\").on_map_key(\"theme\").exists()\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#writing-cdt-data","title":"Writing CDT Data","text":""},{"location":"guide/cdt-operations/#set-a-value","title":"Set a Value","text":"<pre><code># Set a map key\nawait (\n    session.update(users.id(1))\n    .bin(\"settings\").on_map_key(\"theme\").set_to(\"dark\")\n    .execute()\n)\n\n# Set a list element by index\nawait (\n    session.update(users.id(1))\n    .bin(\"scores\").on_list_index(0).set_to(99)\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#add-increment","title":"Add / Increment","text":"<pre><code># Increment a map value\nawait (\n    session.update(users.id(1))\n    .bin(\"counters\").on_map_key(\"views\").add(1)\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#list-operations","title":"List Operations","text":"<pre><code># Append to a list\nawait (\n    session.update(users.id(1))\n    .bin(\"scores\").list_append(95)\n    .execute()\n)\n\n# Add item (insert-sorted for ordered lists)\nawait (\n    session.update(users.id(1))\n    .bin(\"scores\").list_add(95)\n    .execute()\n)\n\n# Append multiple items\nawait (\n    session.update(users.id(1))\n    .bin(\"tags\").list_append_items([\"python\", \"aerospike\"])\n    .execute()\n)\n\n# Clear a list\nawait (\n    session.update(users.id(1))\n    .bin(\"scores\").list_clear()\n    .execute()\n)\n\n# Sort a list\nawait (\n    session.update(users.id(1))\n    .bin(\"scores\").list_sort()\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#map-operations","title":"Map Operations","text":"<pre><code># Upsert map entries\nawait (\n    session.update(users.id(1))\n    .bin(\"settings\").map_upsert_items({\"theme\": \"dark\", \"lang\": \"en\"})\n    .execute()\n)\n\n# Clear a map\nawait (\n    session.update(users.id(1))\n    .bin(\"settings\").map_clear()\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#remove","title":"Remove","text":"<pre><code># Remove a map key\nawait (\n    session.update(users.id(1))\n    .bin(\"settings\").on_map_key(\"deprecated_key\").remove()\n    .execute()\n)\n\n# Remove a list element\nawait (\n    session.update(users.id(1))\n    .bin(\"scores\").on_list_index(-1).remove()\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#nested-navigation","title":"Nested Navigation","text":"<p>CDT navigation composes \u2014 navigate into arbitrarily nested structures:</p> <pre><code># $.users_map[\"alice\"].scores[0] = 100\nawait (\n    session.update(dataset.id(1))\n    .bin(\"users_map\").on_map_key(\"alice\").on_map_key(\"scores\").on_list_index(0).set_to(100)\n    .execute()\n)\n</code></pre>"},{"location":"guide/cdt-operations/#dsl-expressions-on-cdt","title":"DSL Expressions on CDT","text":"<p>The expression DSL supports CDT paths for filtering:</p> <pre><code># Filter records where the list has more than 5 items\nstream = await (\n    session.query(users)\n    .where(\"$.scores.count() &gt; 5\")\n    .execute()\n)\n\n# Filter on a nested map value\nstream = await (\n    session.query(users)\n    .where('$.settings.[\"theme\"] == \"dark\"')\n    .execute()\n)\n</code></pre>"},{"location":"guide/connecting/","title":"Connecting to a Cluster","text":""},{"location":"guide/connecting/#basic-connection","title":"Basic Connection","text":"<p>The simplest way to connect is with a host and port:</p> AsyncSync <pre><code>from aerospike_fluent import FluentClient\n\nasync with FluentClient(\"localhost:3000\") as client:\n    session = client.create_session()\n    # ... use session ...\n</code></pre> <pre><code>from aerospike_fluent import SyncFluentClient\n\nwith SyncFluentClient(\"localhost:3000\") as client:\n    session = client.create_session()\n    # ... use session ...\n</code></pre> <p>Both clients support context managers that automatically close the connection on exit.</p>"},{"location":"guide/connecting/#clusterdefinition","title":"ClusterDefinition","text":"<p>For advanced configuration, use <code>ClusterDefinition</code>:</p> <pre><code>from aerospike_fluent import ClusterDefinition\n\ncluster_def = (\n    ClusterDefinition(\"localhost\", 3000)\n    .with_native_credentials(\"username\", \"password\")\n    .using_services_alternate()\n    .with_ip_map({\"10.0.0.1\": \"3.72.54.187\"})\n)\n\nasync with cluster_def.connect() as client:\n    # ...\n</code></pre>"},{"location":"guide/connecting/#tls","title":"TLS","text":"<p>Server-side TLS with CA certificate verification:</p> <pre><code>cluster = (\n    ClusterDefinition(\"localhost\", 4333)\n    .with_tls_config_of()\n        .tls_name(\"myTlsName\")\n        .ca_file(\"/path/to/ca.pem\")\n    .done()\n    .with_native_credentials(\"username\", \"password\")\n    .using_services_alternate()\n)\n\nasync with await cluster.connect() as c:\n    session = c.create_session()\n    # ... use session ...\n</code></pre> <p>Mutual TLS (mTLS) with client certificate authentication:</p> <pre><code>cluster = (\n    ClusterDefinition(\"localhost\", 4333)\n    .with_tls_config_of()\n        .tls_name(\"myTlsName\")\n        .ca_file(\"/path/to/ca.pem\")\n        .client_cert_file(\"/path/to/client-cert.pem\")\n        .client_key_file(\"/path/to/client-key.pem\")\n    .done()\n    .with_native_credentials(\"username\", \"password\")\n    .using_services_alternate()\n)\n\nasync with await cluster.connect() as c:\n    session = c.create_session()\n    # ... use session ...\n</code></pre> <p>You can also configure TLS directly on a <code>FluentClient</code> by passing a <code>ClientPolicy</code> with <code>tls_config</code> set:</p> <pre><code>from aerospike_async import ClientPolicy, TlsConfig, AuthMode\nfrom aerospike_fluent import FluentClient\n\npolicy = ClientPolicy()\npolicy.tls_config = TlsConfig(\"/path/to/ca.pem\")\npolicy.set_auth_mode(AuthMode.INTERNAL, user=\"admin\", password=\"admin\")\npolicy.use_services_alternate = True\n\nasync with FluentClient(\"localhost:myTlsName:4333\", policy) as client:\n    session = client.create_session()\n    # ... use session ...\n</code></pre> <p>Note</p> <p>When using TLS, the seed string format is <code>host:tls_name:port</code> (three parts). The <code>tls_name</code> must match the server's configured TLS name for certificate validation. With <code>ClusterDefinition</code>, setting <code>tls_name()</code> on the builder automatically applies it to all hosts.</p>"},{"location":"guide/connecting/#rack-awareness","title":"Rack Awareness","text":"<pre><code>cluster = (\n    ClusterDefinition(\"localhost\", 3000)\n    .preferring_racks(1, 2)\n    .connect()\n)\n</code></pre>"},{"location":"guide/connecting/#sessions","title":"Sessions","text":"<p>A Session binds a connected client to a set of policy defaults via a <code>Behavior</code>. All reads and writes go through a session.</p> <pre><code>from aerospike_fluent import Behavior\n\nsession = client.create_session(Behavior.DEFAULT)\nfast_session = client.create_session(Behavior.READ_FAST)\nconsistent_session = client.create_session(Behavior.STRICTLY_CONSISTENT)\n</code></pre> <p>Tip</p> <p>Create different sessions for different workloads. A \"fast read\" session with short timeouts and a \"batch import\" session with longer timeouts can coexist on the same client.</p>"},{"location":"guide/connecting/#behaviors","title":"Behaviors","text":"<p>Predefined behaviors:</p> Behavior Description <code>Behavior.DEFAULT</code> Balanced defaults <code>Behavior.READ_FAST</code> Low-latency reads <code>Behavior.STRICTLY_CONSISTENT</code> Strong consistency mode <code>Behavior.FAST_RACK_AWARE</code> Prefer local rack for reads <p>Custom behaviors via derivation:</p> <pre><code>my_behavior = Behavior.DEFAULT.derive_with_changes(\n    total_timeout_ms=5000,\n    max_retries=3,\n)\n</code></pre>"},{"location":"guide/connecting/#datasets","title":"DataSets","text":"<p>A <code>DataSet</code> represents a namespace + set pair and is a convenient key factory:</p> <pre><code>from aerospike_fluent import DataSet\n\nusers = DataSet.of(\"test\", \"users\")\n\nkey = users.id(42)                       # single key\nkeys = users.ids(1, 2, 3)               # list of keys\ndigest_key = users.id_from_digest(b\"...\")  # key from raw digest\n</code></pre> <p>Pass datasets to session methods to avoid repeating namespace/set strings:</p> <pre><code>stream = await session.query(users).execute()\nawait session.upsert(users.id(1)).put({\"name\": \"Alice\"}).execute()\n</code></pre>"},{"location":"guide/error-handling/","title":"Error Handling","text":""},{"location":"guide/error-handling/#exception-hierarchy","title":"Exception Hierarchy","text":"<p>All Aerospike errors extend <code>AerospikeError</code>:</p> <pre><code>AerospikeError\n\u251c\u2500\u2500 TimeoutError\n\u251c\u2500\u2500 ConnectionError\n\u251c\u2500\u2500 SerializationError\n\u251c\u2500\u2500 AuthenticationError\n\u251c\u2500\u2500 AuthorizationError\n\u251c\u2500\u2500 SecurityError\n\u251c\u2500\u2500 GenerationError\n\u251c\u2500\u2500 InvalidNamespaceError\n\u251c\u2500\u2500 InvalidNodeError\n\u251c\u2500\u2500 BackoffError\n\u251c\u2500\u2500 CommitError\n\u251c\u2500\u2500 QuotaError\n\u2514\u2500\u2500 QueryTerminatedError\n</code></pre> <p>Import from the top-level package:</p> <pre><code>from aerospike_fluent import AerospikeError, TimeoutError, GenerationError\n</code></pre>"},{"location":"guide/error-handling/#default-behavior","title":"Default Behavior","text":"<p>The default error handling depends on the operation type:</p> Operation Default Behavior Single-key (1 key) Raise Exception raised immediately Multi-key (batch) In-stream Errors embedded in <code>RecordResult</code> Set query / scan In-stream Errors embedded in <code>RecordResult</code>"},{"location":"guide/error-handling/#checking-results","title":"Checking Results","text":"<pre><code>stream = await session.query(*users.ids(1, 2, 3)).execute()\n\nasync for result in stream:\n    if result.is_ok:\n        print(result.record.bins)\n    else:\n        print(f\"Key failed: {result.result_code}\")\n</code></pre> <p>Or raise on any failure:</p> <pre><code>async for result in stream:\n    record = result.record_or_raise()  # raises AerospikeError on failure\n</code></pre>"},{"location":"guide/error-handling/#errorstrategy","title":"ErrorStrategy","text":"<p>Override the default with <code>on_error</code> at execute time:</p> <pre><code>from aerospike_fluent import ErrorStrategy\n\n# Force in-stream errors for single-key operations\nstream = await (\n    session.query(users.id(1))\n    .execute(on_error=ErrorStrategy.IN_STREAM)\n)\n# result.is_ok will be False instead of raising\n</code></pre>"},{"location":"guide/error-handling/#errorhandler-callback","title":"ErrorHandler (Callback)","text":"<p>Route errors to a custom callback:</p> <pre><code>from aerospike_fluent import ErrorHandler\n\ndef my_handler(result):\n    log.warning(f\"Record error: {result.result_code} for {result.record.key}\")\n\nstream = await (\n    session.query(*users.ids(1, 2, 3))\n    .execute(on_error=ErrorHandler(my_handler))\n)\n# Errors go to my_handler; only successes appear in the stream\n</code></pre>"},{"location":"guide/error-handling/#common-patterns","title":"Common Patterns","text":""},{"location":"guide/error-handling/#optimistic-locking","title":"Optimistic Locking","text":"<pre><code>try:\n    await (\n        session.update(users.id(1))\n        .ensure_generation_is(expected_gen)\n        .bin(\"balance\").set_to(new_balance)\n        .execute()\n    )\nexcept GenerationError:\n    # Record was modified since we read it \u2014 retry\n    pass\n</code></pre>"},{"location":"guide/error-handling/#conditional-write-with-filter","title":"Conditional Write with Filter","text":"<pre><code>from aerospike_fluent import AerospikeError\n\nstream = await (\n    session.update(users.id(1))\n    .where(\"$.balance &gt;= 100\")\n    .fail_on_filtered_out()\n    .bin(\"balance\").increment_by(-100)\n    .execute()\n)\ntry:\n    result = await stream.first_or_raise()\nexcept AerospikeError:\n    print(\"Insufficient balance or record filtered out\")\n</code></pre>"},{"location":"guide/expression-dsl/","title":"Expression DSL","text":"<p>The expression DSL lets you write Aerospike filter expressions as strings. Pass a DSL string to <code>.where()</code> on any query or write builder.</p> <pre><code>stream = await session.query(users).where(\"$.age &gt; 18\").execute()\n</code></pre>"},{"location":"guide/expression-dsl/#syntax-reference","title":"Syntax Reference","text":""},{"location":"guide/expression-dsl/#bin-access","title":"Bin Access","text":"<p>Prefix bin names with <code>$</code>:</p> <pre><code>$.age\n$.name\n$.settings\n</code></pre>"},{"location":"guide/expression-dsl/#comparison-operators","title":"Comparison Operators","text":"<pre><code>$.age == 30\n$.age != 30\n$.age &gt; 18\n$.age &gt;= 18\n$.age &lt; 65\n$.age &lt;= 65\n</code></pre>"},{"location":"guide/expression-dsl/#logical-operators","title":"Logical Operators","text":"<pre><code>$.age &gt; 18 and $.status == \"active\"\n$.role == \"admin\" or $.role == \"superadmin\"\nnot $.deleted\n</code></pre>"},{"location":"guide/expression-dsl/#arithmetic","title":"Arithmetic","text":"<pre><code>$.price * $.quantity &gt; 1000\n$.score + $.bonus &gt;= 100\n$.total - $.discount &gt; 0\n$.value % 2 == 0\n$.base ** 2 &gt; 100\n</code></pre> <p>Arithmetic functions:</p> <pre><code>abs($.balance) &gt; 100\nceil($.rating)\nfloor($.rating)\nlog($.value)\npow($.base, 2)\nmax($.a, $.b)\nmin($.a, $.b)\n</code></pre>"},{"location":"guide/expression-dsl/#bitwise-operators","title":"Bitwise Operators","text":"<pre><code>$.flags &amp; 0xFF\n$.mask | 0x01\n$.value ^ 0xAA\n~$.mask\n$.bits &lt;&lt; 4\n$.bits &gt;&gt; 2\n$.bits &gt;&gt;&gt; 2\n</code></pre>"},{"location":"guide/expression-dsl/#type-casting","title":"Type Casting","text":"<pre><code>$.count.asFloat() &gt; 3.14\n5.asFloat()\n3.14.asInt()\n</code></pre>"},{"location":"guide/expression-dsl/#string-values","title":"String Values","text":"<p>Use double or single quotes:</p> <pre><code>$.name == \"Alice\"\n$.name == 'Alice'\n</code></pre>"},{"location":"guide/expression-dsl/#list-membership-in","title":"List Membership (IN)","text":"<pre><code>$.status in [\"active\", \"pending\", \"review\"]\n\"gold\" in $.tiers\n</code></pre>"},{"location":"guide/expression-dsl/#cdt-paths","title":"CDT Paths","text":"<p>Access nested data with bracket notation:</p> <pre><code>$.settings.[\"theme\"] == \"dark\"\n$.scores.[0] &gt; 90\n$.matrix.[0].[1] == 42\n$.users.[\"alice\"].age &gt; 30\n</code></pre>"},{"location":"guide/expression-dsl/#cdt-functions","title":"CDT Functions","text":"<pre><code>$.scores.count() &gt; 5\n$.tags.count() == 0\n</code></pre>"},{"location":"guide/expression-dsl/#hex-and-binary-literals","title":"Hex and Binary Literals","text":"<pre><code>$.flags == 0xFF\n$.mask == 0b10101010\n</code></pre>"},{"location":"guide/expression-dsl/#variables-letthen","title":"Variables (let/then)","text":"<p>Bind intermediate values:</p> <pre><code>let $total = $.price * $.qty then $total &gt; 1000\n</code></pre>"},{"location":"guide/expression-dsl/#placeholders","title":"Placeholders","text":"<p>Use <code>?0</code>, <code>?1</code>, etc. for parameterized queries:</p> <pre><code>from aerospike_fluent import parse_dsl\n\nexpr = parse_dsl(\"$.age &gt; ?0 and $.status == ?1\", 18, \"active\")\n</code></pre>"},{"location":"guide/expression-dsl/#auto-index-discovery","title":"Auto Index Discovery","text":"<p>When a secondary index exists on a bin referenced in the DSL expression, the client automatically generates an optimal secondary index <code>Filter</code> alongside the <code>FilterExpression</code>. This is transparent \u2014 no code changes needed.</p> <p>To influence index selection, use <code>QueryHint</code>:</p> <pre><code>from aerospike_fluent import QueryHint\n\nstream = await (\n    session.query(users)\n    .where(\"$.age &gt; 25 and $.city == 'NYC'\")\n    .with_hint(QueryHint(index_name=\"age_idx\"))\n    .execute()\n)\n</code></pre>"},{"location":"guide/expression-dsl/#programmatic-expressions","title":"Programmatic Expressions","text":"<p>For cases where the string DSL is insufficient, use the <code>Exp</code> builder or raw <code>FilterExpression</code> from <code>aerospike_async</code>:</p> <pre><code>from aerospike_fluent import Exp\n\nexpr = Exp.and_([\n    Exp.gt(Exp.int_bin(\"age\"), Exp.int_val(18)),\n    Exp.eq(Exp.string_bin(\"status\"), Exp.string_val(\"active\")),\n])\n\nstream = await session.query(users).where(expr).execute()\n</code></pre>"},{"location":"guide/indexes/","title":"Secondary Indexes","text":"<p>Secondary indexes enable efficient queries on bin values. The fluent client provides both manual index management and automatic index discovery.</p>"},{"location":"guide/indexes/#creating-indexes","title":"Creating Indexes","text":"<pre><code>users = DataSet.of(\"test\", \"users\")\n\n# Numeric index\nawait (\n    session.index(users)\n    .on_bin(\"age\")\n    .named(\"users_age_idx\")\n    .numeric()\n    .create()\n)\n\n# String index\nawait (\n    session.index(users)\n    .on_bin(\"city\")\n    .named(\"users_city_idx\")\n    .string()\n    .create()\n)\n\n# Collection index (list elements)\nfrom aerospike_async import CollectionIndexType\n\nawait (\n    session.index(users)\n    .on_bin(\"tags\")\n    .named(\"users_tags_idx\")\n    .collection(CollectionIndexType.LIST)\n    .create()\n)\n</code></pre>"},{"location":"guide/indexes/#dropping-indexes","title":"Dropping Indexes","text":"<pre><code>await session.index(users).named(\"users_age_idx\").drop()\n</code></pre>"},{"location":"guide/indexes/#auto-index-discovery","title":"Auto-Index Discovery","text":"<p>The <code>IndexesMonitor</code> runs as a background task, periodically fetching secondary index metadata from the cluster. When you use <code>.where()</code> with a DSL expression, the client automatically generates an optimal secondary index <code>Filter</code> if a matching index exists.</p> <p>This is transparent \u2014 no code changes needed:</p> <pre><code># If \"users_age_idx\" exists on the \"age\" bin, this query\n# automatically uses it as a secondary index filter\nstream = await (\n    session.query(users)\n    .where(\"$.age &gt; 25\")\n    .execute()\n)\n</code></pre>"},{"location":"guide/indexes/#configuration","title":"Configuration","text":"<p>The refresh interval defaults to 5 seconds:</p> <pre><code>client = FluentClient(\"localhost:3000\", index_refresh_interval=2.0)\n</code></pre>"},{"location":"guide/indexes/#explicit-override","title":"Explicit Override","text":"<p>Use <code>with_index_context()</code> to explicitly provide index metadata, bypassing auto-discovery:</p> <pre><code>from aerospike_fluent import IndexContext, Index, IndexTypeEnum\n\nctx = IndexContext(indexes=[\n    Index(name=\"age_idx\", bin_name=\"age\", index_type=IndexTypeEnum.NUMERIC)\n])\n\nstream = await (\n    session.query(users)\n    .with_index_context(ctx)\n    .where(\"$.age &gt; 25\")\n    .execute()\n)\n</code></pre>"},{"location":"guide/indexes/#query-hints","title":"Query Hints","text":"<p>Influence which index the server uses with <code>QueryHint</code>:</p> <pre><code>from aerospike_fluent import QueryHint\n\n# Force a specific index\nstream = await (\n    session.query(users)\n    .where(\"$.age &gt; 25 and $.city == 'NYC'\")\n    .with_hint(QueryHint(index_name=\"users_city_idx\"))\n    .execute()\n)\n\n# Hint by bin name\nstream = await (\n    session.query(users)\n    .where(\"$.age &gt; 25 and $.city == 'NYC'\")\n    .with_hint(QueryHint(bin_name=\"city\"))\n    .execute()\n)\n</code></pre>"},{"location":"guide/logging/","title":"Logging","text":"<p>The fluent client produces logs from three layers, each identified by its Python logger name:</p> Logger name Layer Description <code>aerospike_core</code> Rust core Cluster management, connection pooling, wire protocol <code>aerospike_async</code> Python Async Client (PAC) Client init, connection lifecycle <code>aerospike_fluent</code> Python Fluent Client (PFC) Session operations, DSL parsing, index monitoring <p>All three layers use Python's standard <code>logging</code> module, so any handler or formatter you configure will receive messages from all layers.</p>"},{"location":"guide/logging/#quick-start","title":"Quick Start","text":"<p>The simplest way to enable logging is via environment variables:</p> <pre><code>export AEROSPIKE_LOG_LEVEL=DEBUG\nexport AEROSPIKE_LOG_FILE=/tmp/aerospike.log   # optional; omit for stderr\n</code></pre> <p>The test suite and example scripts read these variables automatically (see <code>conftest.py</code> and <code>examples/_env.py</code>).</p>"},{"location":"guide/logging/#programmatic-configuration","title":"Programmatic Configuration","text":"<p>For full control, configure the loggers directly:</p> <pre><code>import logging\n\nhandler = logging.StreamHandler()\nhandler.setFormatter(logging.Formatter(\n    \"%(asctime)s %(levelname)-8s %(name)s: %(message)s\"\n))\n\nfor name in (\"aerospike_core\", \"aerospike_async\", \"aerospike_fluent\"):\n    logger = logging.getLogger(name)\n    logger.setLevel(logging.DEBUG)\n    logger.addHandler(handler)\n</code></pre> <p>You can set different levels per layer \u2014 for example, <code>WARNING</code> for the core and <code>DEBUG</code> for the fluent client:</p> <pre><code>logging.getLogger(\"aerospike_core\").setLevel(logging.WARNING)\nlogging.getLogger(\"aerospike_fluent\").setLevel(logging.DEBUG)\n</code></pre>"},{"location":"guide/logging/#log-output","title":"Log Output","text":"<p>A typical log line looks like:</p> <pre><code>2026-03-30 14:22:01,234 INFO     aerospike_async: connecting to 127.0.0.1:3100\n2026-03-30 14:22:01,289 INFO     aerospike_async: connected to 127.0.0.1:3100\n2026-03-30 14:22:01,290 DEBUG    aerospike_core.cluster: Tending cluster...\n</code></pre> <p>The <code>%(name)s</code> field identifies which layer produced the message, making it straightforward to filter or route logs.</p>"},{"location":"guide/logging/#file-logging","title":"File Logging","text":"<p>To write logs to a file instead of stderr:</p> <pre><code>handler = logging.FileHandler(\"/tmp/aerospike.log\")\nhandler.setFormatter(logging.Formatter(\n    \"%(asctime)s %(levelname)-8s %(name)s: %(message)s\"\n))\n\nfor name in (\"aerospike_core\", \"aerospike_async\", \"aerospike_fluent\"):\n    logger = logging.getLogger(name)\n    logger.setLevel(logging.DEBUG)\n    logger.addHandler(handler)\n</code></pre> <p>Or via environment variables in <code>aerospike.env</code>:</p> <pre><code>export AEROSPIKE_LOG_LEVEL=DEBUG\nexport AEROSPIKE_LOG_FILE=/tmp/aerospike.log\n</code></pre>"},{"location":"guide/logging/#recommended-levels","title":"Recommended Levels","text":"Level Use case <code>WARNING</code> (default) Production \u2014 only unexpected conditions <code>INFO</code> Connection lifecycle, index creation events <code>DEBUG</code> Full detail including cluster tend cycles, expression parsing"},{"location":"guide/reads/","title":"Reading Data","text":"<p>All reads go through <code>session.query()</code>, which returns a <code>QueryBuilder</code>. Chain methods to configure the query, then call <code>.execute()</code> to get a <code>RecordStream</code>.</p>"},{"location":"guide/reads/#point-read-single-key","title":"Point Read (Single Key)","text":"<pre><code>users = DataSet.of(\"test\", \"users\")\n\nstream = await session.query(users.id(1)).execute()\nresult = await stream.first_or_raise()\nprint(result.record.bins)  # {'name': 'Alice', 'age': 30}\n</code></pre>"},{"location":"guide/reads/#set-scan-all-records","title":"Set Scan (All Records)","text":"<pre><code>stream = await session.query(users).execute()\nasync for result in stream:\n    print(result.record.bins)\nstream.close()\n</code></pre>"},{"location":"guide/reads/#batch-read-multiple-keys","title":"Batch Read (Multiple Keys)","text":"<pre><code>stream = await session.query(*users.ids(1, 2, 3)).execute()\nasync for result in stream:\n    print(result.record.key, result.record.bins)\nstream.close()\n</code></pre>"},{"location":"guide/reads/#selecting-bins","title":"Selecting Bins","text":"<p>Return only specific bins to reduce network transfer:</p> <pre><code>stream = await session.query(users).bins([\"name\", \"age\"]).execute()\n</code></pre> <p>Or exclude all bins (metadata only):</p> <pre><code>stream = await session.query(users).with_no_bins().execute()\n</code></pre>"},{"location":"guide/reads/#filtering-with-dsl","title":"Filtering with DSL","text":"<p>Use the Expression DSL to filter records server-side:</p> <pre><code>stream = await (\n    session.query(users)\n    .where(\"$.age &gt; 25 and $.status == 'active'\")\n    .execute()\n)\n</code></pre> <p>Or with a pre-built <code>FilterExpression</code>:</p> <pre><code>from aerospike_fluent import Exp\n\nexpr = Exp.and_([\n    Exp.gt(Exp.int_bin(\"age\"), Exp.int_val(25)),\n    Exp.eq(Exp.string_bin(\"status\"), Exp.string_val(\"active\")),\n])\nstream = await session.query(users).where(expr).execute()\n</code></pre>"},{"location":"guide/reads/#partition-filtering","title":"Partition Filtering","text":"<p>Query specific partitions for parallel consumption:</p> <pre><code>stream = await (\n    session.query(users)\n    .on_partitions(0, 1, 2)\n    .execute()\n)\n</code></pre> <p>Or a contiguous range:</p> <pre><code>stream = await (\n    session.query(users)\n    .on_partition_range(begin=0, count=1024)\n    .execute()\n)\n</code></pre>"},{"location":"guide/reads/#query-policies","title":"Query Policies","text":"<p>Fine-tune query behavior:</p> <pre><code>from aerospike_async import QueryDuration\n\nstream = await (\n    session.query(users)\n    .where(\"$.age &gt; 18\")\n    .expected_duration(QueryDuration.LONG)\n    .chunk_size(500)\n    .execute()\n)\n</code></pre>"},{"location":"guide/reads/#recordresult","title":"RecordResult","text":"<p>Each item in the stream is a <code>RecordResult</code>:</p> <pre><code>async for result in stream:\n    if result.is_ok:\n        record = result.record\n        print(record.key, record.bins, record.generation, record.expiration)\n    else:\n        print(f\"Error: {result.result_code}\")\n</code></pre> <p>Use <code>record_or_raise()</code> to raise on error results:</p> <pre><code>async for result in stream:\n    record = result.record_or_raise()\n</code></pre>"},{"location":"guide/reads/#query-hints","title":"Query Hints","text":"<p>Influence secondary index selection with <code>QueryHint</code>:</p> <pre><code>from aerospike_fluent import QueryHint\n\nstream = await (\n    session.query(users)\n    .where(\"$.age &gt; 25\")\n    .with_hint(QueryHint(index_name=\"age_idx\"))\n    .execute()\n)\n</code></pre>"},{"location":"guide/writes/","title":"Writing Data","text":"<p>All writes go through session entry points that return a <code>WriteSegmentBuilder</code>. Chain bin operations, then call <code>.execute()</code>.</p>"},{"location":"guide/writes/#write-verbs","title":"Write Verbs","text":"Method Behavior Record must exist? <code>upsert()</code> Create or update No <code>insert()</code> Create only No (fails if exists) <code>update()</code> Update only Yes (fails if missing) <code>replace()</code> Replace all bins Yes (fails if missing) <code>delete()</code> Remove record No <code>touch()</code> Reset TTL Yes <code>exists()</code> Check existence No"},{"location":"guide/writes/#bin-chaining","title":"Bin Chaining","text":"<p>Set individual bins with the fluent <code>.bin().set_to()</code> pattern:</p> <pre><code>users = DataSet.of(\"test\", \"users\")\n\nawait (\n    session.upsert(users.id(1))\n    .bin(\"name\").set_to(\"Alice\")\n    .bin(\"age\").set_to(30)\n    .bin(\"active\").set_to(True)\n    .execute()\n)\n</code></pre>"},{"location":"guide/writes/#dict-pattern","title":"Dict Pattern","text":"<p>Set multiple bins at once with <code>.put()</code>:</p> <pre><code>await (\n    session.upsert(users.id(1))\n    .put({\"name\": \"Alice\", \"age\": 30, \"active\": True})\n    .execute()\n)\n</code></pre>"},{"location":"guide/writes/#increment","title":"Increment","text":"<pre><code>await (\n    session.update(users.id(1))\n    .bin(\"login_count\").increment_by(1)\n    .execute()\n)\n</code></pre>"},{"location":"guide/writes/#insert-fail-if-exists","title":"Insert (Fail if Exists)","text":"<pre><code>stream = await (\n    session.insert(users.id(99))\n    .put({\"name\": \"New User\"})\n    .execute()\n)\nresult = await stream.first_or_raise()\n</code></pre>"},{"location":"guide/writes/#replace-overwrite-all-bins","title":"Replace (Overwrite All Bins)","text":"<pre><code>await (\n    session.replace(users.id(1))\n    .put({\"name\": \"Alice Updated\"})\n    .execute()\n)\n# Only \"name\" bin remains; \"age\" and \"active\" are removed\n</code></pre>"},{"location":"guide/writes/#delete","title":"Delete","text":"<pre><code># Single key\nawait session.delete(users.id(1)).execute()\n\n# Multiple keys\nawait session.delete(*users.ids(1, 2, 3)).execute()\n\n# Durable delete\nawait session.delete(users.id(5)).durably_delete().execute()\n</code></pre>"},{"location":"guide/writes/#conditional-writes","title":"Conditional Writes","text":"<p>Filter writes server-side with <code>.where()</code>:</p> <pre><code>await (\n    session.update(users.id(1))\n    .where(\"$.age &gt;= 18\")\n    .bin(\"verified\").set_to(True)\n    .execute()\n)\n</code></pre> <p>Records that don't match the filter are skipped. Use <code>.fail_on_filtered_out()</code> to raise an error instead:</p> <pre><code>await (\n    session.update(users.id(1))\n    .where(\"$.age &gt;= 18\")\n    .fail_on_filtered_out()\n    .bin(\"verified\").set_to(True)\n    .execute()\n)\n</code></pre>"},{"location":"guide/writes/#generation-check-optimistic-locking","title":"Generation Check (Optimistic Locking)","text":"<pre><code>await (\n    session.update(users.id(1))\n    .ensure_generation_is(5)\n    .bin(\"balance\").set_to(100)\n    .execute()\n)\n</code></pre>"},{"location":"guide/writes/#ttl-expiration","title":"TTL / Expiration","text":"<pre><code>await (\n    session.upsert(users.id(1))\n    .expire_record_after_seconds(3600)\n    .put({\"session_token\": \"abc123\"})\n    .execute()\n)\n</code></pre>"},{"location":"guide/writes/#batch-writes","title":"Batch Writes","text":"<p>Multiple keys with the same operation:</p> <pre><code>await (\n    session.upsert(*users.ids(1, 2, 3))\n    .bin(\"status\").set_to(\"migrated\")\n    .execute()\n)\n</code></pre> <p>Mixed operations across different keys are handled automatically when you chain multiple write segments.</p>"}]}